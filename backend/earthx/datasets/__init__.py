"""Dataset specifics that no generic operator covers; stays isolated.

Per-dataset submodules (`datasets.<id>`) are added as datasets are onboarded
(M1-04 onward). Nothing generic imports from here.
"""
