# Development and CI extras on top of requirements.txt.
-r requirements.txt
pytest>=8.2
ruff>=0.6
# Module boundary contracts (architekturplan.md 3.1). Configured in .importlinter
# but not enforced yet — see docs/adr/0002-testaufteilung.md.
import-linter>=2.0
