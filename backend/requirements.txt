fastapi>=0.110
uvicorn[standard]>=0.29
rasterio>=1.3
# Exact pin, not a range (adr/0006 §5 "Zu Frage 1"): rio-tiler is the one package in
# the tile path that brings an HTTP client of its own (`httpx2`, through
# `rio_tiler.io.stac`, measured in §3.2), and the reader interface titiler builds on
# is its public surface. A minor bump there moves both at once.
rio-tiler==9.4.6
rio-cogeo>=5.2
shapely>=2.0
httpx>=0.27
python-dotenv>=1.0
pydantic-settings>=2.2
# pgstac schema migration for the compose `pgstac-migrate` service
# (docs/plans/m1-08-compose-topologie.md). Exact pin, not a range: this is
# the only place the pgstac version is set (docker-compose.yml has none of
# its own, it just runs `pypgstac migrate` from this image) and M1-04 builds
# on it, so it must not drift on its own. Matches the version measured
# working in docs/cloud-umgebung.md §5.
pypgstac[psycopg]==0.9.12
# The STAC API this repo serves outward (M1-07; docs/plans/m1-07-stac-api.md).
# Pinned exact, like pypgstac above: it brings its own DB driver (asyncpg +
# buildpg, plan §5) and its extension set decides which STAC conformance
# classes we advertise (§6) — not something to let drift on its own. 6.4.1 is
# verified against the pypgstac 0.9.12 schema pinned above, against a real
# Postgres, not just read off a changelog.
stac-fastapi.pgstac==6.4.1
# The tile path of M2-04 (docs/adr/0006-kachel-pfad.md, Option B). Exact pin, and
# deliberately only `titiler.core`: `titiler.application` and
# `titiler.mosaic[mosaicjson]` bring `httpx2`/`boto3`, `titiler.xarray` brings
# `obstore` — each an HTTP client that would reach past `gateway` (§3.2). `titiler.core`
# itself imports none. The version was five days old when it was measured, which is
# why the pin is necessary rather than tidy (§8 point 6).
titiler.core==2.3.0
# The Zarr read path of M2-09a (docs/adr/0007-zweites-format-zarr.md §3.7, §6).
# `zarr` is capped below 3.2 rather than left open: 3.1.6 is the last release that
# runs on Python 3.11, and 3.2 upwards require 3.12 (§3.8). The measured path is
# exactly this trio, and moving to 3.12 is its own task (D17).
zarr>=3.1,<3.2
xarray>=2025.1
# Only for the `.rio` accessor: `XarrayReader` reads the CRS off it, and a Zarr
# store need not carry one of its own (§3.4). No second I/O path comes with it —
# rio-tiler already imports rioxarray for the same reason.
rioxarray>=0.17
# FederatingCoreCrudClient's own registry check and search cache (M1-07) need
# a pool, not a single connection; pypgstac[psycopg] above pulls in psycopg
# itself already, this only adds the pooling extra.
psycopg[pool]>=3.1
