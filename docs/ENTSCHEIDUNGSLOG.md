# EarthX — Entscheidungslog

Chronologisches Verzeichnis aller Entscheidungen. Eine Zeile pro Entscheidung. Architekturentscheidungen verweisen auf ihr ADR unter `docs/adr/`, sobald es eines gibt.

Status:
- **fest**: von Otto entschieden.
- **Vorschlag**: gilt, bis Otto widerspricht.
- **offen**: noch nicht entschieden.
- **aufgehoben**: gilt nicht mehr; die Zeile bleibt als Historie stehen und nennt, was sie ersetzt.

Bei Widerspruch geht `ENTSCHEIDUNGEN_2026-09-18.md` allen anderen Dokumenten vor.

| Datum | Entscheidung | Status | Betroffene Dokumente |
|---|---|---|---|
| 2026-08-13 | Sieben Hard Constraints schützen das BIOMASS-Verhalten; `decomp.py` wird nie generalisiert | aufgehoben am 2026-09-18 | ADDING_ESA_DATASETS |
| 2026-08-13 | Format-Hierarchie Zarr > COG > Altformate; eigenes `zarr_reader.py`; `DatasetConfig`-Registry mit `format` | fest | ADDING_ESA_DATASETS, architekturplan |
| 2026-08 | Vorerst nur token-freie Datenquellen; Adapter-Interface entsteht aus realen Quellen | fest | projektuebersicht |
| 2026-09-18 | Alter Sechs-Phasen-Plan entfällt; es gelten Projektübersicht, Architekturplan, Projektplan | fest | alle |
| 2026-09-18 | Grundprinzip Skalierbarkeit und Cloud-Portabilität | fest | projektuebersicht 2.16, architekturplan 13 |
| 2026-09-18 | Nutzer wählt zwischen Cloud- und lokalem Processing; lokaler Runner als Container | fest | architekturplan 7.7 |
| 2026-09-18 | Umsetzung autonom mit Claude Code in der Cloud; Modellzuordnung nach Aufgabentyp | fest | projektplan 2, 3 |
| 2026-09-18 | Bug-Report-Pipeline von Anfang an: erst Triage (Bug oder Bedienfehler), Fix nur mit reproduzierendem Test und nur als PR | fest | projektplan 6.1 |
| 2026-09-18 | DOI wo vorhanden, sonst persistente Zitierangabe; Swipe-Vergleich statt freiem Skalieren; schlanker Export statt Plot-Editor | Vorschlag | projektuebersicht 15 |
| 2026-09-18 | Auflösungen B1 bis B14 in KLAERUNGEN | teilweise aufgehoben, siehe die folgenden Zeilen | KLAERUNGEN |
| 2026-09-18 | Die fertige Plattform enthält nichts, was einen Token braucht; BIOMASS kommt dort nicht vor; kein Aufwand, das BIOMASS-Verhalten zu erhalten | fest | ENTSCHEIDUNGEN_2026-09-18 §1; projektuebersicht, architekturplan, projektplan, UEBERGABE anzupassen |
| 2026-09-18 | Die sieben Hard Constraints sind als verbindliche Regeln aufgehoben; `ADDING_ESA_DATASETS.md` gilt nur noch als Beschreibung des Code-Stands vom 13.08.2026 (Abschnitt 1) | fest | ENTSCHEIDUNGEN §1; ADDING_ESA_DATASETS, KLAERUNGEN (Rangsatz) |
| 2026-09-18 | Aufgehoben: Hard-Constraint-Tests in M0, Golden-Test mit Token, CODEOWNERS für BIOMASS-Pfade, B2, B6 (nicht verschieben), B7 (BIOMASS in pgstac), A1, A4, KLAERUNGEN Abschnitt C. Bestehender Code darf umgebaut, verschoben und umbenannt werden | fest | ENTSCHEIDUNGEN §1; KLAERUNGEN |
| 2026-09-18 | Gelten weiter: B1, B3, B4, B5, B8, B9, B11, B12, B13, B14. Die BIOMASS-bezogenen Sätze darin (Token über GDAL-Umgebung in B8, BIOMASS-Eintrag behält seine Werte in B13) entfallen | fest | ENTSCHEIDUNGEN §5; KLAERUNGEN |
| 2026-09-18 | B10: Capability-Flags werden im Eintrag des jeweiligen Datensatzes gesetzt; jeder Datensatz schaltet jede Fähigkeit ausdrücklich frei. Die Begründung über Hard Constraint 6 und die Sonderregel für das `coverage`-Flag des BIOMASS-Eintrags entfallen | Vorschlag | KLAERUNGEN B10 |
| 2026-09-18 | Coverage Map bleibt Pflicht für jeden Datensatz, als Punkt der Onboarding-Checkliste statt als Hard Constraint | fest | ENTSCHEIDUNGEN §5; projektuebersicht §5 |
| 2026-09-18 | Der Prototyp bleibt wegen seiner Funktionen und Designentscheidungen erhalten, nicht wegen des Datensatzes; sie werden auf token-freie Datensätze übertragen | fest | ENTSCHEIDUNGEN §2 |
| 2026-09-18 | Der BIOMASS-Code bleibt als Referenz im Repo, bis seine Funktionen mit einem token-freien Datensatz laufen; das Entfernen entscheidet Otto (Stufe B). BIOMASS nur lokal bei Otto als Testdaten; nie in CI, Cloud-Sitzungen oder Fixtures | fest | ENTSCHEIDUNGEN §3 |
| 2026-09-18 | Token-Logik (`auth.py`, MAAP-OIDC, Token in der GDAL-Umgebung) wird nicht in die Zielarchitektur übernommen und nicht generalisiert; Token pro Connector ist fernes Zukunftsthema | fest | ENTSCHEIDUNGEN §3; architekturplan |
| 2026-09-18 | `decomp.py` wird als Operator mit Quad-Pol-Capability übernommen, nicht als allgemeine Funktion; bis eine token-freie Quelle existiert, ruht er mit synthetischen Tests | fest | ENTSCHEIDUNGEN §3; architekturplan 7.2 |
| 2026-09-18 | Repo ist öffentlich und bleibt es vorerst (entscheidet A2) | fest | ENTSCHEIDUNGEN §4; KLAERUNGEN A2 |
| 2026-09-18 | Bestehendes Repo wird in `earthX` umbenannt; kein neues Monorepo (entscheidet A3) | fest | ENTSCHEIDUNGEN §4; KLAERUNGEN A3, projektplan 2.1 |
| 2026-09-18 | Folgen der Öffentlichkeit: keine Secrets, Tokens, echten `.env`-Werte oder internen URLs in Repo, Issues, PRs und Logs; Git-History vor dem ersten autonomen Lauf auf Secrets prüfen; Fixtures nur synthetisch oder mit eindeutig offener Lizenz; Bug-Report-Issues nur bereinigt, Sicherheitsmeldungen nie öffentlich; Cloud-Sitzungen nur nach Prüfung teilen | fest | ENTSCHEIDUNGEN §4; projektplan 6.1 |
| 2026-09-18 | M0 neu: (1) Repo-Grundgerüst, CODEOWNERS für `.claude/`, `.github/`, `CLAUDE.md` und Sicherheitsmodule, Secret-Prüfung der History; (2) Cloud-VM klären, Testaufteilung; (3) Funktions- und Design-Inventar des Prototyps; (4) Zustands-Audit als ADR-Entwurf; (5) Bug-Report-Pipeline Stufe 1; (6) Vorschlag für den ersten token-freien Datensatz | fest | ENTSCHEIDUNGEN §6; projektplan M0 |
| 2026-09-18 | M0 Schritt 1 umgesetzt: `KLAERUNGEN.md` und die Plandokumente an die Entscheidungen vom 18.09. angepasst; `ADDING_ESA_DATASETS.md` als historisch gekennzeichnet | fest | KLAERUNGEN, projektplan, architekturplan, projektuebersicht, ADDING_ESA_DATASETS |
| 2026-09-18 | `CLAUDE.md` neu geschrieben: Rangfolge, unverrückbare Regeln, Arbeitsweise (ein Branch, ein Draft-PR, nie mergen, nie force-pushen), Schutz von `.github/`, `.claude/`, `CLAUDE.md`, `CODEOWNERS`, Fragenregel, Sprachregelung | fest | CLAUDE.md; projektplan 2.5 (Gerüst entfällt, verweist jetzt auf die Datei) |
| 2026-09-18 | Subagenten aus Projektplan 3.3 als Dateien unter `.claude/agents/` angelegt; `.claude/settings.json` mit Standardmodell Sonnet (auch für nicht zugeordnete Subagenten). Beim `reviewer` sind die Hard Constraints als Prüfpunkt gestrichen | fest | projektplan 3.3; .claude/ |
| 2026-09-18 | Git-History einmal auf Secrets geprüft (ENTSCHEIDUNGEN §4). Ergebnis und Behandlung des Fundes direkt an Otto, nicht öffentlich | fest | ENTSCHEIDUNGEN §4 |
| 2026-09-18 | Das fest eingetragene OIDC-Client-Secret in `backend/app/config.py` bleibt unverändert: kein Widerruf, keine Code-Änderung, keine History-Bereinigung. Bewusste, **befristete Ausnahme** von "keine Secrets im Repo"; sie endet mit dem Entfernen des BIOMASS-Codes | fest | CLAUDE.md; ENTSCHEIDUNGEN §4 |
| 2026-09-18 | Code-Lizenz: Wechsel von MIT auf **AGPL-3.0-or-later**. Bereits veröffentlichte Versionen bleiben unter MIT; keine Lizenz-Header in einzelnen Dateien | fest | LICENSE, README, frontend/package.json; ENTSCHEIDUNGEN §4 |
| 2026-09-18 | `ENTSCHEIDUNGEN` §2 neu gefasst: Aufzählung des zu Erhaltenden am Inventar ausgerichtet und erweitert (Upload/letzte AOI, Gruppierung, Layer-Manager, Darstellungssteuerung mit "Apply", Asset-Proxy mit Allowlist ohne Token, HUD-Design); Disk-Cache nur noch als Konzept, Umsetzung wird ersetzt; Coverage Map als Heatmap der Abdeckungsdichte mit Footprints, Ausdehnung und Zeit-Histogramm als Ergänzungen und eigenem Umsetzungs-ADR; neu zu bauen: Theme-Umschalter mit heller Basiskarte und Datei-Download des AOI-Zuschnitts | fest | ENTSCHEIDUNGEN §2; prototyp-inventar, architekturplan, projektuebersicht |
| 2026-09-18 | Coverage Map als Heatmap, reagiert auf Filter (Zeitraum, Suchkriterien) | fest | ENTSCHEIDUNGEN §2 |
| — | Technische Umsetzung der Coverage Map (Aggregation pro Anfrage, vorberechnete Zeitscheiben, Vektorkacheln oder anderes) | **entschieden** am 2026-09-19 mit `adr/0004` (angenommen); siehe die Zeile „Antworten zu adr/0004 §7" | ENTSCHEIDUNGEN §2; adr/0004 |
| 2026-09-18 | M0 Schritt 4: Zustands-Audit des Prototyps als ADR-Entwurf `docs/adr/0001-zustand-im-prototyp.md` (Z1–Z9 Backend, FZ1–FZ7 Frontend). Empfehlung: zustandslose Dienste, Zustand in die Ebenen aus Architekturplan 12.3 — Item-Registry nach pgstac, Zuschnitte in den Objektspeicher mit Ablauf, Streckbereich in die Kachel-URL, LRU-Index und Token-Cache entfallen | Vorschlag, Entscheidung in M1 (B1) | adr/0001; architekturplan 3.2, 12.3, 13; KLAERUNGEN B1, B9 |
| 2026-09-18 | M0 Schritt 2, Befund: Die Cloud-Umgebung bietet Python 3.11 ohne conda, Node 22, die Backend-Abhängigkeiten per pip inklusive GDAL 3.10 aus den Wheels, Postgres 16 mit PostGIS 3.4 und pgstac. Docker startet, kann aber keine Images laden. Keine EO-Quelle und kein Geocoder erreichbar | fest (gemessen) | cloud-umgebung; projektplan 2.3 (Rückfallebene für Postgres entfällt) |
| 2026-09-18 | Setup-Skript `scripts/setup-cloud-session.sh` vorgeschlagen (venv per pip statt `environment.yml`, npm ci, Postgres mit PostGIS). Eintragen in die Cloud-Umgebung macht Otto selbst | Vorschlag | cloud-umgebung; projektplan 2.3 |
| 2026-09-18 | Testaufteilung als ADR-Entwurf `docs/adr/0002-testaufteilung.md`: T-A Unit/Rechen und T-B Verträge und T-C Integration mit Postgres laufen in Cloud-Sitzung und CI; T-D Live-Smoke nur zeitgesteuert in GitHub Actions; was Token, echte Szenen oder einen Container braucht, ist `local_only` und gilt als ungeprüft. Netzsperre in Tests per autouse-Fixture, die sich selbst prüft | Vorschlag | adr/0002; projektplan 2.4, 7 |
| 2026-09-18 | CI-Grundgerüst `.github/workflows/ci.yml`: Backend-Lint (ruff, zunächst enger Regelsatz E4/E7/E9/F/I/B) und pytest, Frontend-Lint (oxlint) und Typprüfung (tsc). Erste Tests: App startet, Konfiguration lädt, keine Netzwerkzugriffe. Kein Job braucht MAAP, BIOMASS oder ein Secret | fest | .github/workflows/ci.yml; pyproject.toml; backend/tests |
| 2026-09-18 | Modulgrenzen aus Architekturplan 3.1 als `.importlinter`-Verträge hinterlegt, aber **nicht scharf**: die Zielmodule gibt es noch nicht. Der Job `import-boundaries` läuft nur manuell; ein PR-Test hält die Verträge mit der Tabelle in 3.1 deckungsgleich. Scharfschalten mit M1 | Vorschlag | .importlinter; adr/0002; architekturplan 3.1 |
| — | Wurzelpaketname der Zielarchitektur (`.importlinter` nimmt vorläufig `app` an) | entschieden, siehe Zeile „E1" 2026-09-19 | adr/0002; architekturplan 3.1 |
| — | Ob `production.cloudfront.docker.com` und `pkg-containers.githubusercontent.com` in die Allowlist der Cloud-Umgebung sollen, damit Docker dort nutzbar wird. Empfehlung: vorerst nein | entschieden am 2026-09-23: vorerst nein | cloud-umgebung §4 |
| — | Objektspeicher in Tests: Python-Double (`moto`) oder nur in CI | entschieden, siehe Zeile „E7" 2026-09-19 | adr/0002 §2 |
| — | Schwachstelle in `Settings.host_allowed`: der `endswith`-Zweig lässt Hosts durch, die nur auf den erlaubten String enden (`evilmaap.eo.esa.int`). Als `xfail`-Test festgehalten; Behebung beim Umzug der Allowlist nach `gateway` | gegenstandslos: `backend/app/` entfernt (`adr/0008`); `gateway` normalisiert Hosts seit M1-03 | backend/tests/test_config.py; KLAERUNGEN B8 |
| 2026-09-18 | M0 Schritt 6: Entscheidungsvorlage für den ersten token-freien Datensatz als ADR-Entwurf `docs/adr/0003-erster-datensatz.md` (Optionen EOPF Sentinel Zarr Samples, Sentinel-2 L2A COG über Earth Search, Planetary Computer, Copernicus DEM). Empfehlung: Sentinel-2 L2A COG zuerst als Träger des Umbaus, EOPF Zarr unmittelbar als zweiter Datensatz. Erreichbarkeit, Zugang, Laufzeit und Lizenzen sind in §10 und §11 am Primärdokument nachgezogen | fest, ADR angenommen | adr/0003; ENTSCHEIDUNGEN §6; projektuebersicht §5; KLAERUNGEN B11, B12, B13 |
| 2026-09-18 | **Erster token-freier Datensatz entschieden:** Sentinel-2 L2A COG über Earth Search v1 (`sentinel-2-c1-l2a`), EOPF Sentinel Zarr Samples als zweiter, Copernicus DEM als dritter | fest | ENTSCHEIDUNGEN §6; adr/0003 |
| — | Token-freie Quelle für komplexe Quad-Pol-Daten (für den Dekompositions-Operator) | offen; in adr/0003 §7 recherchiert, keine registrierungsfreie Quelle gefunden, Spur BELSAR unbelegt; Operator ruht weiter | ENTSCHEIDUNGEN §3; adr/0003 §7 |
| — | ESA-only oder Quellenbreite; Registrierungspflicht; NC hinter Bezahlschranke; Job-Queue; Cloud-Anbieter | offen | projektplan 10 |
| — | **AGPL §13:** Die Plattform muss ihren Nutzern einen Link zum Quellcode anbieten. Umzusetzen mit dem ersten öffentlichen Deployment | offene Pflicht | LICENSE; architekturplan (Deployment), projektuebersicht |
| — | Nutzungsbedingungen Esri World Imagery und Nominatim vor dem ersten öffentlichen Deployment klären | offen | prototyp-inventar F2; architekturplan (Deployment) |
| 2026-09-18 | Reihenfolge der ersten beiden Datensätze **entschieden**: Sentinel-2 L2A COG zuerst, EOPF Sentinel Zarr Samples als zweiter. Der Lizenzvorbehalt ist mit §11 eingelöst — die Prüfung stuft beide gleich ein | fest | adr/0003 §6, §9; ENTSCHEIDUNGEN §6 |
| 2026-09-18 | Egress freigegeben; die drei Schritte aus adr/0003 §9 nachgezogen (§10): Erreichbarkeit aller freigegebenen Endpunkte gemessen, partieller COG-Read gegen den Earth-Search-Asset-Bucket bestätigt; Beleg-Markierungen aufgelöst, soweit die Primärquellen erreichbar waren | teilweise erledigt | adr/0003 §10 |
| 2026-09-18 | Korrekturen am Primärdokument zu adr/0003: EOPF hat 15 statt 12 Collections; das EOPF-Archiv ist rund **drei Monate** rollierend, nicht ein Jahr; die EOPF-Collections führen **kein** Thumbnail-/Quicklook-Asset (F6 dort Neubau); die EOPF-S3-Buckets in us-west-2 sind laut Betreiber-FAQ „unofficial" mit unbekannter Zukunft; `sentinel-2-c1-l2a` führt ein Thumbnail-Asset und hat eine reale Archivlücke 2022, nicht aber die im README genannte Lücke 2016–2019 | fest (Sachstand) | adr/0003 §10.3; §4, §5 |
| — | **Lizenzprüfung nach B11 für adr/0003 weiterhin offen — blockiert.** Alle Lizenzpfade enden auf Hosts, die die Egress-Policy sperrt: `sentinel.esa.int` (Legal Notice, Optionen A und B), ersatzweise `sentinels.copernicus.eu`; `spacedata.copernicus.eu` (Option D); `planetarycomputer.microsoft.com` (Option C, nur falls weiterverfolgt). Das STAC-Feld `license: proprietary` ersetzt die Prüfung nicht. Bis dahin bleibt adr/0003 im Status „Vorschlag" und die Reihenfolge vorläufig | **erledigt** am 2026-09-18 mit §11: siehe die Zeilen „Lizenzprüfung Option D (Copernicus DEM GLO-30 Public)" und „Lizenzprüfung Optionen A und B erledigt" weiter unten. Option C (Planetary Computer) entfällt, weil sie nicht weiterverfolgt wird | adr/0003 §10.2, §11; KLAERUNGEN B11 |
| — | Ratengrenzen der Anbieter und Kosten/Latenz der AWS-Region us-west-2 für adr/0003 | teilweise erledigt: für Earth Search v1 gemessen, siehe die Zeile vom 2026-09-19 zu adr/0005 §3.6; für EOPF und Copernicus DEM weiterhin offen, Kosten/Latenz us-west-2 unverändert offen | adr/0003 §10.4; adr/0005 §3.6 |
| — | Eignung von EOPF als Zarr-Datensatz (Laufzeit ~3 Monate, Buckets inoffiziell) im Zarr-ADR vor M2 prüfen | offen; seit 2026-09-20 Teil des Zarr-ADR in M2b, Kandidatenfeld offen (EOPF ist Kandidat, nicht gesetzt), siehe Zeile „M2-Schnitt" | adr/0003 §10.3; ENTSCHEIDUNGEN §2 |
| 2026-09-18 | Egress-Freigabe präzisiert: `sentinel.esa.int`, `sentinels.copernicus.eu`, `spacedata.copernicus.eu` freigegeben; `zarr.eodc.eu` gestrichen (existiert nicht, adr/0003 §10.1); Planetary Computer bleibt bewusst gesperrt, Option C wird nicht weiterverfolgt | fest | adr/0003 §11; cloud-umgebung |
| 2026-09-18 | **Lizenzprüfung Option D (Copernicus DEM GLO-30 Public) am Primärdokument erledigt:** COP-DEM-GLO-30-F erlaubt Bearbeitung, Weitergabe und kommerzielle Nutzung; Attribution ist Pflicht mit vorgeschriebenem Wortlaut, dazu Haftungssatz und Pflichtenkette an nachgelagerte Nutzer. Der Lizenzpfad läuft heute über `dataspace.copernicus.eu`, nicht mehr über `spacedata.copernicus.eu` | fest: B11-Stufe **Processing** | adr/0003 §11.1; KLAERUNGEN B11 |
| 2026-09-18 | **Lizenzprüfung Optionen A und B erledigt — von Otto an der Primärquelle geprüft** (Sentinel Data Legal Notice, ECMWF-Spiegel, CDSE-Verweis). Bearbeitung und Weitergabe ausdrücklich erlaubt, keine Einschränkung kommerzieller Nutzung (Ausnahmen nur Sicherheit, Rechte Dritter, Dienststörung), Attribution Pflicht („Contains modified Copernicus Sentinel data [Jahr]"), dazu Haftungsverzicht. Bestätigt: Die Nur-nichtkommerziell-Klausel der CDSE-Seite betrifft die Portalinhalte, **nicht** die Sentinel-Daten | fest: B11-Stufe **Processing** | adr/0003 §11.2; KLAERUNGEN B11 |
| 2026-09-18 | Vorbehalt aus adr/0003 §6 eingelöst: Die Lizenzprüfung ergibt für A und B dieselbe B11-Stufe, die Reihenfolge ist deshalb **nicht** neu aufzurufen. Sentinel-2 L2A COG zuerst, EOPF Zarr als zweiter — nicht mehr unter Lizenzvorbehalt. ADR bleibt „Vorschlag", weil die Entscheidung bei Otto liegt | fest (Reihenfolge); Status-Satz überholt — adr/0003 seit der Zeile „M0 Schritt 6" oben **angenommen** | adr/0003 §6, §11.4 |
| 2026-09-18 | **Allowlist-Regel der Cloud-Umgebung:** Egress-Freigaben gelten erst für **neu gestartete** Sitzungen. Eine mitten in der Sitzung eingetragene Freigabe wirkt dort nicht; die Prüfung ist erst in der nächsten Sitzung nachholbar. (Berichtigt eine frühere, falsche Gegenbehauptung im ADR) | fest | adr/0003 §10.2, §11.3; cloud-umgebung |
| 2026-09-19 | **Setup der Cloud-Sitzung als SessionStart-Hook statt Umgebungsfeld:** `scripts/setup-cloud-session.sh` scheiterte dort an `BASH_SOURCE`-Pfaden, `sudo` als root und verlorenem Postgres-Zustand nach dem Cache-Snapshot. Umbau zu einem in `.claude/settings.json` registrierten SessionStart-Hook (`startup\|resume`); PostGIS-Paketinstallation bleibt im Umgebungsfeld (apt), das Skript startet nur noch Postgres und legt Rolle/Datenbank/Extension idempotent an, ohne `sudo` als root, immer mit Exit 0 | fest | cloud-umgebung; .claude/settings.json |
| 2026-09-19 | M1-09: Technische Umsetzung der Coverage Map als ADR-Entwurf `docs/adr/0004-coverage-map.md`. Gemessen: Earth Search v1 bietet die STAC-Aggregation-Extension v0.3.0 an und liefert das gefilterte Dichtegitter in 0,3–0,6 s; `stac-fastapi-pgstac` und `pgstac` haben sie nicht, für eigene Items ist SQL in PostGIS zu bauen (37–218 ms gefiltert bei 2 Mio. synthetischen Footprints). Empfehlung: ein Coverage-Anbieter je Datensatz mit drei Umsetzungen (Upstream-Aggregation, eigenes SQL, ausgewiesene Stichprobe), Gitter Geotile, Zählweise Zentroid, materialisierte Sicht nur für den ungefilterten Weltüberblick, keine Vektorkacheln in M2, Pflichtfeld Vollständigkeit mit der Probe `sum(Zellen) == total_count` | fest: adr/0004 von Otto am 2026-09-19 **angenommen**, die fünf Fragen aus §7 beantwortet (siehe die Zeile „Antworten zu adr/0004 §7") | adr/0004; ENTSCHEIDUNGEN §2; architekturplan 5.2, 6.1; projektuebersicht §5; Planung M2 |
| 2026-09-19 | **E1** Wurzelpaket der Zielarchitektur heißt `earthx`, neu unter `backend/`, neben dem Prototyp `app` (Strangler-Muster) | fest | docs/plans/m1-fundament.md §1; adr/0002 §4; architekturplan 3.1, 13 |
| 2026-09-19 | **E2** Die Konsequenzen aus `adr/0001` werden nur im neuen Pfad (`earthx`) umgesetzt; der Prototyp `app` wird nicht umgebaut, sondern später als Ganzes entfernt | fest | docs/plans/m1-fundament.md §1; adr/0001 |
| 2026-09-19 | **E3** Keine Kacheln in M1; Kachel-Pfad, TiTiler-Spike und Z4 (Streckbereich in der URL) kommen in M2 | fest | docs/plans/m1-fundament.md §1; projektplan M2 |
| 2026-09-19 | **E4** Anwendungs-Cache (`adr/0001` §9.1) ist Postgres; Redis erst bei messbarem Bedarf | fest | docs/plans/m1-fundament.md §1; adr/0001 §9.1; architekturplan 15.3 |
| 2026-09-19 | **E5** Für den Zwischenzustand in M1 (`adr/0001` §9.3) gilt Regel (a): ein Fehlschlag eines Zwischenspeichers macht nur langsamer, nie 404 | fest | docs/plans/m1-fundament.md §1; adr/0001 §9.3 |
| 2026-09-19 | **E6** Die Ablauffrist im Objektspeicher (`adr/0001` §9.2) wird auf M4 vertagt; in M1 schreibt nichts in den Objektspeicher | fest | docs/plans/m1-fundament.md §1; adr/0001 §9.2 |
| 2026-09-19 | **E7** Objektspeicher in Tests: `moto` für Unit-Tests, MinIO nur als Service-Container in CI | fest | docs/plans/m1-fundament.md §1; adr/0002 §2 |
| 2026-09-19 | **E8** `adr/0001` und `adr/0002` von Otto auf „angenommen" gesetzt, mit den Festlegungen E2 bis E7 | fest | docs/plans/m1-fundament.md §1; adr/0001; adr/0002 |
| 2026-09-19 | M1-05: Föderierte Item-Suche als ADR-Entwurf `docs/adr/0005-foederierte-item-suche.md`. Gemessen gegen Earth Search v1: gefilterte Suche 0,43–0,71 s bei 10 Items (17,7 kB je Item), `limit` upstream **nicht** gedeckelt, `fields` spart Faktor 89; Paging ist eine zustandslose Keyset-Marke ohne Tiefblätter-Aufschlag; kein `Cache-Control`, aber `304` auf `GET` (nicht auf `POST`); geschlossene Zeitfenster seit 15 Monaten unverändert, offener Rand ~820 neue Items/h; unbekannte Collection liefert `200` mit `matched: 0` statt `404`. `stac-fastapi-pgstac` hat **keinen** Delegationspunkt (alles läuft in `_search_base` → `pgstac.search()`), der Client ist aber als Attribut austauschbar. `pystac_client` scheidet aus: synchron, eigene Session, folgt Redirects ungeprüft (B8-Verstoß), verliert Fehlerformen. Empfehlung: Unterklasse von `CoreCrudClient`, die je Collection an `earthx:source` verzweigt; zwei Cache-Fristen (24 h geschlossen / 5 min offener Rand); eigene undurchsichtige Seitenmarke; eigener `httpx`-Client in `gateway`; `limit ≤ 100` | fest: adr/0005 von Otto am 2026-09-19 **angenommen**, F1–F3 beantwortet (siehe die Zeile „Antworten zu adr/0005 §7") | adr/0005; architekturplan 5.2, 5.3, 6.1, 6.5; Planung M1-03, M1-06, M1-07 |
| 2026-09-19 | Ratengrenzen **für Earth Search v1** gemessen: 30 parallele Metadaten-Anfragen ohne Drosselung, in rund 110 Anfragen kein `429`, kein `Retry-After`, kein `X-RateLimit-*`. Vorschlag trotzdem konservativ: 6 parallele Verbindungen je Host. Die Log-Zeile „Ratengrenzen der Anbieter" bleibt für EOPF und Copernicus DEM offen | fest (Sachstand); für die übrigen Anbieter weiterhin offen | adr/0005 §3.6; adr/0003 §10.4 |
| 2026-09-19 | **Antworten zu adr/0005 §7:** **F1** Cache-Fristen wie empfohlen (24 h geschlossenes Fenster, 5 min offener Rand, 24 h Einzel-Item), präzisiert: ein Zeitfenster gilt erst als geschlossen, wenn sein Ende **mehr als 7 Tage** zurückliegt (Nachlieferungen). **F2** `filter`/CQL2 wird in M1 **gar nicht** ausgewiesen, weil es noch keine eigenen Items gibt; mit **M3** neu zu prüfen. **F3** `limit ≤ 100`, Vorgabe 10. Damit ist adr/0005 angenommen | fest | adr/0005 §5 (Regeln II, V, VI), §7; Planung M1-06, M1-07, M3 |
| 2026-09-19 | M1-02: `backend/earthx/` mit allen elf Modulen aus architekturplan.md 3.1 als leere Pakete angelegt; `.importlinter` von `app` auf `earthx` umgestellt und der CI-Job `import-boundaries` gatet jetzt jeden PR; ergänzende Regel, dass `httpx`, `requests`, `urllib`, `pystac_client` und `aiohttp` nur in `gateway` importiert werden dürfen. Zwei latente Fehler dabei behoben, die erst beim scharfen Testen sichtbar wurden: `lint-imports` fand `app`/`earthx` ohne `PYTHONPATH=backend` gar nicht (CI-Job und `.importlinter`-Kommentar setzen das jetzt); `urllib.request` als verbotenes externes Modul lehnt import-linter ab (nur ganze externe Pakete sind gültig), jetzt `urllib` | fest | docs/plans/m1-fundament.md M1-02; .importlinter; .github/workflows/ci.yml |
| 2026-09-19 | **Antworten zu adr/0004 §7:** **1** Zählweise **Zentroid** wie empfohlen, ergänzt um einen **Deckel der Gitterstufe je Datensatz** aus der typischen Footprint-Größe im Registry-Eintrag (Sentinel-2 L2A höchstens Geotile z8). **2** **Keine Vektorkacheln in M2.** **3** Umschaltpunkt Dichte → Footprints bei **`numberMatched < 500`**, der Zoom bleibt zusätzliche Bremse (**Nachtrag 2026-09-20:** **M2-07c braucht eine Ersatzregel**, weil die EOPF-STAC-API kein `numberMatched` liefert und das Kriterium dort ins Leere greift — `adr/0007` §3.2, §7 Punkt 5). **4** Status: adr/0004 auf **angenommen**. **5** Latenzziel der gefilterten Coverage: **unter 1 s, typisch unter 0,5 s** (ersetzt die Setzung „wenige hundert Millisekunden" in K6) | fest | adr/0004 §5, §7, K6; Planung M1-04, M2 |
| 2026-09-19 | **Aggregation wird eine vierte, optionale Adapter-Fähigkeit** neben Discovery, Suche und Zugriffsauflösung. Die Liste in `architekturplan.md` 6.1 ist damit **nicht mehr abschließend**; Rückfallebene für Quellen ohne Aggregation ist die **ausgewiesene Stichprobe**. Protokollwissen der Quelle liegt in `adapters`, Nahtstelle und SQL in `catalog` | fest; **Nachtrag 2026-09-20:** Die ausgewiesene Stichprobe wird in **M2-09b** gebaut, nicht in M2-05 — `adr/0007` §3.2 hat gemessen, dass die EOPF-STAC-API weder eine Aggregation noch `numberMatched` hat, also braucht sie der zweite Datensatz und sonst niemand | architekturplan 6.1; adr/0004 §5, §6 |
| 2026-09-19 | **Cache-Fristen der Coverage-Aggregation** wie in `adr/0005` F1: **24 h** für ein geschlossenes Zeitfenster (Ende mehr als **7 Tage** zurück), **5 min** am offenen Rand. Damit gilt für Item-Suche und Coverage dieselbe Regel | fest | adr/0004 §5; adr/0005 §5 Regel II, §7 F1; Planung M1-06, M2 |
| 2026-09-19 | `earth-search.aws.element84.com` in `cloud-umgebung.md` §6 als **freigegeben und erreichbar** geführt; die Tabelle hatte noch den Stand der Messung vom 18.09., also vor der Egress-Freigabe. Berichtigt den Nebenbefund in adr/0004 §6, den Satz „Keine EO-Quelle und kein Geocoder erreichbar" in der Zeile vom 2026-09-18 (Geocoder gilt weiter) und die Kontexttabelle in `adr/0002` §1. An der Testaufteilung ändert das nichts: PR-Läufe testen weiter ohne Live-Quelle | fest (Sachstand) | cloud-umgebung §6; adr/0002 §1; adr/0003 §11 |
| 2026-09-19 | **Skills vorerst nicht im Repo (B14 neu gefasst):** `atomic-commits`, `code-cleanup` und `readme-updater` bleiben in Ottos persönlicher Umgebung. `CLAUDE.md` verweist nicht mehr auf sie, sondern nennt die Regel selbst: kleine, thematisch getrennte Commits mit aussagekräftiger Nachricht; vor dem PR die geänderten Dateien aufräumen. Nimmt M0 Schritt 1 aus `ENTSCHEIDUNGEN` §6 insoweit zurück | fest | CLAUDE.md; KLAERUNGEN B14; projektplan 2.1, 8 (M0); ENTSCHEIDUNGEN §6 |
| 2026-09-19 | Korrekturen aus PR #15 nachgeholt, die vor dem Merge nicht auf dem Branch lagen: in `adr/0004` waren `numberMatched` 22 619 und `total_count` 3848 als Werte desselben Filters dargestellt (sie stammen aus zwei Filtern, mit und ohne Wolkenfilter); dazu „zwei Größenordnungen" statt einer, „rund das Vierfache" statt der Faktoren 3,9 und 5,9, falsche Herkunft bei K6 und K8, nur vier Fragen in §7, kein Messanhang §10 und die fehlende Folge „Aggregation als vierte Adapter-Fähigkeit" | fest (Sachstand) | adr/0004 |
| 2026-09-19 | `adr/0004` und `adr/0005` in die Aufgabenschnitte M1-03, M1-04, M1-06 und M1-07 übernommen (URL-Obergrenze ~8 kB und GET mit langer Abfragezeichenfolge, eigener `httpx`-Client ohne `pystac_client`, höchstens 6 parallele Verbindungen je Host, drei Registry-Felder für die Coverage, Collection-Prüfung mit `404`, eigene Seitenmarke, `limit ≤ 100`, `CoreCrudClient`-Unterklasse, kein `filter`/CQL2 in M1) | fest | plans/m1-fundament.md M1-03, M1-04, M1-06, M1-07; adr/0004; adr/0005 |
| 2026-09-19 | M1-08 Plan von Otto wie empfohlen bestätigt: pgstac-Migration (`pypgstac[psycopg]`, gepinnt auf 0.9.x) schon jetzt als einmaliger Compose-Service statt erst mit M1-04; MinIO-Version bei der Umsetzung auf ein bestimmtes Release gepinnt, nie `latest`; alle vier Prozess-Hüllen (`api`, `tiler`, `worker`, `harvester`) als FastAPI-Apps mit `/health`, je im Modul, das den Prozess später fachlich trägt; Host-Portschema 8000–8003 | fest | docs/plans/m1-08-compose-topologie.md; docker-compose.yml; backend/requirements.txt |
| 2026-09-19 | M1-08 Umsetzung, zwei Korrekturen: (1) `minio/minio` existiert auf Docker Hub nicht mehr (Repo entfernt); Image kommt jetzt von `quay.io/minio/minio`, gleicher Release-Tag. (2) `pypgstac[psycopg]` war als Spannbreite (`>=0.9,<0.10`) statt exaktem Pin eingetragen; auf `==0.9.12` korrigiert — das ist die einzige Stelle, an der die pgstac-Version steht, `docker-compose.yml` nennt keine eigene | fest (Sachstand) | docker-compose.yml; backend/requirements.txt; docs/plans/m1-08-compose-topologie.md |
| 2026-09-19 | MinIO Community Edition ist im Wartungsmodus (nur noch Quellcode-Fixes, das Repository ist archiviert). Für M1 lokal/CI mit dem gepinnten Image unproblematisch. **Offen:** Objektspeicher für lokal/CI vor M4 neu bewerten (Alternativen mit Recherche); Produktion nutzt ohnehin einen verwalteten S3-Dienst, nicht MinIO selbst | offen | docker-compose.yml; docs/plans/m1-08-compose-topologie.md; architekturplan 12.1, 12.2 |
| 2026-09-19 | **Antworten zu M1-04 (Plan §2):** **1** Die Datensatz-Registry liegt in **`catalog`** (`registry.py` für den Typ, `datasets.py` für die Einträge); `earthx/datasets/<id>/` bleibt datensatzspezifischem **Code** vorbehalten. Löst den Widerspruch zwischen B13 („in `datasets.py`") und architekturplan 3.1 (`datasets/` bleibt von allem Generischen isoliert, `.importlinter` setzt das durch — dort könnte `catalog` die Einträge nicht lesen). **2** M1-04 wird in **zwei PRs** geteilt: 04a Registry, Validierung, Collection-Abbildung, Sentinel-2-Eintrag, T-A-Tests; 04b Migrationen, Laden nach pgstac, T-C-Tests, Postgres in CI und im Setup-Skript. **3** `.github/workflows/ci.yml` (Postgres-Service-Container) und `scripts/setup-cloud-session.sh` (`pypgstac migrate`) dürfen dafür geändert werden. **4** Lizenzfeld ohne SPDX-Kennung: `spdx_id = None` plus Name und URL, STAC trägt `"license": "proprietary"` wie die Quelle. **5** Eigener Migrationsläufer für nummerierte SQL-Dateien **neben** der gepinnten pgstac-Version | fest | docs/plans/m1-04-registry-katalog-pgstac.md §2; KLAERUNGEN B13; architekturplan 3.1, 5.1; adr/0003 §11.2; adr/0004 §6 |
| 2026-09-19 | **STAC-Version und Lizenzwert sind aneinander gebunden:** Wir geben **STAC 1.0.0** mit `license: "proprietary"` aus, weil pgstac und Earth Search das sprechen. STAC **1.1** hat `proprietary` durch `other` ersetzt (aufgefallen beim Rücklesen mit `pystac` 1.15, das still normalisiert). Der Wert wird deshalb **aus der Version abgeleitet**, nicht im Datensatz eingetragen: SPDX-Kennung unverändert, sonst 1.0 → `proprietary`, 1.1 → `other`. Welche Version wir ausliefern, entscheidet die `stac-fastapi-pgstac`-Version in **M1-07** | fest | m1-04-registry-katalog-pgstac.md §3.3; adr/0003 §11.2; Planung M1-07 |
| 2026-09-19 | **M1-04b baut auf M1-08 auf:** `pypgstac` wird dort (PR #24) in `requirements.txt` gepinnt und als `pgstac-migrate`-Dienst in compose geführt. 04b benutzt denselben Pin; es gibt **keine zweite Pin-Stelle**. 04a ist davon unberührt | fest | m1-04-registry-katalog-pgstac.md §2, §3.6; Planung M1-08 |
| 2026-09-19 | **Die drei Coverage-Felder aus `adr/0004` §6 bleiben im Registry-Eintrag und gehen nicht als neuntes `earthx:`-Feld in die Collection** — §5 sagt, der Registry-Eintrag entscheide den Coverage-Weg; ein zusätzliches Feld wäre eine Erweiterung von architekturplan 5.1. DOI und Zitierung (Checklistenpunkt 3) liegen in der `scientific`-Extension, die 5.1 nennt, nicht unter `earthx:` | fest | m1-04-registry-katalog-pgstac.md §3.3; architekturplan 5.1; adr/0004 §5, §6; projektuebersicht §5 |
| 2026-09-19 | **Die `dataset_id` des ersten Datensatzes ist `sentinel-2-c1-l2a`**, gleich der genutzten Upstream-Collection. Die kürzere Form `sentinel-2-l2a` ist der Name der **älteren** Earth-Search-Collection, die Collection 1 ablöst (adr/0003 §3, Option B) — als eigene ID wäre sie mit jener verwechselbar | fest | m1-04-registry-katalog-pgstac.md §3.2; adr/0003 §3, §6 |
| 2026-09-19 | **Antworten zu M1-03 §8 (Fetch-Gateway):** **F1** Umsetzung in drei PRs (Sperrregeln ohne HTTP, Client, GDAL und Wächtertest), weil Code und Tests zusammen über dem 400-Zeilen-Richtwert liegen. **F2** Der Client verbindet die **geprüfte Adresse** statt des Namens, der Hostname dient nur noch SNI und Zertifikatsprüfung; das schließt die Lücke zwischen Prüfung und Verbindung, bis die Netzwerk-Policy in M6 greift (B8). **F3** Circuit Breaker vertagt auf ein Issue nach M1-04: er braucht Zustand über Prozessgrenzen, den erst der Anwendungs-Cache (E4) hergibt; in M1 tragen der Deckel von 6 parallelen Verbindungen je Host und der Backoff die Rücksicht auf die Quelle. **F4** In M1 nur `https`; `s3` erst, wenn der erste Leser es braucht (M2) | fest | plans/m1-03-fetch-gateway.md; plans/m1-fundament.md M1-03; architekturplan 6.5; KLAERUNGEN B8 |
| 2026-09-19 | M1-03 umgesetzt (drei PRs laut F1): `earthx/gateway/` mit Allowlist und Host-Normalisierung, SSRF-Prüfung über alle aufgelösten Adressen, `check_url` als einzige Freigabestelle, `httpx`-Client mit 8-kB-URL- und 8-MB-Rumpfgrenze, sechs parallelen Verbindungen je Host, Backoff und eigener Redirect-Schleife, dazu die zentrale GDAL/VSI-Konfiguration ohne Token (Z8). Drei Befunde beim Bauen: (1) Der Client verbindet die geprüfte Adresse, prüft das Zertifikat aber weiter gegen den **Namen** (`sni_hostname` → `server_hostname`); ohne diese Setzung prüft `httpcore` gegen die IP — mit Test belegt. (2) Ein Aufrufer konnte über `headers={'Host': …}` einen zweiten Host-Header setzen und damit mitbestimmen, gegen welches Zertifikat geprüft wird; der Header wird jetzt verworfen. (3) `UpstreamUnreachable` ergänzt den Plan: eine Verbindung, die gar nicht zustande kommt, ist kein Zeitablauf. Die Allowlist wird weiter hineingereicht; die Verdrahtung mit `SourceInfo.endpoint` aus der Registry gehört in die zusammensetzende Schicht, weil `gateway` `catalog` nicht importieren darf | fest | plans/m1-03-fetch-gateway.md; architekturplan 6.5; KLAERUNGEN B8; adr/0001 Z8; adr/0004 §3.4; adr/0005 §6 |
| 2026-09-19 | **Lücke in den Importverträgen geschlossen (Otto, Option 1):** Der Vertrag „HTTP-Clients nur in `gateway`" deckte nur die elf Module aus architekturplan 3.1 ab; `earthx/logging.py` als Modul der Paketwurzel lag außerhalb, ein `import httpx` dort wäre an `lint-imports` vorbeigekommen. Der Vertrag nennt jetzt auch die Wurzelmodule, und der Deckungstest liest sie von der Platte statt aus einer Liste — ein neues Wurzelmodul lässt die Suite fallen, bis `.importlinter` es nennt. Beide Richtungen gegengeprüft: ohne den Eintrag fällt der Deckungstest, mit ihm fällt `lint-imports` bei einem `import httpx` in `logging.py`. Der Vertrag „datasets-isolated" hat dieselbe Form, aber kein Sicherheitsgewicht, und bleibt unverändert | fest | .importlinter; backend/tests/test_module_boundaries.py; KLAERUNGEN B8; architekturplan 3.1 |
| 2026-09-19 | **Beim Download gibt die Plattform die Bedingungen der Quelle weiter, keinen eigenen Haftungsausschluss.** `liability_notice` im Registry-Eintrag ist durch `terms_url` (Link auf das Sentinel Data Legal Notice) und `terms_notice` ersetzt, letzteres je Sprache (Deutsch Pflicht, Englisch daneben für eine englische Oberfläche). Der Terms-Text ist **attributionsfrei**; die Attribution bleibt ein eigener Text und steht beim Download davor — sonst erschiene sie doppelt oder behauptete bearbeitete Daten, wo unveränderte weitergegeben werden. Anlass: Das Legal Notice enthält **keine** Freizeichnung des Anbieters, sondern einen **Anspruchsverzicht des Nutzers** gegenüber EU und Datenanbietern sowie „without any express or implied warranty"; ein früherer Entwurf hatte einen Satz aus der Copernicus-DEM-Lizenz (adr/0003 §11.1) übernommen und damit die Richtung umgedreht. Wortlaut am 19.09.2026 an der Primärquelle gelesen — der Host ist freigegeben | fest | m1-04-registry-katalog-pgstac.md §3.2; adr/0003 §11.1, §11.2, §11.3; projektuebersicht §5 |
| 2026-09-19 | Eigene Nutzungsbedingungen und Haftungsausschluss der Plattform vor dem ersten öffentlichen Deployment klären | **offen** | ENTSCHEIDUNGEN §4 (AGPL §13, Pflicht beim ersten Deployment); projektuebersicht §5 |
| 2026-09-19 | **M1-04b: Die Buchführungstabelle `earthx_migrations` gehört dem Migrationsläufer, nicht einer Migration.** Der Plan sah sie als Datei `001` vor; so müsste aber jedes Migrationsverzeichnis sie mitbringen, was die Tests sofort zeigten. `catalog/migrations/` ist damit in M1-04 **leer** (mit README), die erste eigene Migration ist der Anwendungs-Cache aus E4 in M1-06. Der Läufer selbst bleibt wie in Frage 5a entschieden | fest | m1-04-registry-katalog-pgstac.md §2 Frage 5, §3.4; Planung M1-06 |
| 2026-09-19 | **T-C-Tests laufen im bestehenden CI-Job `backend`, nicht in einem eigenen Job**: `adr/0002` §6 verlangt, dass Sitzung und CI **denselben** Befehl ausführen (`pytest`). Der Postgres-Service-Container hängt deshalb an diesem Job; das Setup-Skript migriert pgstac in der Sitzung, CI tut es als eigener Schritt. Die pgstac-Version kommt beidseits aus dem Pin in `requirements.txt`, nirgends sonst | fest | adr/0002 §2, §6; .github/workflows/ci.yml; scripts/setup-cloud-session.sh |
| 2026-09-19 | **Zwei Fundstellen aus dem Review von M1-04b, beide denselben Ursprungs:** pgstac setzt auf seinen Rollen `search_path = pgstac, public` und spricht in eigenen Triggern Tabellen unqualifiziert an. Folge (a): unsere Buchführungstabelle heißt ausdrücklich `public.earthx_migrations`, sonst landet sie im pgstac-Schema und ist bei dessen Neuaufbau still weg; Folge (b): vor pgstac-Aufrufen setzen wir den Suchpfad selbst, sonst scheitert etwa das Löschen einer Collection an `partition_stats`. Beides mit Tests festgehalten | fest | m1-04-registry-katalog-pgstac.md §3.4; KLAERUNGEN B9 |
| 2026-09-19 | **`psycopg` ist für `jobs` und `processing` gesperrt** (eigener Import-Vertrag). KLAERUNGEN B9 verlangt den Worker-Kern ohne Datenbank; seit M1-04b ist `psycopg` eine echte Abhängigkeit, also wird die Regel geprüft statt nur behauptet. Lesen der Datenquellen über `gateway` bleibt erlaubt | fest | .importlinter; KLAERUNGEN B9; architekturplan 3.1 |
| 2026-09-19 | **Der Terms-Hinweis gibt die Haftungsklausel des Legal Notice vollständig weiter**, in allen drei Teilen: kein Gewährleistungsversprechen (auch nicht zu Qualität und Eignung), Verzicht des Nutzers auf Schadensersatzansprüche gegenüber EU und Datenanbietern, und der Umfang dieses Verzichts (vertragliche und deliktische Ansprüche, vor Gericht, im Schiedsverfahren oder in anderer Streitbeilegung). Zwei davon fehlten zunächst; sie wegzulassen hätte den Verzicht für den Leser enger aussehen lassen, als er ist. Wortlaut je deutsch und englisch, am 19.09.2026 am Dokument selbst geprüft | fest | m1-04-registry-katalog-pgstac.md §3.2; adr/0003 §11.2 |
| 2026-09-19 | **Antworten zu M1-06 (Plan §9):** **F1–F5 alle wie empfohlen** — Schnitt in drei Schritten (Adapter, Cache, Live-Smoke), Cache-Protokoll in `adapters` mit der Postgres-Umsetzung in `catalog`, `limit > 100` wird **abgelehnt** statt gedeckelt, eine unbekannte Collection bekommt einen eigenen Fehlertyp in `adapters`, Live-Smoke **täglich** mit zwei Metadaten-Anfragen. **F6 bleibt offen** (Lizenz der Earth-Search-Metadaten): Fixtures bleiben synthetisch. Abweichung bei der Umsetzung: Die drei Schritte liegen als drei Commits in **einem** PR, weil die Sitzung an einen Branch gebunden ist | fest; F6 **erledigt** am 2026-09-19, siehe die Zeile „Fixtures bleiben dauerhaft synthetisch“ | plans/m1-06-earth-search-adapter.md §9; adr/0005 §8 |
| 2026-09-19 | **Der Importvertrag `http-only-in-gateway` zählt nur noch direkte Importe** (`allow_indirect_imports = True`). Er zählte auch Ketten, und `gateway.policy` zerlegt URLs mit `urllib.parse` — damit war `adapters → gateway → urllib` ein Verstoß und jeder Import von `gateway` unmöglich, obwohl architekturplan 3.1 ihn `catalog`, `adapters`, `readers` und `discovery` ausdrücklich erlaubt. B8 verlangt, dass niemand außerhalb von `gateway` **selbst** eine Verbindung öffnet; der Weg **durch** das Gateway ist die Architektur. Die direkte Regel bleibt doppelt geprüft (Vertrag und Syntaxbaum-Test), beide gegen ein absichtliches `import httpx` gegengeprüft. Lockerung einer Importgrenze, deshalb von Otto entschieden | fest | .importlinter; KLAERUNGEN B8; architekturplan 3.1 |
| — | Dieselbe Kettenzählung trifft später die Verträge `access` und `processing`: `access → readers → gateway` ist nach architekturplan 3.1 gewollt, gilt dort aber weiterhin als Verstoß. Otto am 19.09.2026: bleibt **bis vor M2** offen und wird dann entschieden, sobald `readers` und `access` Inhalt bekommen | **entschieden** am 2026-09-20, siehe Zeile „Importverträge zählen direkte Importe" | .importlinter; architekturplan 3.1 |
| 2026-09-19 | M1-06 umgesetzt: `earthx/adapters/earth_search.py` (Suche, Einzel-Item, eigene Seitenmarke, Eingabeprüfung), `earthx/catalog/search_cache.py` mit Migration `002_search_cache.sql`, T-D-Workflow `live-smoke.yml`. Vier Befunde beim Bauen: (1) **Eine `bbox` mit `west > east` wird zugelassen** statt abgewiesen — so schreiben GeoJSON und STAC eine Box über die Datumsgrenze, und Earth Search liest sie so; abgewiesen wird nur, was upstream selbst abweist (`south >= north`) und was es still duldet (Breiten außerhalb ±90). Der Plan §6 hatte beides zusammengefasst. (2) Statt die Item-ID für den Pfad zu maskieren (`urllib` ist außerhalb von `gateway` gesperrt) wird sie **gegen ein Muster geprüft**; das sperrt `../` gleich mit. (3) Drei Migrationstests brauchten einen sauberen Stand, weil `earthx.catalog.load` **committet** — nach einem vollen Lauf trägt die Datenbank Version 002 wirklich, und ein Test mit einer eigenen 002 las das als geänderte Migration. (4) Zwei kleine Abweichungen von der Plan-Skizze: `SearchParams` trägt `start`/`end` statt eines Tupels, und `SearchCache.set` nimmt die `dataset_id` mit, damit die Spalte der Tabelle gefüllt werden kann | fest (Sachstand) | plans/m1-06-earth-search-adapter.md; adr/0005 Regeln I–V; adr/0001 §8, §9.3 |
| 2026-09-19 | **Befund aus dem Review von M1-06, Gateway:** Ein Redirect mit 301/302/303 machte aus einem `POST` ein `GET` und verwarf den Rumpf. Bei `POST /search` heißt das: Die Quelle antwortet mit einer **ungefilterten Vorgabeseite**, die der Aufrufer nicht von seinen eigenen Treffern unterscheiden kann — still falsch statt Fehler. Ein Redirect, der den Rumpf verlöre, wird jetzt abgewiesen; 307 und 308 erhalten Methode und Rumpf und werden weiter verfolgt. **Ändert Verhalten aus M1-03**, das dort bewusst so geschrieben und getestet war; der Test nennt jetzt den Grund | fest (Sachstand) | earthx/gateway/client.py; adr/0005 §3.3 |
| 2026-09-19 | **Befund aus dem Review von M1-06, Cache:** Die Cache-Anweisungen liefen ohne Savepoint auf der Verbindung des Aufrufers. Eine fehlgeschlagene Anweisung bricht die **ganze** Transaktion ab — ab M1-07 ist das die Transaktion des Requests: Die Suche hätte geantwortet, alles danach wäre an `InFailedSqlTransaction` gestorben, und der Cache hätte auf dieser Verbindung nie wieder schreiben können. E5 war damit nur im Adapter erfüllt, nicht im Prozess. Jede Anweisung läuft jetzt in einem eigenen Savepoint, wie beim Migrationsläufer; der T-C-Test benutzt die Verbindung danach weiter und fällt ohne den Savepoint | fest (Sachstand) | earthx/catalog/search_cache.py; plans/m1-fundament.md §1 (E5); adr/0001 §9.3 |
| — | **`sortby` wird bewusst nicht mitgeschickt.** Das Keyset-Paging verlässt sich damit auf die undokumentierte Vorgabesortierung von Earth Search (`adr/0005` §8 Punkt 5). Ein ausdrückliches `sortby` würde die Seitenmarke vermutlich brechen: Die gemessene Fehlermeldung zur kaputten Marke (`search_after has 1 value(s) but sort has 3`) zeigt, dass die Zahl der Sortierfelder zur Marke passen muss. Das lässt sich nur an der Quelle messen, und Sitzungen greifen nicht live zu (`adr/0002` T-D). Vor einer Sortierzusage nach außen (M1-07) zu klären | fest; erledigt am 2026-09-19, siehe „M1-07: eigener fester `sortby`“ unten | adr/0005 §8 Punkt 5; earthx/adapters/earth_search.py; Planung M1-07 |
| — | `docker-compose.yml` führt `pypgstac migrate` aus, aber nie `python -m earthx.catalog.load`. Ein compose-Start hat deshalb weder die Collection noch die Cache-Tabelle aus M1-06. Fällt mit M1-07 an, wenn die STAC-API beides braucht | fest; erledigt am 2026-09-19, siehe „M1-07 umgesetzt“ unten | docker-compose.yml; earthx/catalog/load.py; Planung M1-07 |
| 2026-09-19 | **Fixtures bleiben dauerhaft synthetisch — F6 aus dem M1-06-Plan ist damit erledigt.** Otto entscheidet: Es werden **keine** Earth-Search-Antworten aufgezeichnet, auch nicht, wenn die Lizenz der Metadaten später geklärt würde. Die Frage nach der Lizenz stellt sich für Fixtures deshalb nicht mehr; `adr/0005` §8 Punkt 1 ist insoweit geschlossen. Dass die Fixtures gegen die echte Quelle veralten können, fängt der zeitgesteuerte T-D-Test ab | fest | plans/m1-06-earth-search-adapter.md §9 F6; adr/0005 §8; ENTSCHEIDUNGEN §4; tests/fixtures/earth_search/README.md |
| 2026-09-19 | **Antworten zu M1-07 (Plan §15): F1–F6 alle wie empfohlen.** Zwei Datenbanktreiber nebeneinander (`asyncpg`/`buildpg` für `stac-fastapi-pgstac`, `psycopg` für Registry-Check und Such-Cache), `sort` zusätzlich zu `filter` in M1 nicht ausgewiesen, ein fester eigener `sortby` in `earth_search.py` statt der undokumentierten Vorgabe, Präfix `/stac`, der compose-Nachtrag `catalog-load`, `stac-fastapi.pgstac` auf `6.4.1` gepinnt. Zusatzauflage zu F6/§10 (STAC-Version/Lizenz-Kopplung): ein Test statt eines zweiten Pins auf `stac-pydantic` — siehe die eigene Zeile dazu | fest | plans/m1-07-stac-api.md §15 |
| 2026-09-19 | **STAC-Version/Lizenz-Kopplung: ein Test, keine zweite Pin-Stelle.** `stac_version` der ausgelieferten API kommt aus `stac_pydantic.version.STAC_VERSION`, einer transitiven Abhängigkeit mit Versionsspanne (`stac-pydantic<4.0,>=3.3.0`), nicht aus dem `stac-fastapi.pgstac`-Pin. Geprüft: `STAC_VERSION` steht auf `"1.0.0"` in **jeder** Version dieser Spanne (3.3.0 bis zur aktuellen 3.6.0) — ein zweiter Pin würde also kein Risiko abstellen, das nicht schon durch die Spanne selbst abgestellt ist, brächte aber eine weitere Versionsstelle zum Pflegen. Statt dessen misst ein Test (`tests/integration/test_api_federating.py::TestConformance::test_stac_version_matches_what_catalog_derives_the_licence_from`) bei jedem Lauf, ob die ausgegebene `stac_version` weiter zu `catalog.collection.STAC_VERSION` passt, aus der sich der Lizenzwert ableitet (Zeile „STAC-Version und Lizenzwert sind aneinander gebunden“ oben) — ein künftiger Versionssprung von `stac-pydantic` würde diesen Test brechen, statt still ein falsches `license`/`stac_version`-Paar auszuliefern | fest | plans/m1-07-stac-api.md §10, §15; tests/integration/test_api_federating.py; m1-04-registry-katalog-pgstac.md §3.3 |
| 2026-09-19 | **M1-07 umgesetzt:** `earthx/api/main.py` (Prozess, `/stac`-Präfix, `/health` unverändert außerhalb des Präfixes für die compose-Healthcheck), `earthx/api/federating_client.py` (`FederatingCoreCrudClient`, adr/0005 Option 2 — überschreibt nur die vier öffentlichen Methoden), `earthx/api/dependencies.py` (Gateway und Such-Cache-Pool aus der Registry), `earthx/gateway/policy.py` (`host_of`, löst das seit M1-03 offene Versprechen „ab M1-04 entscheidet die Registry die Allowlist“ ein — niemand hatte es bis hierhin gebraucht), `earthx/adapters/earth_search.py` (fester `sortby`), `docker-compose.yml`/`.github/workflows/ci.yml` (`catalog-load`-Dienst). Vier Befunde beim Bauen: (1) **`Extensions()` ohne `settings=` baut sich eine eigene, zweite `Settings()`-Instanz**, die die hier hart gesetzte `enabled_extensions`-Liste nie sieht — ohne `Extensions(settings=settings)` an `instantiate_api` wären `filter`/`sort` trotz der eigenen Vorgabe auf der Landing Page erschienen; ein Konformitätstest hat das gefangen. (2) **Der Such-Cache übersteht einen Testlauf**, weil sein Pool mit `autocommit=True` läuft (nötig, damit ein Cache-Fehlschlag nie die Anfrage-Transaktion mitreißt, M1-06-Befund) — ein Testlauf ohne eigene Aufräumzeile las einen Treffer aus dem vorigen Lauf statt den gemockten Transport zu befragen. (3) Eine Suche über mehr als eine Quelle gleichzeitig war zunächst mit einer Best-Effort-Zusammenführung gebaut, aber mit der heutigen Registry (ein Datensatz) nie erreichbar und deshalb nie scharf testbar — vor dem Merge durch eine klare `400` ersetzt (siehe eigene Log-Zeile unten). (4) **`item_collection`s `**kwargs` enthalten ein abgeschaltetes Extension-Feld gar nicht erst** — eine Prüfung dort auf `filter`/`sortby` hätte nie etwas gefunden; ein `?filter=true` an `/collections/{id}/items` kam bis zu einem gezielten Test klaglos beim Adapter an. Die Prüfung liest jetzt `request.query_params` selbst, wie bei `get_search` schon richtig gemacht | fest (Sachstand) | plans/m1-07-stac-api.md; adr/0005 §4 Option 2, Regeln I–VI; earthx/api/ |
| 2026-09-19 | Gemischte Suche über mehrere Quellen: vorerst abgelehnt; umsetzen und testen, sobald ein zweiter Datensatz dazukommt (M2) | **aufgehoben** am 2026-09-20, siehe Zeile „Gemischte Suche bleibt abgelehnt" | plans/m1-07-stac-api.md §16 Punkt 3; adr/0005 Regel I; earthx/api/federating_client.py |
| 2026-09-19 | **Befund aus dem CI-Job `compose-topology` (PR #29, M1-07):** `api` startete nicht — `PostgresSettings` (der `asyncpg`-Pool von `stac-fastapi-pgstac`) verlangt `pguser`/`pgpassword`/`pghost`/`pgport`/`pgdatabase`, aber der `api`-Dienst in `docker-compose.yml` hatte nie einen `environment`-Block: Der Prototyp-Stub, den er bisher startete, sprach nie mit Postgres. `catalog-load` selbst lief im selben Lauf fehlerfrei durch (Log zeigt „migrations applied: 002“, „collections written: sentinel-2-c1-l2a“) — die Lücke betraf nur `api`. Behoben mit denselben `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE`-Werten, die `pgstac-migrate` und `catalog-load` schon setzen; das Dockerfile selbst brauchte keine Änderung — es installiert generisch aus `requirements.txt`, und der Fehler war ein `pydantic`-Validierungsfehler, kein Importfehler, was schon zeigte, dass `stac-fastapi.pgstac`/`asyncpg` im Image ankommen | fest (Sachstand) | docker-compose.yml; plans/m1-07-stac-api.md; .github/workflows/ci.yml (Job `compose-topology`) |
| 2026-09-20 | `UEBERGABE_CHAT.md` ist gelöscht, weil überholt. Der Chat startet mit `CLAUDE.md`, `ENTSCHEIDUNGEN_2026-09-18.md`, diesem Log, `KLAERUNGEN.md` und den Plandokumenten. B3 nennt die Datei insoweit nicht mehr | fest | KLAERUNGEN B3 |
| 2026-09-20 | **M2-Schnitt:** M2 zerfällt in **M2a** (Sentinel-2 durchgehend im Viewer: Kacheln, Coverage-Heatmap, Download des AOI-Zuschnitts, Frontend) und **M2b** (Zarr als zweites Format). Das Zarr-ADR startet sofort parallel als Stufe C, mit **offenem Kandidatenfeld**: EOPF ist Kandidat, nicht gesetzt (Archiv ~3 Monate, Buckets inoffiziell, kein Thumbnail, STAC 1.1). Die Abnahme von M2 bleibt „zwei Datensätze in zwei Formaten im selben Viewer" | fest | projektplan M2; plans/m2-*.md; adr/0003 §10.3 |
| 2026-09-20 | **Importverträge zählen direkte Importe.** Die neun Modulverträge aus architekturplan 3.1 (`gateway` bis `identity`) bekommen `allow_indirect_imports = True`, weil die Spalte „darf importieren" direkte Importe meint. Mit Kettenzählung scheiterten sonst `access` (über `readers` und `catalog` an `gateway`), `processing` (über `catalog` an `gateway`) und `jobs` (über `processing` an fast allem). **Weiter Ketten zählen** `datasets-isolated` und `no-database-in-worker-core`: Für B9 ist gerade die Kette entscheidend. Folge ab M4: `processing` darf nur DB-freie Teile von `catalog` erreichen, `catalog` wird dafür geteilt, sobald `processing` Inhalt bekommt. In M2 ist `processing` leer, der Vertrag schlägt deshalb noch nicht an. Lockerung einer Importgrenze, deshalb von Otto entschieden | fest | .importlinter; architekturplan 3.1; KLAERUNGEN B8, B9 |
| — | `jobs` steht unter dem psycopg-Verbot (`no-database-in-worker-core`), soll laut projektplan M4 aber eine Postgres-gestützte Queue tragen. Worker-Kern (zustandslos, B9) und Queue-Verwaltung müssen dafür getrennt werden. Im Queue-ADR in M4 zu klären | offen | .importlinter; KLAERUNGEN B9; architekturplan 3.1, 7.5 |
| 2026-09-20 | **Download des AOI-Zuschnitts in M2:** synchron gestreamt, nichts wird gespeichert, mit Größendeckel (Wert schlägt der M2-Plan vor). Geliefert wird ein ZIP aus COG und Textdatei mit Attribution, `terms_notice`, `terms_url` und Zitierangabe. Objektspeicher mit Ablauf, signierte URLs und Rezept als Begleitdatei bleiben M4; E6 gilt unverändert | fest | architekturplan 6.4; adr/0001 Z2, §9.2; ENTSCHEIDUNGEN §2; plans/m1-fundament.md §1 (E6) |
| 2026-09-20 | **Onboarding-Checkliste v1 (M2):** Punkt 9 gilt in M2 als End-to-End-Test „Suche → Anzeige → Zuschnitt-Download" gegen Fixtures; der echte Processing-Schritt wird mit M4 nachgezogen. Punkt 10 („zuletzt erfolgreich geprüft") ist der Zeitpunkt des letzten grünen T-D-Smoke-Laufs, bis Health-Checks kommen (M5) | fest | projektuebersicht §5; KLAERUNGEN B12; adr/0002 |
| 2026-09-20 | **Rahmen des TiTiler-Spikes (Stufe C):** (1) TiTiler-Fabriken nur mit `dataset`/`item` als Eingabe, **kein freier `url`-Parameter** nach außen (B8, SSRF). (2) Streckbereich in der Kachel-URL (adr/0001 Z4), dazu ein Statistik-Endpunkt mit Postgres-Cache (E4, E5). (3) Mosaik zustandslos per Item-Liste in der URL oder per Such-ID mit Cache: gegeneinander abwägen. (4) Nur `https`, kein `s3` für COG. (5) Prüfpunkt Asset-Host: `cloud-umgebung` §6 nennt `sentinel-cogs…`, adr/0003 §10.1 misst `e84-earth-search-sentinel-data…`; welcher Host für `sentinel-2-c1-l2a` gilt und in der Allowlist steht, am Code und an echten Items klären. Stitching kommt nur in M2, wenn der Spike es zustandslos hergibt | fest | architekturplan 6.3, 6.5, 15.2; adr/0001 Z4; cloud-umgebung §6; adr/0003 §10.1 |
| 2026-09-20 | **Ratenbegrenzung der Coverage (adr/0004 §8.6) bewusst vertagt:** Bis dahin tragen Postgres-Cache und der Deckel paralleler Verbindungen je Host im Gateway die Rücksicht auf die Quelle. Eine Begrenzung pro IP kommt mit dem ersten öffentlichen oder Vorschau-Deployment, pro Nutzer mit M6 | fest (vertagt) | adr/0004 §8.6; architekturplan 6.5; projektplan M6 |
| 2026-09-20 | **Frontend auf Sentinel-2:** Vor der Umstellung setzt Otto den Tag `prototype-biomass` auf `main`. Dann wird das Frontend vollständig auf die earthx-API umgestellt, die BIOMASS-Teile im Frontend entfallen. Das Backend `app` bleibt vorerst; sein Entfernen (und damit das Ende der Secret-Ausnahme in `config.py`) wird als letzte M2-Aufgabe zur Entscheidung vorgelegt (Stufe B) | fest | ENTSCHEIDUNGEN §3; architekturplan 13; CLAUDE.md; plans/m1-fundament.md §1 (E2) |
| 2026-09-20 | **Gemischte Suche bleibt abgelehnt (`400`).** Der Viewer sucht je Datensatz. Umgesetzt wird die gemischte Suche erst mit eigenen Items in M3, wie adr/0005 §8.4 es vorsieht. Hebt die Zeile vom 2026-09-19 auf | fest | adr/0005 Regel I, §8.4; plans/m1-07-stac-api.md §16; earthx/api/federating_client.py |
| 2026-09-20 | **Bug-Report-Pipeline Stufe 2** (Knopf „Fehler melden") wird aus M2 herausgenommen und bis zur Frage nach dem Deployment vertagt, weil sie ein GitHub-Schreibrecht und damit ein Secret braucht | fest (vertagt) | projektplan 6.1; ENTSCHEIDUNGEN §4 |
| 2026-09-20 | **Theme-Umschalter** läuft im Viewer-Strang parallel zu M2, zunächst ohne helle Basiskarte. Die helle Basiskarte folgt erst, wenn die Bedingungen von Esri und die Alternativen geklärt sind | fest | ENTSCHEIDUNGEN §2; projektplan Viewer-Strang; prototyp-inventar N1, N2 |
| 2026-09-20 | **Ortssuche in M2 ausgeblendet (F1 a):** Nach der Umstellung des Frontends auf `earthx` fehlt die Geocode-Route des Prototyps; die Ortssuche kommt mit M3 („Ortssuche mit Umriss"). Die Nominatim-Bedingungen bleiben bis zum ersten öffentlichen Deployment offen | fest | plans/m2-format-und-viewer.md §1.2, M2-07a; projektplan M3; prototyp-inventar F2 |
| 2026-09-20 | **Frontend-Tests mit Vitest (F2 a):** reine Logik (URL-Bau für Kacheln und Statistik, Gruppierung, Umschaltpunkt der Coverage, Datums-Fallback), keine Oberflächentests. M2-07a richtet Vitest ein und nimmt es als Schritt in den CI-Job `frontend` auf; die Änderung an `.github/workflows/ci.yml` ist dafür erlaubt. Ergänzt `adr/0002`, das nur das Backend regelt | fest | plans/m2-format-und-viewer.md §1.2, M2-07; adr/0002; .github/workflows/ci.yml |
| 2026-09-20 | M2-02: Kachel-Pfad als ADR-Entwurf `docs/adr/0006-kachel-pfad.md`. Gemessen: `titiler.core` 2.3.0 lässt den freien `url`-Parameter über eine ersetzte `path_dependency` fallen (im OpenAPI-Schema nachgewiesen) und bringt selbst keinen HTTP-Client mit — der steckt in `rio-tiler` (`httpx2`, schon heute im venv, geladen auch ohne `STACReader`); `titiler.xarray` brächte `obstore`, `titiler.mosaic[mosaicjson]` `cogeo-mosaic`. Die Registry-Allowlist kennt den Asset-Host **nicht** (`policy_from_registry` liest nur `source.endpoint`), der Lesepfad stünde in M2-04 still. Assets von `sentinel-2-c1-l2a` liegen auf `e84-earth-search-sentinel-data…`, `sentinel-cogs…` gehört zur älteren Collection — `cloud-umgebung.md` §6 ist falsch beschriftet, beide Hosts sind erreichbar. Asset-Bucket und Earth Search senden `Access-Control-Allow-Origin: *`, ein Quicklook-Proxy entfällt damit ersatzlos. Kachel warm 54–193 ms, ab z12 rund 2 MB je Blockbereich von der Quelle, COG-Header 32 kB der teuerste Posten einer kalten Ansicht. Zustandsloses Mosaik über `mosaic_reader` trägt (Item-Liste eines Aufnahmetags ≤ ~680 Zeichen), kostet je Kachel aber 2,7 MB. Empfehlung: `titiler.core` exakt gepinnt, Mosaik in M2 nur im Zuschnitt, Statistik in die Kachel-URL mit eigenem Postgres-Cache (30 Tage), nur `https`, Prozess-Einstieg des `tiler` nach `api`, `httpx2` und `obstore` auf die Verbotsliste | fest: `adr/0006` von Otto am 2026-09-20 **angenommen**, die sieben Fragen aus §7 beantwortet (siehe die Zeile „Antworten zu adr/0006 §7") | adr/0006; architekturplan 6.3, 6.5, 12.3; adr/0001 Z4; cloud-umgebung §6; .importlinter; Planung M2-04, M2-06, M2-07b, M2-09 |
| 2026-09-20 | **Antworten zu adr/0006 §7:** **1** Mosaik in M2 **nur im Zuschnitt** (M2-06), nicht im Kachel-Pfad. **2** Feld **`asset_hosts`** an `SourceInfo`, ohne Vorgabewert (B10), fließt in die Gateway-Allowlist; Umsetzung in M2-04. **3** Statistik-Cache als **eigene Tabelle** `earthx_stats_cache` mit **30 Tagen**; ein Cache für den COG-Header kommt **nicht** in M2 (eigene offene Zeile). **4** Quicklook-Proxy entfällt, der Browser lädt das Thumbnail direkt — gilt nur für Quellen, deren Asset-Host CORS sendet (Bedingung am Datensatz, `AccessInfo.cors`), und hebt einen Punkt aus ENTSCHEIDUNGEN §2 auf (eigene Zeile). **5** Prozess-Einstieg des `tiler` wandert nach **`api`** (`earthx/api/tiler.py`); `access` behält Fabrik und Renderlogik, weil es `gateway` nach 3.1 nicht importieren darf. **6** `httpx2` und `obstore` auf die Verbotsliste von `http-only-in-gateway` und in `CORE` des AST-Tests; die Liste erfasst bewusst **nur direkte Importe**, dazu ein eigener Test gegen `rio_tiler.io.stac`/`STACReader`, weil `import-linter` Unterpakete externer Pakete ablehnt. **7** `adr/0006` auf **angenommen** | fest | adr/0006 §5, §7; architekturplan 6.3, 6.5, 12.3; .importlinter; Planung M2-04, M2-06 |
| 2026-09-20 | **Asset-Proxy aus `ENTSCHEIDUNGEN` §2 aufgehoben.** Der „Asset-Proxy mit Host-Allowlist als Vorläufer von `gateway`" stand dort unter dem, was vom Prototyp erhalten bleiben soll. Er entfällt ersatzlos: Sein einziger Grund war der Token (Inventar F15), der Allowlist-Gedanke ist in `gateway` aufgegangen (6.5, B8), und der Asset-Host von Sentinel-2 sendet `Access-Control-Allow-Origin: *` (adr/0006 §3.6) — der Browser lädt den Quicklook direkt, wie architekturplan 6.4 es ohnehin verlangt. **Bedingung:** Das gilt je Datensatz, nicht allgemein; für eine Quelle ohne CORS (EOPF-Objektspeicher: kein Header, Preflight `403`, adr/0007) käme ein Proxy zurück | fest (Aufhebung eines Punktes in ENTSCHEIDUNGEN §2) | ENTSCHEIDUNGEN §2; architekturplan 6.4, 6.5; adr/0006 §3.6, §5; adr/0007; prototyp-inventar F6, F15 |
| — | Cache für COG-Header (`architekturplan.md` 12.3, „Header-Infos von COGs"): In `adr/0006` §3.4 als größter Hebel für die erste Kachel einer kalten Ansicht gemessen (32 kB, ~629 ms Prozessstart), von Otto am 2026-09-20 aber **aus M2 herausgenommen** — in M2 kommt nur der Statistik-Cache. Wann und wo der Header-Cache gebaut wird, ist nicht entschieden | offen | architekturplan 12.3; adr/0006 §3.4, §7 Frage 3, §8 Punkt 8 |
| 2026-09-20 | M2-03: Zweites Format als ADR-Entwurf `docs/adr/0007-zweites-format-zarr.md`. Gemessen: Der Objektspeicher der aktuellen EOPF-Produkte ist **`data.eodc.eu` und aus der Cloud-Sitzung gesperrt** — das in `adr/0003` §10.1 gemessene `objects.eodc.eu` trägt nur die älteren Tutorial-Produkte und die S1-SLC-Collection; dieser Teil der Aufgabe ist deshalb angehalten. Die EOPF-STAC-API liefert **kein `numberMatched`** und **keine Aggregation** (404), Items sind STAC 1.1.0 an einer 1.0.0-API; kein Store führt `multiscales` oder eine CRS (die kommt aus `proj:code`), das ausgewiesene `TCI_10m`-Quicklook-Asset löst mit 404 nicht auf; eine Kachel in nativer Auflösung kostet 3–16 MB und mehrere Sekunden. Der Lesepfad dagegen ist vollständig gemessen und trägt: eigener `zarr.abc.store.Store` (jeder Byte-Read durch `gateway`) → `zarr` → `xarray` → `rio_tiler.io.xarray.XarrayReader` liefert Kachel, Statistik und Zuschnitt ohne zweiten HTTP-Stapel. `titiler-eopf` scheidet aus (Python ≥3.12, `zarr>=3.2`, `obstore`, kein Release), und unter dem CI-Pin Python 3.11 ist `zarr` auf 3.1.x festgelegt. Empfehlung: M2-09 in 09a (Reader gegen synthetisches Mini-Zarr, sofort) und 09b (realer Datensatz, nach Freigabe) teilen | fest: `adr/0007` von Otto am 2026-09-20 **angenommen**, die sieben Fragen aus §8 beantwortet — **F1** `data.eodc.eu` freigegeben, `download.user.eopf.eodc.eu` bleibt gesperrt (gezippte Produkte braucht die Plattform nie); **F2** M2-09 geteilt in 09a (Reader, quellenunabhängig) und 09b (realer Datensatz), M2-10 hängt an 09b; **F3** Lizenzeinstufung wie beim ersten Datensatz, dasselbe Sentinel Data Legal Notice, förmlich bestätigt; **F4** im Adapter von STAC 1.1.0 auf 1.0.0 normalisieren, Feldliste in §6 Punkt 4; **F5** Python bleibt 3.11, `zarr` auf 3.1.x; **F6** Mini-Zarr per Skript im Test, keine Binärdatei im Repo; **F7** mit F1 gegenstandslos, bleibt als Rückfallweg stehen. **Weiterhin unbelegt:** alles zu den bisher gesperrten Collections — das misst eine eigene, neu gestartete Sitzung vor M2-09b nach (§9.1) | adr/0007; adr/0003 §10.1, §10.3; adr/0004 §5, §6; architekturplan 6.2, 14 Punkt 5; cloud-umgebung §6; Planung M2-05, M2-07c, M2-09, M2-10 |
| 2026-09-20 | `cloud-umgebung.md` §6 nachgezogen (M2-03b): `data.eodc.eu` ist gemessen erreichbar, `stac.core.eopf.eodc.eu` ist neu in der Tabelle, `download.user.eopf.eodc.eu` und `stac.browser.user.eopf.eodc.eu` bleiben gesperrt. Dazu der Hinweis, dass die Sperre gezippte Produkte nicht mehr fernhält (`zipped_product`, 1,2 GB, liegt auf `data.eodc.eu`), und die Streichung des Satzes, die EOPF-Quelle lasse sich in einer Cloud-Sitzung nicht ausprobieren | fest (Sachstand) | adr/0007 §7 Punkt 3, §12.1; cloud-umgebung §6 |
| — | Ratengrenzen von EODC: keine dokumentierte Grenze gefunden, bei 10 parallelen Metadaten-Anfragen keine Drosselung. **M2-03b hat nachgelegt:** 12 parallele Chunk-Abrufe auf `data.eodc.eu` und 20 parallele STAC-Suchen, alle `200`, kein `429`, kein `Retry-After`. Die offene Zeile „Ratengrenzen der Anbieter" bleibt für EOPF trotzdem unbeantwortet — nichts davon ist ein Beleg für „keine Grenze" | offen | adr/0007 §3.9, §12.8; adr/0003 §10.4 |
| 2026-09-20 | **M1-03 F4 beantwortet: `gateway` bleibt `https`-only.** Der erste Zarr-Leser braucht kein `s3`-Schema. Gemessen an `objects.eodc.eu`: Chunks und konsolidierte Metadaten kommen mit anonymem `GET` über `https` (Range → `206`), und selbst das Auflisten geht ohne Signatur über `?list-type=2`. Auflisten wird ohnehin kaum gebraucht, weil die Stores konsolidierte Metadaten führen und die STAC-Items Gruppe und Variablen benennen — ein Store ohne konsolidierte Metadaten bleibt damit lesbar. Dasselbe gilt für die übrigen Kandidaten (`storage.googleapis.com`, AWS-Open-Data-Buckets). Schließt die Zeile „`s3` erst, wenn der erste Leser es braucht (M2)" | fest (Sachstand) | adr/0007 §3.10, §7 Punkt 9; plans/m1-03-fetch-gateway.md §8 F4; architekturplan 6.5; KLAERUNGEN B8 |
| — | **Wechsel von Python 3.11 auf 3.12** als eigene Aufgabe: `zarr` ab 3.2.0 verlangt 3.12, unter dem CI-Pin 3.11 bleibt der Zarr-Stapel auf `zarr` 3.1.x — der letzten Reihe, die 3.11 noch unterstützt. Ein Wechsel berührt CI-Matrix und Images und gehört nicht nebenbei in M2-09. In M2 bewusst nicht angefasst (`adr/0007` F5) | offen | adr/0007 §3.8, §8 F5; .github/workflows/ci.yml; backend/requirements.txt |
| 2026-09-20 | **`data.eodc.eu` in der Allowlist der Cloud-Umgebung freigegeben**, `download.user.eopf.eodc.eu` bleibt gesperrt (ganze gezippte Produkte werden nie gebraucht, der Lesepfad holt Chunks). Die Freigabe wirkt nur in neu gestarteten Sessions und betrifft allein die Umgebung; in die Gateway-Allowlist der Plattform kommt der Host erst über den Registry-Eintrag in M2-09b | fest | cloud-umgebung §4, §6; adr/0007 §8 F1; plans/m2-format-und-viewer.md §1.3, M2-09b |
| 2026-09-20 | **M2-Plan Fassung 2:** nach Annahme von adr/0006 und adr/0007 aufgenommen: Mosaik nur im Zuschnitt, `asset_hosts` in der Registry, Statistik-Cache 30 Tage, Quicklook ohne Proxy, Einstieg des `tiler` nach `api`, Verbotsliste um `httpx2` und `obstore`, Teilung in M2-09a und M2-09b, neue Aufgabe M2-03b, Ersatzregel für den Umschaltpunkt in M2-07c, Python bleibt 3.11 | fest | plans/m2-format-und-viewer.md; adr/0006 §7; adr/0007 §8 |
| 2026-09-20 | **M2-05 Plan angenommen, F1–F7 wie empfohlen.** Route `GET /coverage/{dataset_id}` an der Basis-App von `api`, außerhalb von `/stac`; das Pflichtfeld aus Regel V führt `complete`/`truncated`/`sample`; **keine Vorberechnung des ungefilterten Weltüberblicks** — gemessen liefert Earth Search ihn über Geotile bis z6 vollständig in unter 1,1 s, dazu 24 h Frist für die vollständig ungefilterte Anfrage und ein zweiter Stufendeckel z6 ohne räumlichen Filter; Zellen kompakt als `{"k","n"}` auf der Leitung, das Polygon rechnet der Client; M2-05 geteilt in **M2-05a** (Nahtstelle in `catalog`, Aggregation in `adapters`) und **M2-05b** (Route in `api`); Coverage-Cache in `public.earthx_search_cache` mit Schlüsselpräfix `coverage:`, ohne Migration | fest | plans/m2-05-coverage.md; adr/0004 §3.3, §5; plans/m2-format-und-viewer.md M2-05, M2-07c |
| 2026-09-20 | **Geotile kappt erst ab z7, und `overflow` bleibt `0`.** Die 10 000-Zellen-Grenze von Earth Search trifft das gewählte Geotile-Gitter viel später als Geohash p4: bis z6 ist der ungefilterte Weltüberblick vollständig (2863 Zellen, 160 kB, 0,88 s), ab z7 fehlen Aufnahmen und ab z8 ein Drittel — bei durchweg gemeldetem `overflow: 0`. Die Kopfzahl des Dienstes ist damit kein Vollständigkeitssignal, allein Regel V ist es | fest (Messung) | adr/0004 §3.3, §5; plans/m2-05-coverage.md §3.3 |
| — | **`gateway` erreicht die Quelle aus einer Cloud-Sitzung nicht.** `gateway` verbindet absichtlich die von `check_url` geprüfte **Adresse**, damit sich zwischen Prüfung und Verbindung nichts bewegt (M1-03); die Egress-Freigabe der Umgebung arbeitet dagegen **namensbasiert** und weist CONNECT auf eine Adresse mit `403` ab. Deshalb kommt `curl` durch und `tests_live` nicht. Kein Fehler und nichts zu ändern — die Adress-Pinnung ist eine Sicherheitseigenschaft. Status offen, weil dasselbe für ein späteres Deployment hinter einem Proxy gilt und dann entschieden werden muss, wie der Produktivpfad dort hinauskommt | offen | plans/m2-05-coverage.md §7.1, §10 F7; plans/m1-03-fetch-gateway.md; Planung M2-04, M2-09a, M2-09b |
| 2026-09-20 | **M2-05b: der Antwortkörper der Quelle bleibt aus Antwort und Log, `gateway` wird nicht angefasst.** Gemessen an sechs Fehlerbildern von `/aggregate`: Earth Search zitiert in keinem Fall einen Koordinatenwert aus der Anfrage — die Beschreibungen sind allgemein oder nennen einen Zeichen-Offset (`[1:177]`), also eine Position, nicht den Inhalt. Sechs Stichproben sind kein Beweis, deshalb trotzdem: der Auszug bleibt am `UpstreamError` (Test und Traceback brauchen ihn), aber die Route liefert und loggt nur Statuscode und eigenen Text. Die Lösung bleibt damit auf die Coverage begrenzt. **Dazu in den Umfang von M2-05b aufgenommen:** die Stützpunkte von `intersects` gegen ±90/±180 prüfen — ein Polygon mit Länge 999 und Breite 888 beantwortet die Quelle mit `200` und einer plausiblen Zahl, dasselbe stillschweigende Annehmen, das `adr/0005` §3.5 für die bbox beschreibt | fest | plans/m2-05-coverage.md §3.5, §3.6; projektplan 7 Punkt 6; adr/0005 §3.5 |
| 2026-09-20 | **M2-04, Feldform der Standard-Visualisierung (Checkliste Punkt 8):** `DefaultRender` trägt die Feldnamen der STAC-`render`-Extension — `title`, `assets`, `rescale`, `colormap_name`, `expression`, `resampling` — statt `bands`/`stretch`/`colormap`. Grund: Es sind dieselben Namen, die die Kachel-URL ohnehin führt, und dieselben, unter denen die Werte später als `renders` an der eigenen Collection veröffentlicht werden können (`adr/0006` §5 „Zu Frage 3"). Für Sentinel-2 steht dort `visual` mit `rescale` **0–255 je Band**, gemessen an fünf echten Szenen auf vier Kontinenten: das zweite Perzentil des TCI läuft von 9 über Vegetation bis 202 über Wüste, ein fester Stretch würde also eine Klimazone abschneiden; den szenenbezogenen Streckbereich holt der Viewer aus `/statistics` (F18) | fest (Otto am 2026-09-20, Frage 1 zu M2-04: Option 1) | registry; catalog/collection; adr/0006 §5; projektuebersicht §5 Punkt 8; Planung M2-07b |
| 2026-09-20 | **M2-04, Form der Kachel-URL:** Die Routen des `tiler` liegen unter `/collections/{dataset}/items/{item}/…` — dieselbe Form, unter der die STAC-API dasselbe Item beschreibt. Der Asset-Schlüssel ist **Pflicht-Abfrageparameter** (`?asset=visual`) und wird **nicht** aus `default_render` vorbelegt: sonst zeigte dieselbe URL nach einer Registry-Änderung ein anderes Bild (Z4). Die Standard-Visualisierung erreicht den Client über die Collection | fest (Otto am 2026-09-20, Frage 2 zu M2-04: Option 1) | adr/0001 Z4; adr/0006 §5.1; architekturplan 6.3; Planung M2-06, M2-07b |
| 2026-09-20 | **`earthx:viewer` als neuntes Plattformfeld (von Otto freigegeben, Umfang von M2-04 erweitert):** Neben `earthx:default_render` tragen Registry und Collection ein Feld `earthx:viewer`. Es hält in M2 genau einen Wert, `group_by` — den Gruppierungsschlüssel der Zeitleiste —, damit der Viewer ihn liest statt ihn im Code zu führen. **Vorgeschlagene Form:** eine Liste von Item-Eigenschaften in Schlüsselreihenfolge, `properties.` impliziert, ohne Vorgabewert (B10); eine Eigenschaft mit einem STAC-Zeitpunkt geht als ihr **UTC-Datum** in den Schlüssel ein, weil eine Zeitleiste einen Aufnahmetag gruppiert und keine Sekunde. Für Sentinel-2 steht dort `["datetime", "grid:code"]` (Aufnahmetag je MGRS-Kachel). Verworfene Alternative: ein zweites Feld `group_by_date` statt der Typregel — eindeutiger, aber breiter als nötig. M2-07a liest das Feld und legt nichts eigenes fest. **Die Regel steht an der Feldbeschreibung** (`ViewerInfo.group_by`, `architekturplan.md` 5.1) und ist als Test belegt: `group_key` ist die Referenzumsetzung, `tests/catalog/test_group_key.py` prüft Tageswechsel, zweite Zeitzone und fehlende Eigenschaft | fest (Otto am 2026-09-20, Frage 3 zu M2-04: Option 1; Alternative `group_by_date: bool` benannt und verworfen) | architekturplan 5.1 (neunte Zeile); registry; catalog/collection; plans/m2-format-und-viewer.md M2-07a |
| 2026-09-20 | **`AccessInfo.cors = True` für Sentinel-2.** Das Feld stand seit M1-04 auf `None` („nicht gemessen"). `adr/0006` §3.6 hat es gemessen — Asset-Bucket und Earth Search senden `Access-Control-Allow-Origin: *`, auch auf die OPTIONS-Vorabanfrage und für JPEG wie GeoTIFF. Damit ist die Bedingung erfüllt, unter der der Quicklook-Proxy entfällt (D14): Der Browser lädt das Thumbnail direkt und darf es fürs Canvas-Keying auswerten (Inventar F6). Der Wert gilt **je Datensatz**; für eine Quelle ohne CORS käme ein Proxy zurück | fest | registry (`AccessInfo`); catalog/datasets; adr/0006 §3.6, §5 „Zu Frage 5"; ENTSCHEIDUNGEN §2; prototyp-inventar F6 |
| 2026-09-20 | **B008 für FastAPI-Abhängigkeiten abgeschaltet, nicht je Zeile unterdrückt:** `pyproject.toml` nennt `fastapi.Depends`, `fastapi.Path` und `fastapi.Query` in `ruff.lint.flake8-bugbear.extend-immutable-calls`. `Depends(...)` im Vorgabewert ist die Art, wie FastAPI eine Abhängigkeit deklariert — der Aufruf läuft einmal beim Import und wird nie verändert, also genau der Fall, nach dem B008 fragt. So bleibt die Regel für alles andere an, statt in jedem Endpunkt ein `noqa` zu tragen | fest (Otto am 2026-09-20, Frage 4 zu M2-04: Option 1) | pyproject.toml; access/tiles; api/tiler |
| 2026-09-20 | M2-03b: Nachmessung von `sentinel-2-l2a-zarr3` nach der Freigabe, als §12 in `adr/0007`. Der **v3-Bestand ist anders gebaut als das v2-Tutorial-Produkt**, an dem §3 gemessen hatte, und durchweg günstiger: `multiscales` (Pilotkonvention v0.1) **und** CRS im Store, **sechs** Auflösungsstufen (`r10m` bis `r720m`) statt drei, **Sharding** mit 1024er Innenchunks hinter einem 11264er Shard. Eine Kachel in nativer Auflösung kostet damit **0,9–6,3 MB je Band** (Echtfarbe rund 3,7 MB) statt der in §3.6 gefürchteten 3–16 MB; Statistik auf `r720m` 0,04 MB und 0,6 s; AOI-Zuschnitt skaliert mit den geschnittenen Innenchunks. `data.eodc.eu` sendet **CORS vollständig, einschließlich `Range`** (`AccessInfo.cors = True`; §3.9 hatte `objects.eodc.eu` gemessen), aber **kein `ETag`/`Cache-Control`**. Der Lesepfad aus §6 läuft end-to-end gegen die echte Quelle: Statistik, Kachel und Zuschnitt in **15 Anfragen und 2,6 MB**. Dagegen: Die Collection heißt selbst **„Zarr3 staging"** (K9 bleibt verletzt und ist schärfer), Bestand gut zwei Monate und nur 34°–72° N bei rund 1 300 Items je Tag, **kein Quicklook-Asset überhaupt**, `numberMatched` und Aggregation fehlen weiterhin. **Alle sechs offenen Punkte aus §9.1 sind beantwortet** (§12.12), darunter: die 2017er Items von `sentinel-2-l2a` zeigen auf existierende Objekte, `TCI_10m` löst auf keinem Host auf. Empfehlung: `sentinel-2-l2a-zarr3` trägt als zweiter Datensatz, Zoomstufen **z8–z14** freigeben, Auflösungsstufen aus dem **Store** lesen (das Item weist nur drei der sechs aus) | **fest**: von Otto am 2026-09-20 entschieden, alle drei Fragen aus §12.13 wie empfohlen — **F8 (a) mit zwei Auflagen**, **F9 (a)** Zoomstufen **z8–z14** mit Überzoom darüber, die Stufe aus der Bodenauflösung der Kachel gerechnet statt fest zugeordnet, **F10 (a)** Auflösungsstufen aus `multiscales` im **Store**, Item als Rückfall. `adr/0007` ist damit **ohne offene Fragen angenommen** | adr/0007 §12; adr/0004 §5, §6; adr/0006 §7 Punkt 4; cloud-umgebung §6; plans/m2-format-und-viewer.md M2-03b, M2-09b, M2-10 |
| 2026-09-20 | **Die zwei Auflagen zu F8** (`adr/0007` §12.11 Punkt 14 und 15): **(1)** Der Status **„staging"** wird sichtbar ausgewiesen — als eigenes Feld im Registry-Eintrag und im Viewer an der Stelle, an der der Datensatz gewählt wird; das Feld gibt es heute nicht, seine Form schlägt M2-09b im Plan-Schritt vor, die Anzeige macht M2-10. **(2)** Die **Abnahme von M2 hängt nicht am Fortbestand der Quelle**: Für das Kriterium „zweites Format" genügt der Lesepfad gegen das synthetische Mini-Zarr aus **M2-09a**. Fällt `sentinel-2-l2a-zarr3` weg, fällt die Aufnahme des zweiten Datensatzes (M2-09b, M2-10), nicht die Abnahme von M2 — der Rückfallweg aus `adr/0007` §8 F7 ist damit keine Möglichkeit mehr, sondern die stehende Regel | fest | adr/0007 §6, §12.11 Punkt 14–15, §12.13; plans/m2-format-und-viewer.md §5, M2-09b, M2-10, M2-12; projektuebersicht §5 |
| — | **`plans/m2-format-und-viewer.md` nachziehen, drei Stellen:** (1) §5 verlangt in den Abnahmekriterien 1, 3 und 5 weiterhin „beide Datensätze" — gegen die zweite F8-Auflage zu prüfen und gegebenenfalls umzuformulieren. (2) Die Risikotabelle nennt „Native Zarr-Kacheln kosten 3–16 MB"; gemessen sind es 0,9–6,3 MB je Band (`adr/0007` §12.4). (3) Dieselbe Tabelle stellt den EOPF-Bestand und den fehlenden Quicklook auf „M2-03b misst nach" — das ist erledigt (`adr/0007` §12.6, §12.7). Alles drei sind Planänderungen und gehören zu **M2-12** | erledigt mit Fassung 5 des M2-Plans | plans/m2-format-und-viewer.md §5, Risikotabelle; adr/0007 §12.4, §12.6, §12.7, §12.11 Punkt 15 |
| 2026-09-20 | **Umsetzungsauflagen für M2-09b**, von Otto am 2026-09-20 ausdrücklich bestätigt; aus `adr/0007` §12.11: Der gateway-gebundene Store **muss Byte-Ranges können** (`get_partial_values`, `Range`/`Offset`/`Suffix`), sonst holt `zarr` je Kachel den ganzen 158-MB-Shard; Bänder **nie** über `to_dataarray`/`to_array` stapeln (materialisiert die Arrays vollständig — in der Messung 1,5 GB), sondern einzeln fenstern; beim Öffnen `zarr_format=3` setzen (11 → 5 Anfragen); `asset_hosts` = `data.eodc.eu`; `zipped_product` (1,2 GB) nicht in den Registry-Eintrag; Cache nach Frist statt Revalidierung; `sentinel-2-l2a` und `sentinel-2-l2a-zarr3` führen dieselben Item-IDs, aufgenommen wird nur `…-zarr3` | fest (Umsetzungsauflagen) | adr/0007 §12.11; plans/m2-format-und-viewer.md M2-09b |
| 2026-09-20 | **M2-07a: Plan angenommen, sechs Fragen beantwortet.** **1** Gruppierungsschlüssel für Sentinel-2 ist **Datum + `grid:code`**, nicht `s2:datatake_id`: Nach D11 gibt es in M2 kein Mosaik im Kachel-Pfad, eine Gruppe muss also auf genau ein Item und damit eine Kachel-URL führen, während ein Datatake viele MGRS-Kacheln umfasst; `s2:datatake_id` sollte als angezeigte Eigenschaft der Gruppe erhalten bleiben — **überholt:** M2-04 hat `ViewerInfo` bewusst auf `group_by` beschränkt, der Datatake wird in M2 nicht geführt. **2** Das Feld dafür kommt als neuntes `earthx:`-Feld **`earthx:viewer`**, angelegt **in M2-04** (eigene Zeile dort); M2-07a liest es nur und hängt damit an M2-04. **3** M2-07a wird **ein** PR samt Abbau der Pfade ohne `earthx`-Backend, rund 700 geänderte Zeilen davon etwa 400 Löschungen; der PR vermerkt ausdrücklich, dass der Viewer auf `main` zwischen 07a und 07b keine vollaufgelösten Bilder zeigt. **4** Der TypeScript-Client bleibt in M2 handgeschrieben (eigene Zeile). **5** Datums-Fallback über Fenster ±7/±30/±90 Tage mit dem zeitlich nächsten Item und sichtbarem Hinweis; weil die Quelle nicht sortiert und `sort` in `earthx` abgeschaltet ist, wird je Stufe **eine** Seite mit `limit=100` gesondet und daraus nur das Datum bestimmt, die vollständige Suche für dieses Datum läuft danach normal mit Seitenmarke — eine gekappte Sonde sagt im Hinweis „nächstgelegene **gefundene** Aufnahme". **6** Serverzustand bleibt bei `zustand`, kein TanStack Query in M2 | fest | plans/m2-07a-frontend-api-suche-quicklooks-zeitleiste.md; plans/m2-format-und-viewer.md §1.1 D19/D20, §3, M2-04; architekturplan 8.1, 8.2; adr/0004 §3.3; prototyp-inventar F5, F6 |
| 2026-09-20 | **TypeScript-Client bleibt in M2 handgeschrieben.** `architekturplan.md` 8.1 sieht einen aus dem OpenAPI-Schema generierten Client vor; in M2 hat der Client zwei Funktionen, und die Kachel-, Coverage- und Download-Routen stehen erst mit M2-04 bis M2-06 fest. Die Generierung wird eine **eigene Aufgabe nach M2-06** | fest | architekturplan 8.1; plans/m2-format-und-viewer.md D20; frontend/src/api.ts |
| 2026-09-20 | **M2-07a an den gemergten Stand von M2-04 (#39) angeglichen.** Das Feld heißt `earthx:viewer` mit dem einen Schlüssel **`group_by`**, für Sentinel-2 `["datetime", "grid:code"]`; eine Eigenschaft mit STAC-Zeitpunkt geht als ihr **UTC-Datum** in den Schlüssel ein. §5 des 07a-Plans ist damit keine Bestellung mehr, sondern Beschreibung des Vorhandenen. Dazu zwei Festlegungen von Otto: **(a) keine Vorgabewerte im Frontend** — fehlt `earthx:viewer.group_by`, ist das ein Fehler des Datensatzes und kein Fall für einen eingebauten Ersatzschlüssel; der Viewer meldet ihn, statt ersatzweise nach Datum zu gruppieren. **(b)** Die **12 Fälle aus `backend/tests/catalog/test_group_key.py`** werden als Vitest-Fälle übernommen, damit `group_key` und die Frontend-Umsetzung nicht auseinanderlaufen: gleicher Tag mit zwei Szenen, derselbe Moment in `Z` und in `+02:00`, eine Sekunde über UTC-Mitternacht, fehlende Eigenschaft als Fehler | fest | plans/m2-07a-frontend-api-suche-quicklooks-zeitleiste.md §4.3, §5, §6; catalog/registry.py (`ViewerInfo`, `group_key`); tests/catalog/test_group_key.py; architekturplan 5.1 |
| — | **Schwelle des Nodata-Keyings für Quicklooks** (Inventar F6: „gehört als Feld an den Datensatz statt in den Code"): `ViewerInfo` trägt nach M2-04 bewusst nur `group_by`, ein Feld dafür gibt es also nicht. M2-07a behält die 16 des Prototyps als benannte Konstante im Frontend und wählt das Quicklook-Asset generisch über die Rolle (`thumbnail`, dann `overview`, dann `image/*`). Zu entscheiden, sobald ein zweiter Datensatz eine andere Schwelle braucht — bei EOPF stellt sich die Frage nicht, dort gibt es keinen Quicklook (`adr/0003` §10.3) | offen | prototyp-inventar F6; catalog/registry.py (`ViewerInfo`); plans/m2-07a-… §4.4, §5; Planung M2-10 |
| 2026-09-20 | **M2-Plan Fassung 3:** nach #37–#40 aufgenommen: D19 `earthx:viewer`, D20 `DefaultRender` und Pflicht-`asset`, D21 `UrlTooLong` nur nach gemessener Länge, D22 Histogramm monatlich, D23 zweiter Datensatz `sentinel-2-l2a-zarr3` mit z8–z14 und drei Auflagen, D24 die Abnahme hängt nicht am Fortbestand der Quelle. Dazu Teilung M2-05a/05b, neue Aufgabe M2-13 (SessionStart-Hook, Live-Smoke-Nachtrag), Abnahmekriterien 1, 3 und 5 entsprechend gefasst | fest | plans/m2-format-und-viewer.md; adr/0006 §7; adr/0007 §12 |
| 2026-09-20 | **M2-07a umgesetzt.** `frontend/src` spricht nur noch mit `earthx` unter `/stac` (`api.ts`), wählt den Datensatz aus `/stac/collections`, gruppiert Items generisch über `earthx:viewer.group_by` (`grouping.ts`, mit den 12 gespiegelten Fällen aus `test_group_key.py`), zeigt Quicklooks direkt vom Asset-Host ohne Proxy (D14) und fällt bei leerem Zeitraum auf das nächstgelegene Datum zurück (`dateFallback.ts`, ±7/±30/±90 Tage, Plan §4.5.1). BIOMASS-Pfade (`products.ts`, `ViewerControls.tsx`, `DownloadBar.tsx`, `SearchBox.tsx`, Token-Meldung) sind abgebaut; der Viewer zeigt bis M2-07b nur noch Quicklooks, keine Kacheln, Darstellungssteuerung, Coverage oder Download. Vitest eingerichtet und im CI-Job `frontend` (F2 = a). | fest | plans/m2-07a-frontend-api-suche-quicklooks-zeitleiste.md; frontend/src; .github/workflows/ci.yml |
| 2026-09-20 | **M2-06, drei Punkte aus dem Plan-Schritt von Otto entschieden:** **(1) Prozess:** die Download-Route läuft im `tiler`-Prozess (`earthx.api.tiler`), der die Item-Auflösung über `gateway` und die zentrale GDAL-Konfiguration bereits für den Kachel-Pfad hält. **(2) Größendeckel**, vor jedem Lesezugriff geprüft: **4096×4096 px** je Asset (strukturell über `max_size` an jedem Lesezugriff erzwungen) und **200 MB** insgesamt über alle Items und Assets einer Anfrage (worst-case-Rechnung vor dem Öffnen eines Readers, ohne Quellenmessung — ein Startwert, keine Messung wie in `adr/0006` §3.4). **(3) Hinweisdatei:** eine Textdatei (`ATTRIBUTION.txt`), keine zusätzliche JSON-Begleitdatei. Umgesetzt: `access/download.py` (Zuschnitt, Mosaik über `rio_tiler.mosaic_reader` nur hier, nie im Kachel-Pfad, D11; echte COGs vollständig im Arbeitsspeicher über `MemoryFile`/`/vsimem`, `rio_cogeo.cog_translate`), Route `POST /collections/{dataset}/download` in `api/tiler.py`; Download nur ab Lizenzstufe **Processing** (KLAERUNGEN B11) | fest (Otto am 2026-09-20, alle drei Empfehlungen bestätigt) | plans/m2-format-und-viewer.md M2-06; architekturplan 6.4 (Nachtrag D3); adr/0006 §3.4, §3.5, §4.2; access/download.py; api/tiler.py; KLAERUNGEN B11 |
| 2026-09-20 | **M2-13: Nachtrag in `adr/0002` (T-D).** Der Befund, dass ein Live-Smoke aus einer Cloud-Sitzung nicht von Hand nachfahrbar ist (`gateway` bindet an die geprüfte Adresse, die Egress-Freigabe der Sitzung arbeitet namensbasiert und weist `CONNECT` mit `403` ab), stand bislang nur verstreut in `adr/0003`, `adr/0007` und `plans/m2-05-coverage.md`. Er ist jetzt an `adr/0002` T-D festgehalten, dem Dokument, das eine Live-Smoke-Aufgabe zuerst liest; Latenzen für einen PR werden über `curl` belegt, nicht über einen Sitzungslauf. `gateway` bleibt unangetastet. Dazu: Der SessionStart-Hook installiert Backend-Abhängigkeiten jetzt bei jedem Start, auch wenn das venv bereits existiert, damit ein älteres venv (z. B. ohne `titiler.core`, seit M2-04 in `backend/requirements.txt`) nachzieht, statt dass Sitzungen es von Hand nachholen müssen | fest | adr/0002 (T-D-Nachtrag); scripts/setup-cloud-session.sh; plans/m2-format-und-viewer.md M2-13 |
| 2026-09-20 | **M2-07b umgesetzt.** Der fokussierte Modus zeigt volle Kacheln aus dem `tiler`-Prozess (M2-04) statt Quicklooks: eine Kachel-URL je gewähltem Item, `asset` aus der Standard-Visualisierung der Registry (`earthx:default_render`), Streckbereich einmal aus `/statistics` gemessen (adr/0006 §3.4, 2./98. Perzentil über alle Bänder) und über `rescale`/`colormap_name` in die Kachelvorlage geschrieben. `ViewerControls` macht Colormap/vmin/vmax mit explizitem „Apply" editierbar (F18), `ViewBar` ersetzt für den Einstieg die BIOMASS-`DownloadBar` (kein Datei-Download hier, der bleibt M2-06/M2-07d). Der Layer-Manager (F19, für Quicklooks bereits vorhanden) pinnt jetzt auch volle Kacheln als `raster`-Overlay. `AppliedRender`/`DownloadedInfo` tragen jetzt TiTiler's eigene Parameter (`bidx`, `colormap_name`) statt der Namen des Prototyps; `aoiHash` (ungenutzter Rest des AOI-Zuschnitt-Flusses) ist weg | fest | plans/m2-format-und-viewer.md M2-07b; frontend/src/store.ts, api.ts, render.ts, components/ViewBar.tsx, components/ViewerControls.tsx |
| 2026-09-20 | **M2-07c umgesetzt.** Die Coverage-Heatmap gegen `GET /coverage/{dataset_id}` (M2-05b): `fill`-Layer mit logarithmischer Skala, Stützstellen aus `max_count` der jeweiligen Antwort (`coverage.ts::coverageFillColorExpression`), Legende „Aufnahmen mit Mittelpunkt in der Zelle" und der Hinweis zu `gekappt`/`stichprobe` auf Deutsch (adr/0004 §5.3: die deutschen Begriffe bleiben Sprache der Oberfläche), Zeit-Histogramm neben dem Datumsfilter in `ControlPanel.tsx`. Umschaltpunkt Dichte→Footprints folgt allein `footprints_advised` aus der Antwort (die Ersatzregel für eine Stichprobe ohne geprüfte Gesamtzahl ist damit serverseitig schon erfüllt), zusätzlich gebremst durch eine Zoomstufe, unter der Otto nicht gefragt wurde: **`FOOTPRINT_MIN_ZOOM = 4`** ist eine Setzung dieser Sitzung, keine Messung, ebenso der Cache **`FOOTPRINT_FETCH_LIMIT = 500`** (= Backend-`FOOTPRINT_THRESHOLD`). Ein Fehlschlag der (optionalen) Footprint-Anfrage lässt die schon geladene Dichte stehen (E5) statt die ganze Ebene zu leeren. `searchAllPages` ist aus `store.ts` nach `api.ts` gewandert, damit Suche und Coverage-Footprints dieselbe Paging-Funktion teilen. Ein echter Browser-Lauf (Chromium, gemockte `/coverage`-Antwort) deckte einen MapLibre-Fehler auf, den `tsc` nicht sieht: `interpolate`-Stützstellen müssen echt aufsteigen, was bei `max_count <= 1` sonst kollabiert — behoben und mit `it.each([0,1])` abgesichert. Live gegen Earth Search nicht vorführbar (M2-13/adr/0002 T-D: `gateway` erreicht die Quelle aus dieser Sitzung nicht); `/coverage` antwortet dafür nachweislich mit `502` | fest (Sitzungsentscheidungen zu Zoom-Bremse und Footprint-Deckel; Otto kann beides mit einer Messung ersetzen) | plans/m2-format-und-viewer.md M2-07c; adr/0004 §5; frontend/src/coverage.ts, mapLayers.ts, store.ts, components/ControlPanel.tsx, components/MapView.tsx |
| 2026-09-20 | **M2-07c, zwei Korrekturen nach Ottos lokalem Test.** (1) **Bug behoben:** `refreshCoverage` schickte den kompletten Kartenausschnitt als `bbox` bei jedem `moveend` — das macht aus jedem Browsen ungefragt eine „gefilterte" Anfrage und schaltet den Weltüberblick-Deckel (`WORLD_LEVEL_CAP`, z6) aus `adr/0004` §6.3 dauerhaft aus, ohne dass Otto je eine Fläche gewählt hätte. `bbox` kommt jetzt nur noch aus der echten Such-AOI (`store.aoi`); die Kartenzoom-Stufe bleibt wie vorgesehen der Ausgangswert für `zoom` (`level_for_viewport`). **Gemessen** (`level_for_viewport` direkt aufgerufen, Sentinel-2-Eintrag, z8/z6-Deckel): Kartenzoom → Stufe läuft 1:1 bis z6 (z. B. Zoom 1,6 → Stufe 1 = **4 Weltzellen à 180°**, Zoom 4 → Stufe 4 = 256 Zellen à 22,5°), ab Zoom 7 klemmt der Weltüberblick-Deckel auf 6 (**4096 Zellen à 5,6°**), ab Zoom 9 der Datensatz-Deckel auf 8. Bei der Standard-Ansicht (Zoom 1,6, keine AOI) liegt die angeforderte Stufe damit **nicht** an einem Deckel — beide liegen weit darüber —, sondern ist die wörtliche Ableitung aus dem Kartenzoom selbst; das macht die Zellen beim Herauszoomen riesig, unabhängig vom jetzt behobenen `bbox`-Fehler. Ein Vorschlag ohne Deckel-Verstoß liegt im PR: für die ungefilterte Weltansicht **immer** Stufe 6 anfordern (§3.3 misst dafür 2863 Zellen, 160 kB, 0,88 s — deutlich unter der 500-kB-Umkehrschwelle), unabhängig vom Kartenzoom; erst mit einer echten AOI folgt die Stufe wie bisher dem Zoom bis Deckel 1 (z8). Das ändert `catalog/coverage.py::level_for_viewport` (M2-05, bereits angenommen und getestet) und bleibt deshalb Otto vorbehalten. (2) **Sprache:** Die in M2-07c neu eingeführten Oberflächentexte (Legende, `gekappt`/`stichprobe`-Hinweis) sind auf Englisch umgestellt, passend zur übrigen (englischen) Oberfläche. `adr/0004` §5.3 verlangt heute noch ausdrücklich die deutschen Begriffe „an der Stelle, wo die Legende gebaut wird" — das ist jetzt ein Widerspruch zum tatsächlichen Code und braucht Ottos Entscheidung samt ADR-Nachtrag, nicht nur eine stille Änderung in einer Komponente | offen (beide Punkte: Gitterstufen-Vorschlag und Sprachfrage warten auf Otto) | plans/m2-format-und-viewer.md M2-07c; adr/0004 §5.3, §6.3; catalog/coverage.py::level_for_viewport; frontend/src/store.ts, coverage.ts, components/ControlPanel.tsx |
| 2026-09-22 | **M2-07c abgeschlossen im kleinstmöglichen Umfang** — die Gitterstufen-Verbesserung aus der vorigen Zeile hat Otto zurückgestellt, `level_for_viewport` bleibt unangetastet. Drei Punkte: **(1)** Bug behoben: eine gezeichnete AOI konnte über einer gewrappten Weltkopie Längen jenseits ±180 tragen (MapLibre wiederholt die Karte bei niedrigem Zoom, ein Rechteck darüber behält die unverpackten Koordinaten); die Coverage-Route weist das mit `400` ab, die Heatmap verschwand. `clampBboxLongitude` (`coverage.ts`) kappt `west`/`east` hart auf ±180, bevor die Anfrage rausgeht — mit einem echten Chromium-Lauf bestätigt (weit herausgezoomt, breiter Zug ergibt jetzt `bbox=-180,...,180` statt einer abgewiesenen Anfrage). **(2)** Alle in M2-07c eingeführten Oberflächentexte (Legende, `gekappt`/`stichprobe`-Hinweis) waren schon mit der vorigen Zeile auf Englisch umgestellt; unverändert offen bleibt `adr/0004` §5.3 gegen den tatsächlichen (englischen) Code. **(3)** Die Heatmap ist standardmäßig aus (unverändert der Vorgabewert) und schaltet sich jetzt über „Layers" ein — der Schalter ist aus `ControlPanel.tsx` in `LayerManager.tsx` gewandert, als eigene Zeile mit demselben Auge-Symbol wie die gepinnten Layer, aber ohne Deckkraft/Reihenfolge, weil es kein gepinnter Layer ist. Mit einem echten Chromium-Lauf bestätigt: keine `/coverage`-Anfrage vor dem Einschalten | fest | plans/m2-format-und-viewer.md M2-07c; frontend/src/coverage.ts, store.ts, components/ControlPanel.tsx, components/LayerManager.tsx |
| 2026-09-20 | **Coverage-Heatmap im Viewer zurückgestellt.** Die Umsetzung aus M2-07c ist in der Übersicht zu grob (weltweit höchstens z6, rund 625 km) und wird nur mit Minimalumfang gemergt: Ausschnitt auf ±180° begrenzt, englische Texte, standardmäßig aus. Die Verbesserung kommt nach M2; Kandidat ist ein täglich aktualisierter Zählwürfel je Datensatz (Zelle z9 × Monat × Wolkenklasse), der M2-05 F3 („keine Vorberechnung") aufheben würde und vorher gemessen werden muss. Die M2-Abnahme verlangt für die Coverage nur die Route mit geprüfter Vollständigkeit, nicht die Qualität der Anzeige | fest (vertagt) | adr/0004; plans/m2-format-und-viewer.md M2-07c, §5; plans/m2-05-coverage.md |
| 2026-09-20 | **M2-14: Die Auflösung des Asset-Hosts wird 5 s lang zwischengespeichert, die Prüfung nicht.** `CachingResolver` in `gateway/resolver.py`, durch die seit M1-03 vorhandene `resolve`-Naht von `check_url` hereingereicht; eine Instanz je `tiler`-Prozess, geteilt von der Pfad-Abhängigkeit und dem `Gateway`-Client. Gespeichert wird allein, was der Systemresolver geantwortet hat — `check_addresses` läuft bei jedem Treffer erneut über die volle Adressliste, eine Frist von 5 s liegt am gemessenen Satz-TTL des Hosts (`e84-earth-search-sentinel-data.s3.us-west-2.amazonaws.com`, A-Eintrag 5 s), und eine frische Antwort **ersetzt** die alte, statt mit ihr vereinigt zu werden. Nur Erfolge werden gespeichert, höchstens 256 Hosts. Verworfen: Memoisierung als Modulglobal in `resolve_host` (verborgener Prozesszustand im `gateway`) und ein Cache des fertigen `AssetPath` (überspränge `check_url` ganz). Gemessen an 80 gleichzeitigen Kacheln: **eine** Auflösung statt 80, Median je Anfrage 3024 → 1018 ms | fest (Otto am 2026-09-22: 5 s angenommen) | plans/m2-format-und-viewer.md M2-14; gateway/resolver.py; readers/cog.py; api/tiler.py; KLAERUNGEN B8; plans/m1-03-fetch-gateway.md |
| — | **Zahl der Uvicorn-Worker des `tiler`.** Befund 2 aus dem Abschnitt „Ladeverhalten" in #47: `docker-compose.yml` startet `tiler` mit genau einem Worker, und `anyio`s Thread-Pool für den blockierenden Render-Code ist prozessweit auf 40 gleichzeitige Worker gedeckelt — ab etwa 40 Kacheln (ein Vollbild sind 20–30, mit gepinnten Ebenen schnell mehr) warten Anfragen in der Warteschlange. M2-14 hat das **nicht** angefasst: es ist eine Betriebsfrage (Worker-Zahl, Thread-Pool-Größe, Speicher je Worker) und gehört nach **M5**, nicht in eine kleine Korrektur am Lesepfad | offen | plans/m2-format-und-viewer.md M2-14; docker-compose.yml; adr/0006 §3.4; projektplan M5 |
| 2026-09-20 | **Karte ohne Drehen und Kippen, Globus umschaltbar.** Maus und Touch verschieben und zoomen nur; die Globusprojektion von MapLibre GL JS (ab 5.0, Januar 2025) ist reine Darstellung im Browser, Backend und Kachel-URLs bleiben unverändert. Zusammen mit dem Theme-Umschalter in V-1, startet nach M2-15 | fest | plans/m2-format-und-viewer.md V-1, D27 |
| 2026-09-22 | **M2-07d umgesetzt.** Ein „Download"-Knopf am gepinnten Vollauflösungs-Layer im Layer-Manager öffnet einen Dialog gegen `POST /collections/{dataset}/download` (M2-06): Attribution und der `terms_notice` der Quelle stehen sichtbar vor dem Herunterladen, dazu der Link auf `terms_url` (adr/0003 §11.2). Die Collection trägt dafür jetzt `earthx:license_flags` im Frontend-Typ (`Collection`); `attributionText`/`termsNoticeText` füllen `{year}`/`{terms_url}` clientseitig, da `earthx:license_flags` die Rohtexte der Registry unverändert überträgt. Der Knopf erscheint nur an Layern mit `focusMode`, gemerkter AOI und mindestens einem `downloaded`-Eintrag (`downloadRequestFor`) — ein reiner Quicklook-Layer hat weder Asset-Schlüssel noch Zuschnitt-AOI. Eine Lizenzstufe unter Processing deaktiviert den Download-Knopf im Dialog, statt den Nutzer den `403` erst am Server erleben zu lassen (Checkliste, KLAERUNGEN B11). **Sprache:** Wie am 20.09.2026 für die Oberfläche festgelegt, läuft der Download durchgehend auf Englisch — `language: 'en'` in der Anfrage, Attribution/`terms_notice` im Dialog aus dem `en`-Text der Registry, mit Rückfall auf `en`, dann auf die erste vorhandene Sprache, falls eine künftige Quelle kein Englisch führt. `LayerRestore` trägt jetzt zusätzlich `datasetId`, damit ein gepinnter Layer sein eigenes Datenset für den Download kennt, auch wenn die Auswahl im Bedienfeld inzwischen weitergeschaltet ist | fest | plans/m2-format-und-viewer.md M2-07d; frontend/src/download.ts, store.ts, api.ts, types.ts, layers.ts, components/DownloadDialog.tsx, components/LayerManager.tsx |
| 2026-09-22 | **M2-15 umgesetzt: Oberfläche durchgehend englisch.** Die drei verbliebenen deutschen Reste seit M2-07: der Datums-Fallback-Hinweis und die App-Unterzeile im Frontend (`dateFallback.ts`, `ControlPanel.tsx`), sowie die Vorgabesprache des `terms_notice`-Textdatei-Downloads (`language: str = "de"` in `api/tiler.py` und `access/download.py`, Pflichtschlüssel `"de"` in `TermsOfUse.notice`) — alle jetzt Englisch als Vorgabe; Deutsch bleibt über `language=de` wählbar, wo ein Datensatz es führt (Sentinel-2 L2A tut das weiter). `adr/0004` §5.3 (Nachtrag 20.09.2026, dort noch "Deutsche Begriffe bleiben Sprache ... der Oberfläche") um einen zweiten Nachtrag ergänzt, der das aufhebt | fest | plans/m2-format-und-viewer.md M2-15; adr/0004 §5.3; frontend/src/dateFallback.ts, components/ControlPanel.tsx, coverage.ts; backend/earthx/api/tiler.py, access/download.py, catalog/registry.py |
| 2026-09-22 | **M2-09a umgesetzt: Zarr-Lesepfad gegen das synthetische Mini-Zarr.** `readers/zarr_reader.py` baut den in `adr/0007` §3.7 gemessenen Pfad: eigener `zarr.abc.store.Store` (`GatewayStore`), dessen **jeder** Byte-Read eine `Gateway`-Anfrage ist — kein `fsspec`, kein `obstore` —, darauf `zarr` → `xarray` → `rio_tiler.io.xarray.XarrayReader` für Kachel, Statistik und AOI-Zuschnitt. Umgesetzte Auflagen aus §12.11: Byte-Ranges (`Range`/`Offset`/`Suffix` und `get_partial_values`) zwingend, `zarr_format=3` beim Öffnen, nie `to_dataarray`/`to_array` (durch einen Test über den Modultext abgesichert). Der Store **listet nicht** (`supports_listing = False`, §3.10): konsolidierte Metadaten sind Pflicht, ihr Fehlen ist ein definierter Fehler (`StoreNotReadable`) statt eines Crawls. Vier Sitzungssetzungen, die nicht in `docs/` standen: **(1) Adressregel** `split_asset_href` — der Store ist das Pfadsegment auf `.zarr`, die Variable das letzte Segment, dazwischen liegt die Gruppe; getrennt nötig, weil die konsolidierten Metadaten an der Store-Wurzel liegen und nicht an jeder Gruppe (an `zarr` selbst gemessen). **(2) CRS-Reihenfolge:** der Store gewinnt, das Item (`proj:code`, ersatzweise `proj:epsg`) ist der Rückfall (§12.11 Punkt 3); fehlt beides, `MissingCrs` statt Raten. **(3)** Ein `Gateway` je Store, angelegt auf der Event-Loop, auf der `zarr` seine Store-Aufrufe fährt, und mit dem Store geschlossen. **(4)** Der Dispatch hängt an `format` der Registry und sitzt allein in `api/tiler.py`; `access` bekommt über `open_asset` den fertigen Pfad. `DataFormat.LEGACY` ergibt **501**, nicht stillschweigend COG; die vier definierten Reader-Fehler ergeben **502** (Quelle und Item widersprechen sich) und nennen Datensatz, Item und Asset, nie eine Adresse. Der Statistik-Cache aus `adr/0006` gilt jetzt für beide Formate. Neue Abhängigkeiten: `zarr>=3.1,<3.2` (letzte Fassung für Python 3.11, D17), `xarray`, `rioxarray`. Das Mini-Zarr wird im Test per Skript erzeugt (`tests/earthx/readers/mini_zarr.py`), keine Binärdatei im Repo | **fest** (Otto am 2026-09-22: Adressregel Option 1, so lassen). `adr/0007` §7 Punkt 7 stellt weiterhin ein Registry-Feld in Aussicht; M2-09b kann die Regel dadurch ersetzen, muss es aber nicht | adr/0007 §3.4, §3.7, §3.10, §6 Punkt 2 und 6, §7 Punkt 7, §12.11; plans/m2-format-und-viewer.md M2-09a, M2-09b; readers/zarr_reader.py; access/tiles.py; access/download.py; api/tiler.py; backend/requirements.txt |
| 2026-09-22 | **M2-09b: Plan angenommen, alle sieben Fragen wie empfohlen beantwortet** (`plans/m2-09b-zweiter-datensatz-katalog.md` §10, Otto). **F1** Schnitt in drei PRs (Katalog/Suche, Lesepfad, Stichprobe). **F2** Bandadressierung über ein Registry-Feld, Asset-Schlüssel als `SR_10m:b04` (Gruppe:Variable) an der Kachel-URL. **F3** Suche ohne `collections` bleibt mit `400` abgewiesen; **Zusatz von Otto:** die Meldung nennt jetzt die verfügbaren Collections, statt nur „nenne genau eine". **F4** Zehntes Feld `earthx:maturity` (`stable`/`staging`/`experimental`, ohne Vorgabewert) — Nachtrag in `architekturplan.md` 5.1 nachgezogen. **F5** Deckel der ausgewiesenen Stichprobe 500 Footprints (fünf Seiten à 100). **F6** Startwert des Streckbereichs `(0.0, 0.30)` je Band in `default_render`; **Zusatz von Otto:** der bestehende `/statistics`-Ablauf aus M2-07b (`store.ts::autoStretch`, `render.ts::autoRescale`) ist asset- und bandzahlunabhängig und braucht dafür keinen Sonderfall — geprüft, hält. **F7** Vorführung gegen die echte Quelle lokal bei Otto, die Sitzung belegt Erreichbarkeit über `curl` und die Logik über synthetische Fixtures (`adr/0002` T-D). Die beiden Befunde aus dem Plan-Schritt bleiben unverändert Grundlage der Umsetzung: das Asset zeigt auf eine Auflösungsgruppe, nicht auf eine Variable (F2 löst das); der zweite föderierte Datensatz macht den Mehrquellen-Zweig in `federating_client.py::_dispatch_search` erstmals erreichbar (F3 löst das) | fest | plans/m2-09b-zweiter-datensatz-katalog.md §10; architekturplan.md 5.1 (`earthx:maturity`); adr/0007 §7 Punkt 7, §12.11; plans/m2-format-und-viewer.md M2-09b; readers/zarr_reader.py; api/federating_client.py; frontend/src/store.ts, render.ts |
| 2026-09-22 | **M2-15 nachgeschärft: nur Englisch, keine Sprachwahl.** Otto: die `terms_notice`-Hinweisdatei des AOI-Zuschnitt-Downloads (M2-06) bietet keine Sprachauswahl an, nicht einmal mit englischem Standardwert. `DownloadRequest.language` (`api/tiler.py`) und der `language`-Parameter von `build_notice_text`/`build_download_zip` (`access/download.py`) entfallen ersatzlos; die Hinweisdatei ist immer der `en`-Text. `TermsOfUse.notice` (`catalog/registry.py`) lässt jetzt nur noch genau `{"en"}` als Schlüsselmenge zu, ein zweiter Sprachschlüssel ist ein `ConfigError`. Die deutsche Fassung des Sentinel-2-Legal-Notice-Texts ist aus `catalog/datasets.py` entfernt. `adr/0004` §5.3 um einen zweiten Nachtrag ergänzt | fest | plans/m2-format-und-viewer.md M2-15; adr/0004 §5.3; backend/earthx/api/tiler.py, access/download.py, catalog/registry.py, catalog/datasets.py |
| 2026-09-22 | **V-1 umgesetzt: Kartenbedienung, Globus und Theme.** Der `MapLibreMap`-Konstruktor bekommt `dragRotate: false`, `pitchWithRotate: false`, `touchPitch: false`, `maxPitch: 0`; zusätzlich `touchZoomRotate.disableRotation()` und `keyboard.disableRotation()`, damit weder Maus- noch Touch- noch Tastaturbedienung dreht oder kippt (D27) — Backend und Kachel-URLs unverändert. Globus über `map.setProjection({type: 'globe'|'mercator'})` (MapLibre GL JS 5.24, bereits im Frontend vorhanden, kein Versionssprung nötig); der Aufruf sitzt in `style.load` bzw. im Effekt, der auf den Store-Wert reagiert — direkt nach `new Map(...)` wirft MapLibre sonst „Style is not done loading.“ Theme-Umschalter setzt `data-theme` auf dem `.app`-Wurzelelement zwischen den beiden in `index.css` bereits vollständig vorbereiteten, aber bislang unerreichbaren Token-Sätzen `tech`/`normal` (Inventar N1) um; die Basiskarte bleibt in beiden Esri World Imagery (D10), kein neues Kartenbild. Beide Wahlen liegen neu in `store.ts` (`theme`, `projection`) und werden über `frontend/src/preferences.ts` in `localStorage` gehalten (Vorschlag, ungeprüft von Otto, aber ohne Alternativen in `docs/`); ein Fehlschlag dort (privates Fenster, voller Speicher) fällt lautlos auf den Vorgabewert zurück. Zwei Schaltflächen dafür in der oben-rechts-Zone neben „Zoom to selection“/„Layers“. Vitest deckt `preferences.ts` ab (Vorgabewert, Rundreise, Ausreißerwert, blockierter `localStorage`) | fest | plans/m2-format-und-viewer.md V-1, D27, D10; prototyp-inventar.md N1; frontend/src/preferences.ts, store.ts, App.tsx, components/MapView.tsx |
| 2026-09-22 | **M2-16 (neu, Stufe A) angelegt und nachgeschärft: Quicklook-Eckpunkte kommen aus der Kachel, nicht aus `proj:bbox`.** Der Befund war: der Quicklook einer Randszene liegt falsch, weil `quicklookCoords` die Eckpunkte aus der Datengeometrie ableitete, die bei Randszenen kleiner ist als die von Earth Search abgebildete MGRS-Kachel. Geprüft an `S2C_T32TNT_20260920T103025_L2A` (echte Anfrage gegen Earth Search): Der ursprünglich vorgesehene Weg — Eckpunkte aus `proj:bbox`/`proj:code` des Items — geht an der `sentinel-2-c1-l2a`-Collection vorbei, die item- und assetseitig kein `proj:bbox` setzt (STAC-Projection-Extension v1.1 führt es nur als optionales Feld), nur `proj:epsg`. Otto auf Rückfrage: Eckpunkte stattdessen aus `proj:transform`/`proj:shape` eines georeferenzierten Assets derselben Kachel (`visual`, nicht das Thumbnail selbst — das trägt keine eigene Projektion), CRS aus `proj:code`, ersatzweise `proj:epsg` (wie `tiler.py::_proj_code`); fehlen die nötigen Felder, kein Quicklook statt eines falsch platzierten. UTM→WGS84 über die neue Abhängigkeit `proj4`, auf `EPSG:326xx`/`327xx` beschränkt | fest (Otto am 2026-09-22, Option 1 der Rückfrage) | plans/m2-format-und-viewer.md M2-16; frontend/src/geoUtils.ts, types.ts, mapLayers.ts, store.ts; frontend/package.json |
| 2026-09-22 | **M2-08: Plan angenommen bis auf F2; fünf von sechs Fragen wie empfohlen beantwortet** (`plans/m2-08-onboarding-checkliste.md` §11, Otto). **F1** drei PRs (Punkte 1–8 / Punkt 9 / Punkt 10). **F3** der zweite Datensatz bekommt einen eigenen Live-Smoke-Job; ein roter Lauf färbt keinen PR rot, er lässt das Datum stehen. **F4** Punkt 9 führt die Suche über den Adapter und Kachel wie Download über die Routen, ohne Postgres. **F5** die Extents des ersten Eintrags kommen jetzt aus der Quelle. **F6** eine Logzeile genügt, ein ADR nur, falls F2 `gateway` eine zweite Policy gibt. **F2 bleibt offen** — siehe die folgende Zeile | fest | plans/m2-08-onboarding-checkliste.md; projektuebersicht §5 |
| 2026-09-22 | **Punkt 3 und die Extents von `sentinel-2-c1-l2a` aus der Quelle geschlossen** (M2-08-1). Die Earth-Search-Collection führt einen `cite-as`-Link auf `https://doi.org/10.5270/S2_-742ikth` (Copernicus Sentinel-2 MSI Level-2A), gelesen am 22.09.2026 über `curl`. `adr/0003` hatte Punkt 3 offen gelassen, weil M1 nichts aus der Quelle las. Der DOI benennt das ESA-Produkt, nicht Element 84s COG-Verteilung, und unterscheidet sich deshalb vom DOI des Zarr-Eintrags. Ebenfalls abgelesen und eingetragen: der Beginn des Zeitraums, `2015-06-27T10:25:31.456Z`, Ende offen; die Fläche meldet die Quelle selbst als ganze Welt und bleibt unverändert | fest (Sachstand) | catalog/datasets.py; adr/0003 §11; projektuebersicht §5 Punkt 3 |
| 2026-09-22 | **M2-08 F2, erster Vorschlag gefallen: ein Dauer-PR, den der Live-Smoke mit `GITHUB_TOKEN` aktuell hält, trägt nicht.** Belegt an GitHubs eigener Quelle (`github/docs`, `data/reusables/actions/actions-do-not-trigger-workflows.md`, Zweig `main`, 22.09.2026; `docs.github.com` ist aus der Sitzungsumgebung gesperrt): ein von `GITHUB_TOKEN` erzeugter oder aktualisierter PR erzeugt `pull_request`-Läufe nur im Zustand **approval-required**, die eine Person mit Schreibrecht je Aktualisierung freigeben muss, und ein reiner Push mit `GITHUB_TOKEN` löst gar keinen Workflow aus. Die Pflicht-Checks des geschützten `main` liefen also nie von selbst. Den von den Docs genannten Ausweg — App- oder Personal-Access-Token — schließt die Secret-Regel aus. Unabhängig davon müsste Otto den PR laufend mergen, sonst zeigt die Plattform ein altes Datum, das wie „geprüft" aussieht. Nebenbefund: die Collections liegen in pgstac und werden von `catalog-load` einmalig befüllt, ein täglich wechselnder Wert wird also nicht dadurch aktuell, dass er im Repo aktuell ist | fest (Sachstand) | plans/m2-08-onboarding-checkliste.md §4.4, §11 F2; .github/workflows/live-smoke.yml |
| 2026-09-22 | **M2-08 F2 entschieden, und D4 dabei enger gefasst: Punkt 10 heißt in M2 „vom T-D-Smoke abgedeckt", nicht „zuletzt geprüft am"** (Otto). Alle drei vorgeschlagenen Wege, den Zeitpunkt des letzten grünen Live-Smoke-Laufs ohne Secret und ohne laufende Handarbeit in die laufende Plattform zu bringen, sind verworfen — Statuszweig mit Laufzeit-Abruf, Statuszweig ohne Netz, Ausnahme im Branch-Schutz (Plan §11 F2). Stattdessen prüft der Checklisten-Test über den Marker `live_dataset` in `backend/tests_live/`, dass jeder Registry-Eintrag vom T-D-Smoke abgedeckt ist; ein **sichtbares Prüfdatum zeigt die Plattform in M2 gar nicht**, es kommt mit den eigenen Health-Checks in M5. `earthx:health.last_checked_ok` trägt heute das Datum des Onboarding-Checks und wird so benannt oder beschriftet, dass es nirgends als „zuletzt geprüft" gelesen werden kann (M2-08-3). Ersetzt die Fassung v1 von Punkt 10 aus D4 (20.09.2026) durch Fassung v1.1; die Fassung v1 von Punkt 9 bleibt unberührt | fest | projektuebersicht §5 Punkt 10; KLAERUNGEN B12; plans/m2-08-onboarding-checkliste.md §4.4, §11 F2 |
| 2026-09-22 | **Der Prototyp wird entfernt, vor M2-12** (M2-11, `adr/0008`, von Otto am 2026-09-22 angenommen). Geprüft: 17 der 21 Prototyp-Funktionen laufen token-frei in `earthx`, F2 (Ortssuche) und F11 (Mosaik im Kachel-Pfad) sind durch eigene Entscheidungen nach M3 bzw. M4 verschoben und blockieren nicht, F13–F16 sind bewusst anders oder ersatzlos gelöst; offen war allein F17. Tragender Befund: Seit M2-07a ist der Prototyp **nicht mehr vollständig lauffähig** — `frontend/vite.config.ts` proxyt nur noch `/stac`, `/coverage` und `/collections`, README-Abschnitt 3 beschreibt einen Start, den es nicht mehr gibt; die Referenz hängt am Tag `prototype-biomass`, nicht am Arbeitsbaum. **Entschieden:** (1) Rechenkern und Warp-Regel von `decomp.py` ziehen **unverändert** und ohne jede Verallgemeinerung nach `backend/earthx/datasets/quad_pol_reference/` — neutral nach der Fähigkeit benannt, **ohne Registry-Eintrag** —, mit synthetischen T-A-Tests; `decompose_crop` zieht nicht mit (hängt an `auth` und `store`, ausgeschlossen durch ENTSCHEIDUNGEN §3 und KLAERUNGEN B9). (2) Danach fällt `backend/app/` (1703 Zeilen) samt `build_coverage.py`, den zwei Prototyp-Testdateien, `environment.yml`, README-Abschnitt 3, `COPY app` im Dockerfile, den zwei `pyproject`-Einträgen und `pystac-client` aus `requirements.txt`. (3) Ein PR, getrennte Commits: erst Umzug, dann Entfernen | fest (Otto am 2026-09-22, alle sechs Fragen aus §9) | adr/0008; plans/m2-format-und-viewer.md M2-11, D7; ENTSCHEIDUNGEN §3; architekturplan 13 |
| 2026-09-22 | **Die befristete Ausnahme für das fest eingetragene OIDC-Client-Secret endet mit dem Entfernen des Prototyps** (Folge der Zeile darüber, `adr/0008` §9 Frage 4). Die Absätze dazu in `SECURITY.md` und `CLAUDE.md` werden im selben PR entfernt; die Änderung beider Dateien ist dafür **ausdrücklich erlaubt**. Unverändert bleibt, was schon galt: Die Git-History wird nicht bereinigt, der Wert nicht widerrufen und nirgends wiedergegeben — auch nicht in Commit, PR oder Log | fest (Otto am 2026-09-22) | adr/0008 §9 Frage 4; SECURITY.md; CLAUDE.md; backend/app/config.py |
| 2026-09-22 | **M2-10 freigegeben, mit zwei Zusätzen** (Otto, 2026-09-22, Plan `plans/m2-10-zweiter-datensatz-viewer.md` §10). Angenommen F1 (a) bis F6 (a): die freigegebenen Zoomstufen stehen als `min_zoom`/`max_zoom` in `earthx:viewer`; der erste Datensatz behält `0..19`; der Quicklook-Ersatz ist eine Rasterkachel auf der gröbsten freigegebenen Stufe, ohne neuen Endpunkt; die Trefferliste behält den Platzhalter mit Begründung; die Checkliste v1 **als Test** bleibt M2-08, M2-10 führt die Kette nur vor; der ZIP-Eintragsname des Zuschnitts wird in M2-10 bereinigt. **Zusatz 1 (zu F1):** Der Tiler setzt die Stufen ebenfalls durch und antwortet außerhalb davon mit einem definierten Fehler, belegt mit Test — sonst kann jeder Client teure Zarr-Kacheln weit über z14 anfordern. **Zusatz 2 (zu F2):** `0..19` steht ausdrücklich im Registry-Eintrag von Sentinel-2, nicht als Vorgabewert im Code (KLAERUNGEN B10) | fest | plans/m2-10-zweiter-datensatz-viewer.md; plans/m2-format-und-viewer.md M2-10; architekturplan 5.1; adr/0007 §12.10 |
| 2026-09-22 | **M2-10 umgesetzt** (#61). `earthx:viewer` trägt `min_zoom`/`max_zoom` ohne Vorgabewert — `sentinel-2-c1-l2a` `0..19` (unverändertes Verhalten, jetzt im Eintrag statt im Client), `sentinel-2-l2a-zarr3` `8..14` (D23/F9); `architekturplan.md` 5.1 ist entsprechend nachgezogen. Die Kachelroute weist eine Stufe außerhalb der Spanne mit `400` ab, **bevor** sie das Item holt — dass kein Request nach außen geht, deckt ein Test ab, die Latenz ist im PR per `curl` belegt, ein Eintrag ohne `viewer` mit `501`. Der fehlende Quicklook des zweiten Datensatzes wird im Browse-Modus durch Kacheln auf der gröbsten freigegebenen Stufe ersetzt — kein neuer Endpunkt und kein weiteres Registry-Feld, weil die gröbste freigegebene Stufe die Vorschau *ist*. Der Reifegrad (`earthx:maturity`) steht jetzt in der Oberfläche (D23-Auflage); das Prüfdatum bleibt weg, siehe die Zeile weiter unten | fest | plans/m2-10-zweiter-datensatz-viewer.md; architekturplan 5.1; catalog/registry.py; api/tiler.py; frontend/src/datasets.ts |
| 2026-09-22 | **Zwei Nebenbefunde aus dem ersten Zuschnitt über das zweite Format** (M2-10, #61), beide behoben: der ZIP-Eintrag hieß roh nach dem Asset-Schlüssel, was bei Zarr `SR_10m:b04,b03,b02.tif` ergibt — unter Windows kein zulässiger Dateiname; er wird jetzt bereinigt und die Hinweisdatei nennt den ursprünglichen Schlüssel. Und `crop_asset` fing nur die Bereichsfehler von rio-tiler ab, während rioxarray `NoDataInBounds` wirft: eine AOI neben der Szene ergab bei Zarr `500`, wo dieselbe AOI bei COG `400` ergibt | fest | access/download.py; tests/earthx/access/test_download_zarr.py |
| 2026-09-22 | **Kein Prüfdatum im Viewer, bis es eines gibt** (Otto, 22.09.2026, nach dem Befund von M2-08 in #62). `earthx:health.last_checked_ok` trägt heute das Datum der *Aufnahme* des Datensatzes, nicht das einer Prüfung; die in M2-10 gebaute Zeile „source checked OK on …" hätte also behauptet, was die Plattform nicht weiß, und ist wieder entfernt. Punkt 10 der Onboarding-Checkliste („zuletzt erfolgreich geprüft ist gesetzt **und sichtbar**") bleibt damit offen statt scheinbar erfüllt; ein echtes Prüfdatum kommt mit den Health-Checks in M5. Der Hinweis auf „staging" (D23) und der auf die Zoom-Untergrenze bleiben | fest | plans/m2-10-zweiter-datensatz-viewer.md §3.3, §4.4; projektuebersicht §5 Punkt 10; frontend/src/components/ControlPanel.tsx |
| 2026-09-22 | **Review-Befunde zu M2-10 eingearbeitet** (#61). Wesentlich: die Vorschau-Kachel trug den Streckbereich aus `earthx:default_render` nicht mit — kein Fehler, aber rio-tiler fällt dann auf die Typgrenzen zurück und rendert ausgewaschen (gemessen 127..255 statt 0..255), Vorschau und Vollauflösung derselben Szene sähen verschieden aus; die Übersetzung `DefaultRender` → Kachelparameter liegt jetzt einmal in `render.ts::appliedRenderFrom` und wird von beiden Wegen benutzt. Dazu zwei Löcher im Zoom-Riegel geschlossen: `supported_tms` steht auf `WebMercatorQuad` (in einem anderen Raster bedeutet dieselbe Stufennummer eine andere Auflösung), und `/preview` ist nicht mehr registriert (trug keine Stufe und rechnete keine Zielauflösung, las bei Zarr also die native). Ferner: `assets` im Download wird dedupliziert, `zoomRangeOf` spiegelt die Registry-Obergrenze 22 | fest | plans/m2-10-zweiter-datensatz-viewer.md §4.1b, §4.3; access/tiles.py; frontend/src/render.ts |
| 2026-09-22 | **Offen: `/tilejson.json` weist die Zoomstufen seines Readers aus, nicht die freigegebenen** (Review zu M2-10). Ein Client, der dem TileJSON folgt statt Kachel-URLs selbst zu bauen, wird auf Stufen geschickt, die der Riegel mit `400` abweist — also genau der fremde Client, für den der Riegel gedacht ist. `minzoom`/`maxzoom` der rio-tiler-Reader sind berechnete Eigenschaften und nicht setzbar; es bliebe, TiTilers Route nachzubauen. Eigene kleine Aufgabe, nicht Teil von M2-10 | offen | plans/m2-10-zweiter-datensatz-viewer.md §4.1b; access/tiles.py |
| 2026-09-22 | **Die Checklisten-Kette von M2-08 nimmt ihre Kachel aus der freigegebenen Spanne** (Zusammenführung von #62 und #61). `synthetic_chain._covering_tile` wählte die erste Zoomstufe, deren Kachel in den synthetischen Bestand passt — für `sentinel-2-l2a-zarr3` z16, also außerhalb der freigegebenen `8..14`, was der Riegel aus M2-10 seither mit `400` abweist. Die Kette nimmt jetzt die feinste **freigegebene** Stufe, was ein Client auch tut; eine Kachel, die den Mittelpunkt enthält, schneidet den Bestand immer | fest | tests/catalog/synthetic_chain.py; plans/m2-10-zweiter-datensatz-viewer.md §4.1b |
| 2026-09-22 | **Der Zoom-Untergrenzen-Hinweis stand an der falschen Stelle** (M2-10, von Otto lokal gefunden). Er hing im `ControlPanel`, und `runSearch` setzt `panelCollapsed: true` — die Tafel ist also genau dann weggeschoben, wenn es Szenen gibt, die unterhalb von `min_zoom` verschwinden. Verdeckt hat ihn kein React-Zustand, sondern ein `transform` im CSS, weshalb ihn weder Typprüfung noch ein Test der Regel fangen konnte. Er steht jetzt in der `StatusBar` (die einzige immer sichtbare Auflage) und hängt an nichts als am gewählten Datensatz und am Zoom; die Regel liegt rein als `datasets.ts::zoomFloorHint` daneben. Neu dazu: ein Render-Test, der `App` mountet und prüft, dass der Hinweis nicht innerhalb der eingeklappten Tafel liegt, wofür `jsdom` als Dev-Abhängigkeit hinzukommt und `vite.config.ts` auch `.tsx`-Tests einsammelt — zustand gibt einem statischen Render seinen Anfangszustand, ein Render ohne DOM hätte also nichts gezeigt | fest | frontend/src/components/StatusBar.tsx; frontend/src/datasets.ts; frontend/vite.config.ts; plans/m2-10-zweiter-datensatz-viewer.md §4.4 |
| 2026-09-22 | **Review-Befunde zum Zoom-Hinweis eingearbeitet** (M2-10). Der Render-Test belegte nur „der Hinweis steht in der StatusBar", nicht „er ist sichtbar" — er mountet jetzt `App` und prüft, dass der Hinweis nicht innerhalb der eingeklappten `.panel-dock` liegt; Gegenprobe gemacht. Die Bindung an Suchtreffer ist wieder raus, weil sie den Hinweis genau beim Datensatzwechsel (leert die Treffer) und bei angehefteten Ebenen abschaltete. Der stehende Hinweis nimmt keine Klicks mehr: `.status-stack` kreuzt bei üblichen Fensterbreiten die Schaltflächen oben rechts, bisher folgenlos, weil alle Toasts flüchtig waren — jetzt trägt der Stapel `pointer-events: none` und jeder Toast außer dem Hinweis holt sie sich zurück. Dazu: `IS_REACT_ACT_ENVIRONMENT` gesetzt, `createRoot` wird abgeräumt, `zoomFloorHint` weist nicht-endliche Zoomwerte ab, und Kommentar wie Plan nennen die Grenzen der Regel (gilt für den Kachelweg, folgt `moveend`) | fest | frontend/src/components/StatusBar.tsx, StatusBar.test.tsx; frontend/src/datasets.ts; frontend/src/index.css |
| 2026-09-23 | **M2-08-3 umgesetzt: Punkt 10 (Fassung v1.1) scharf gestellt, `earthx:health.last_checked_ok` gestrichen.** Jedes `tests_live`-Modul trägt jetzt `@pytest.mark.live_dataset("<id>")`; der Checklisten-Test liest den Marker über `ast` (kein Import, kein Netz) und lässt einen Registry-Eintrag ohne Abdeckung fallen — Punkt 10 ist damit `CHECKED_ELSEWHERE` wie Punkt 9, `NOT_YET_CHECKED` ist leer. Der zweite Datensatz bekommt einen eigenen Job `eopf` in `live-smoke.yml` (F3), mit zwei Metadaten-Anfragen gegen `stac.core.eopf.eodc.eu` nach demselben Muster wie der erste; Suchfenster und -box sind am 23.09.2026 direkt per `curl` gemessen (`gateway` bleibt einer Cloud-Sitzung verschlossen, adr/0002 Nachtrag M2-13), nicht den erfundenen Werten der M2-09b-Fixtures entnommen. Otto gibt vorab frei: `HealthInfo.last_checked_ok` entfällt ersatzlos statt umbenannt zu werden — die Angabe war ohnehin nur das Onboarding-Datum, nicht die eines Health-Checks, und steckt für Punkt 6 bereits in `access.token_free_checked_at`. `earthx:health` führt jetzt nur noch `status`; `architekturplan.md` 5.1 entsprechend nachgezogen. Ein echtes Prüfdatum bleibt M5 vorbehalten | fest (Otto, vorab freigegeben) | plans/m2-08-onboarding-checkliste.md §4.4, §7, §11 F2/F3; projektuebersicht §5 Punkt 10; KLAERUNGEN B12; architekturplan.md 5.1; backend/tests_live/; .github/workflows/live-smoke.yml; catalog/registry.py, datasets.py, collection.py |
| 2026-09-23 | **V-2, Punkt 5: „Add to layers" zieht in die Auswahl-Leiste, nicht aus `ViewerControls`** (Otto, im Plan-Schritt gefragt). „Add to layers" und „View full resolution" erschienen im Code nie in derselben Box — sie sind unabhängig sichtbar in `ViewBar` (Browse, bei Auswahl), `TimeSlider` (Browse) und `ViewerControls` (Volltauflösung). Der eigentliche Befund: `store.ts::addCurrentToLayers` unterstützt das Anpinnen eines Quicklooks im Browse-Modus bereits seit M2-10, war dafür aber nur über den kleinen Link im `TimeSlider` erreichbar, nicht direkt neben „View full resolution". Otto entscheidet sich für die Empfehlung: „Add to layers" zusätzlich in `ViewBar` neben „View full resolution", der Link im `TimeSlider` bleibt | fest (Otto, V-2) | plans/m2-format-und-viewer.md V-2; frontend/src/components/ViewBar.tsx |
| 2026-09-23 | **V-2 Punkt 1 korrigiert: der Theme-/Projektions-Knopf nennt das Wechselziel, nicht den aktuellen Zustand** (Otto, 23.09.2026). Der erste Fix ging von der Lesart „Zustand" aus und wurde zurückgewiesen — gemeint war von Anfang an das Gegenteil: im Dunkelmodus soll „Light" stehen, im Globus-Modus „Mercator". Umgesetzt in `App.tsx`, Vitest-Fälle in `AppTopBar.test.tsx` entsprechend gedreht | fest (Otto) | plans/m2-format-und-viewer.md V-2; frontend/src/App.tsx |
| 2026-09-23 | **V-2 Punkt 4 nachgebessert: Kalendersymbol im Dunkelmodus war mit reinem `filter: invert(1)` noch zu blass/nicht erkennbar** (Otto, 23.09.2026, per Screenshot). `filter` um `brightness(1.5)` ergänzt, damit das Symbol reinweiß statt nur hellgrau invertiert wird | fest (Otto) | frontend/src/index.css |
| 2026-09-23 | **V-2 Punkt 4, zweite Nachbesserung: Symbol war auch mit `invert(1) brightness(1.5)` noch schwarz** (Otto, 23.09.2026). `filter` auf `::-webkit-calendar-picker-indicator` und `color-scheme` hängen davon ab, dass die Engine sie tatsächlich umsetzt — offenbar nicht zuverlässig überall. Ersetzt durch ein eigenes weißes Kalender-Symbol als `background-image` des Felds (eine gewöhnliche CSS-Eigenschaft, motorunabhängig); das native Symbol bleibt für den Klick zuständig, ist aber jetzt `opacity: 0` | fest (Otto) | frontend/src/index.css |
| 2026-09-22 | **Trefferliste gruppiert nach Überflug.** Die sichtbare Gruppierung im Viewer erfolgt nach Tag und `s2:datatake_id`, damit Kacheln desselben Überflugs beisammenstehen. Sie ist reine Darstellung: Szenen bleiben einzeln, jede mit eigenem Quicklook und eigener Kachel-URL. D19 und D11 bleiben unberührt, es entsteht kein Mosaik | fest | plans/m2-format-und-viewer.md V-4, D19 |
| 2026-09-23 | **V-4 nachgebessert (Ottos Durchsicht des offenen PR), vier Befunde behoben.** ESRI-Attribution im Dunkelmodus unlesbar: `.maplibregl-compact` gewinnt sonst mit `color: #000`, jetzt für alle Attributions-Zustände auf Weiß gesetzt. Kalender im Dunkelmodus nicht klickbar: `padding-right` verschob den unsichtbaren nativen Indikator vom gezeichneten Symbol weg, ersetzt durch einen echten Knopf, der `showPicker()` aufruft. Trefferliste war frei verschiebbar statt fest angedockt: jetzt rechts fixiert und ein-/ausklappbar wie das Suchmenü links, gespiegelt. Layer-Manager sprang nach Schließen/Wiederöffnen auf die ursprüngliche Position zurück, weil `if (!open) return null` `Draggable` jedes Mal neu montierte und dessen Verschiebe-Zustand verwarf; `Draggable` bleibt jetzt montiert, nur per CSS versteckt | fest | plans/m2-format-und-viewer.md V-4; frontend/src/index.css; frontend/src/components/ControlPanel.tsx, LayerManager.tsx, App.tsx |
| 2026-09-23 | **V-6: fünf weitere Befunde aus Ottos Durchsicht des offenen PR behoben.** Globus-Hintergrund war trotz der V-4-Korrektur weiterhin weiß, weil MapLibre den Canvas jeden Frame transparent leert und der Sky-Pass das nicht in jedem Fall vollständig abdeckt — `body { background: #04070a }` als tatsächliche Garantie, die Sky-Farben bleiben als Ergänzung. Datumsfelder zu breit, kein Platz für den Pfeil: mittlere Spalte jetzt echte Grid-Spur (`auto`) statt Overlay, Felder dadurch etwas schmaler als die Kachelpaare darüber. Kopiersymbol durch dasselbe Icon wie in Claudes eigener Oberfläche ersetzt (zwei überlappende Rechtecke, Inline-SVG). Download jetzt auch für nur als Quicklook angepinnte Layer im Layer-Manager möglich: `LayerRestore.itemIds` hält die gepinnten Szenen fest (nicht mehr von den aktuellen Suchergebnissen abhängig), `downloadRequestFor` weicht ohne Volltauflösung auf das Standard-Render-Asset der Registry aus. Trefferliste: eine aufgeklappte Gruppe lässt sich jetzt einklappen, ohne eine andere zu öffnen — eigener lokaler Zustand in `ResultsPanel`, synchronisiert mit `activeGroupIndex`, das weiterhin Karte und Zeitschieber steuert | fest | plans/m2-format-und-viewer.md V-4; frontend/src/index.css, mapStyles.ts, download.ts, layers.ts, store.ts, components/ResultsPanel.tsx, LayerManager.tsx, DownloadDialog.tsx |
| 2026-09-23 | **V-7: drei weitere Befunde aus Ottos Durchsicht des offenen PR behoben.** ESRI-Info-Knopf stand standardmäßig aufgeklappt, weil `compact: true` MapLibre nur einklappbar macht und der erste Zustand trotzdem offen ist (Einklappen hängt an einem `drag`-Listener) — jetzt direkt nach Kartenerzeugung eingeklappt. Layer-Manager stand standardmäßig bei `left: 420px`, jetzt `left: 16px` wie das Suchmenü. Abgeschnittene Namen: der Layer-Namensknopf trug als `title` nur einen festen Bedienhinweis statt des (oft abgeschnittenen) Namens, jetzt beides; die Gruppen-Überschrift der Trefferliste bekam denselben Hover-Titel, die einzelne Szenen-ID hatte ihn schon | fest | plans/m2-format-und-viewer.md V-4; frontend/src/components/MapView.tsx, LayerManager.tsx, ResultsPanel.tsx; frontend/src/index.css |
| 2026-09-23 | **V-8: zwei Befunde aus Ottos Durchsicht per Screenshot behoben, einer davon eine Korrektur von V-7.** Die Datumsfelder liefen trotz V-6-Korrektur weiterhin über den Panelrand hinaus (rechtes Kalendersymbol am Rand abgeschnitten): ein bloßes `1fr` in einem Grid schrumpft nie unter die Content-Mindestbreite, die durch das reservierte Icon-Padding größer war als der verfügbare Platz — jetzt `minmax(0, 1fr)` plus `min-width: 0` auf Feld/Input. Der Layer-Manager sollte bei offener Steuerungstafel rechts daneben ausweichen statt darunterzuliegen und beim Einklappen zurück an den linken Rand wandern; V-7 hatte ihn nur pauschal auf `left: 16px` gesetzt, ohne auf `panelCollapsed` zu reagieren — jetzt `overlay layermgr` mit `controls-open`-Klasse aus demselben `panelCollapsed`, `left: 420px` offen / `left: 16px` eingeklappt, mit Übergang. Mit Playwright gegen den lokalen Dev-Server geprüft (Screenshot beider Zustände) | fest | plans/m2-format-und-viewer.md V-4; frontend/src/index.css, App.tsx |
| 2026-09-23 | **V-9: Play-Knopf der Zeitleiste lief rückwärts.** `groups` ist neueste-zuerst, die Zeitleiste zeigt älteste links, neueste rechts; das Play-Intervall zählte `activeGroupIndex` hoch statt runter und lief damit visuell von rechts nach links (neueste zu älteste), entgegen der eigenen Beschriftung. Jetzt Herunterzählen mit Wraparound: läuft links nach rechts ab der aktuellen Reglerposition, und von der Standardposition ganz rechts (neueste Szene) beginnt der erste Schritt automatisch bei der ältesten (ganz links) | fest | plans/m2-format-und-viewer.md V-4; frontend/src/components/TimeSlider.tsx |
| 2026-09-23 | **V-10: Layer-Manager weicht nur bei echter Überlappung aus und verlässt nie den Bildschirm.** Die Runde-4-Lösung (`.controls-open`-CSS-Klasse) hing rein an `panelCollapsed`, nicht an tatsächlicher Kollision, und verschob den Layer-Manager auch dann, wenn die Steuerungstafel gar nicht im Weg war; auch der Bildschirmrand war nicht geschützt. Ersetzt durch zwei generische, opt-in Fähigkeiten in `Draggable.tsx`: `avoidSelector` (Kollisionstest per `getBoundingClientRect` gegen das benannte Element, nur an der unberührten Ankerposition, manuelle Verschiebung gewinnt immer; neu geprüft bei `transitionend` des Ziels und über ein vom Aufrufer gesetztes `recalcTrigger`, nötig weil das Öffnen des Layer-Managers selbst sein Anker-Rechteck ändert, ohne dass ein DOM-Ereignis dafür feuert) und eine bedingungslose Bildschirmrand-Klemme für Ausweichung wie manuelles Ziehen. `LayerManager.tsx` nutzt `avoidSelector=".panel-dock"`. Acht Szenarien mit Playwright gegen den lokalen Dev-Server verifiziert | fest | plans/m2-format-und-viewer.md V-4; frontend/src/components/Draggable.tsx, LayerManager.tsx, App.tsx; frontend/src/index.css |
| 2026-09-23 | **V-11: Quicklooks blieben trotz eingeklappter Trefferliste auf der Karte sichtbar.** `expandedGroupIndex` (V-6) lebte nur lokal in `ResultsPanel.tsx`; `MapView.tsx` las weiterhin `activeGroupIndex`, das ein manuelles Einklappen absichtlich nicht ändert. `expandedGroupIndex` zieht in den Store: `setActiveGroupIndex` hält es weiterhin mit `activeGroupIndex` synchron (Zeitschieber, „Play", Klick, Layer-Auswahl), die neue Aktion `toggleResultsGroup` ist der einzige Pfad, der nur `expandedGroupIndex` ändert. `grouping.ts::itemsForMap` nimmt jetzt `number | null` (kollabiert = nur Auswahl sichtbar, wie ein außerhalb liegender Index). `MapView.tsx` wählt je Aufrufstelle zwischen `activeGroupIndex` (Fokusmodus, Liste nicht sichtbar) und `expandedGroupIndex` (Browse-Modus) | fest | plans/m2-format-und-viewer.md V-4; frontend/src/store.ts, grouping.ts, components/ResultsPanel.tsx, MapView.tsx |
| 2026-09-23 | **V-12: Layer-Manager-Ausweichen misst jetzt die tatsächliche (ggf. verschobene) Position und gleitet statt zu springen.** Otto fand zwei Lücken in der V-10-Lösung: Die Überlappungsprüfung maß nur den festen CSS-Anker, nie eine manuelle Verschiebung, und griff nach der ersten Verschiebung gar nicht mehr; das Ausweichen selbst geschah sprunghaft. `Draggable.tsx`: Ruhe-Rechteck jetzt aus Anker plus aktuellem `off`, ohne Sonderfall; `off` bleibt von der Ausweich-Logik unangetastet, sodass das Panel immer an seine zuletzt eingenommene Stelle zurückgleitet, sobald nichts mehr im Weg ist; neu geprüft auch beim Loslassen der Maustaste; ein Greifen mitten im Ausweichen faltet den Versatz in `off`; CSS-Übergang (außerhalb aktiven Ziehens) animiert Ausweichen und Rücknahme. Mit Playwright verifiziert (Ziehen auf die Steuerungstafel, gleitendes Ausweichen mit acht Zwischenwerten, Rückkehr an die exakte Ablagestelle nach dem Einklappen) | fest | plans/m2-format-und-viewer.md V-4; frontend/src/components/Draggable.tsx |
| 2026-09-23 | **M2-17: Szene per Namen finden — Ottos Entscheidung.** Alle fünf Empfehlungen angenommen: direkter Item-Abruf statt `ids` in der Suche; Suche nur im gewählten Datensatz, die Meldung bei unbekanntem Namen nennt ausdrücklich den Datensatz (die beiden Kataloge vergeben verschiedene Namen für dieselbe Szene); eigenes Feld „Scene name“ mit eigenem Knopf; bei unbekanntem Namen nur eine Meldung, Trefferliste/AOI/Zeitraum bleiben unverändert; `ids`/`intersects` werden im föderierten Pfad jetzt (nicht erst später) mit `400` abgewiesen, Durchreichen bleibt eine eigene Aufgabe; der Nebenbefund `500` statt `502` beim Einzelabruf wird mitgenommen | fest | plans/m2-format-und-viewer.md M2-17 |
| 2026-09-23 | **M2-17b: Bedienung der Szenensuche nach Ottos Ausprobieren geändert.** Das Feld „Scene name“ steht jetzt ganz oben im Suchmenü, über den AOI-Werkzeugen (Point/Rectangle/…), statt unter der Datensatz-Wahl; es hat keinen eigenen Knopf mehr. Der bisherige „Search scenes“-Knopf heißt jetzt „Search“ und übernimmt beide Wege: mit Szenennamen `findSceneByName`, sonst `runSearch` wie bisher; ein Name geht vor und verlangt weder AOI noch Zeitraum. `findSceneByName` selbst ist unverändert | fest | plans/m2-format-und-viewer.md M2-17; frontend/src/components/ControlPanel.tsx |
| 2026-09-22 | **Trefferliste gruppiert nach Überflug.** Die sichtbare Gruppierung im Viewer erfolgt nach Tag und `s2:datatake_id`, damit Kacheln desselben Überflugs beisammenstehen. Reine Darstellung: Szenen bleiben einzeln, jede mit eigenem Quicklook und eigener Kachel-URL. D19 und D11 bleiben unberührt, es entsteht kein Mosaik | Dublette der Zeile mit demselben Inhalt | plans/m2-format-und-viewer.md V-4, D30 |
| 2026-09-23 | **M3-Vorentscheidung 1, Nicht-STAC-Quelle:** Die Wahl fällt nach einem Spike der Stufe C (Opus, Effort hoch). Kandidaten: Hansen GFC, Copernicus DEM direkt aus dem Bucket, ein Zenodo-Record, ARCO-ERA5 (`adr/0007` §4). Kriterien: keine Such-API, token-frei, Lizenz nach B11 einstufbar, `readers` unverändert (echte COGs bzw. vorhandener Zarr-Pfad), Art der Coverage. Die Auswahl entscheidet Otto nach dem ADR | fest (Spike offen) | projektplan M3; architekturplan 5.2, 6.1, 15.1 |
| 2026-09-23 | **M3-Vorentscheidung 2, Copernicus DEM:** Der Spike aus Vorentscheidung 1 klärt, ob der DEM-Bucket als Nicht-STAC-Quelle zählt. Wenn ja, kann DEM zugleich dritter Datensatz und Nicht-STAC-Quelle sein; wenn nein, wird DEM vierter Datensatz. Präzisiert `adr/0003` ("Copernicus DEM als dritter") | fest | adr/0003 §6; projektplan M3 |
| 2026-09-23 | **M3-Vorentscheidung 3, Allowlist:** Der Spike-Prompt nennt die Hosts aller Kandidaten; Otto trägt sie vor dem Start der Session in die Cloud-Umgebung ein (Freigaben wirken nur in neu gestarteten Sessions) | fest | cloud-umgebung §6 |
| 2026-09-23 | **M3-Vorentscheidung 4, Materialisierung:** Ablage der materialisierten Items (pgstac oder stac-geoparquet) und ausführender Ort (Loader in `catalog` oder Prozess `harvester`) werden im Spike aus Vorentscheidung 1 mit Stand der Technik und Quellen vorgeschlagen und dann von Otto entschieden | fest (Spike offen) | architekturplan 5.2, 3.2 |
| 2026-09-23 | **Quellenbreite statt ESA-only.** Schließt diesen Teil der offenen Zeile "ESA-only oder Quellenbreite; Registrierungspflicht; NC hinter Bezahlschranke; Job-Queue; Cloud-Anbieter"; die übrigen Teile bleiben offen | fest | projektuebersicht Prinzip 3; projektplan 10 |
| 2026-09-23 | **Suche in M3:** gemischte Suche über eigene und föderierte Collections (D8, `adr/0005` Regel I), erneute Prüfung von CQL2/`filter` (`adr/0005` Regel VI) und Durchreichen von `intersects` und `ids` (D31) gehören zu M3. `intersects` ist nötig, damit Polygon-AOIs aus Upload und Ortssuche genau statt über die bbox suchen | fest | adr/0005; plans/m2-format-und-viewer.md D8, D31 |
| 2026-09-23 | **AOI-Upload über das Backend** mit einheitlicher Prüfung, zusätzlich Shapefile; Einzelheiten im Plan-Schritt der Aufgabe | fest | projektplan M3; plans/m2-format-und-viewer.md §2 |
| 2026-09-23 | **Ortssuche (F2):** Nominatim über `api` und `gateway`, mit Cache, höchstens 1 Anfrage pro Sekunde, ohne Autocomplete; vorher eine kurze Recherche zu Nutzungsbedingungen und Alternativen. Die Klärung der Nominatim-Bedingungen vor dem ersten öffentlichen Deployment bleibt offen | fest | prototyp-inventar F2; adr/0008 §11 Punkt 3 |
| 2026-09-23 | **Datensatz-Katalog in der Suchkachel:** in M3 ein einfacher Filter über `/stac/collections` statt fester Blöcke; die Hybrid-Suche bleibt M5 | fest | plans/m2-format-und-viewer.md §3 (Nach M2 vorgemerkt); projektplan M5 |
| 2026-09-23 | **Datensatzspezifische Sonderfälle im Frontend** (Gruppierung der Trefferliste nach `s2:datatake_id` aus D30, Nodata-Schwelle 16 der Quicklooks, feste Blöcke der Suchkachel) werden in M3 zu Registry-Feldern, soweit der dritte Datensatz sie trifft. Grund: M3-Abnahme "ohne Sonderfälle im Frontend". Gibt der offenen Zeile zur Nodata-Schwelle die Richtung | fest | projektplan M3; catalog/registry.py (`ViewerInfo`); plans/m2-format-und-viewer.md D30 |
| 2026-09-23 | **Mosaik im Kachel-Pfad bleibt M4.** `adr/0006` §6 ("Mosaik-Kacheln wandern nach M3") und "Nach M2 vorgemerkt" im M2-Plan werden daran angeglichen | fest | adr/0006 §6; adr/0008 §3; plans/m2-format-und-viewer.md D11 |
| 2026-09-23 | **Zuschnitt auch auf der Karte (Otto):** In der Trefferliste bleiben die Quicklooks ganz sichtbar wie heute. Sobald der Nutzer in die volle Auflösung wechselt, zeigt die Karte von den gewählten Szenen **nur noch den Teil innerhalb der AOI**; außerhalb ist das Bild ausgeblendet, die Basiskarte bleibt. Ein Knopf wie bisher: mit AOI schneidet er zu, ohne AOI (Szenensuche per Namen, M2-17) zeigt er die ganze Szene; die Beschriftung klärt der Plan-Schritt (Oberfläche englisch). Der Download bleibt wie seit M2-06/M2-07d (auf die AOI zugeschnitten, mehrere Szenen per `mosaic_reader` zu einer Datei). Den technischen Weg schlägt eine M3-Aufgabe der Stufe B im Plan-Schritt mit Stand der Technik vor. Randbedingungen: keine exakte AOI in Kachel-URLs oder Logs, zustandslos (kein Zwischenspeicher des Zuschnitts, adr/0001), Mosaik im Kachel-Pfad bleibt M4 (D11), keine datensatzspezifischen Sonderfälle im Frontend | fest (Verhalten); Knopf Vorschlag; Weg offen bis zum Plan-Schritt | plans/m2-format-und-viewer.md §3 (Nach M2 vorgemerkt), M2-06, M2-17, D11; prototyp-inventar F8, F10, F11; adr/0001 |
| 2026-09-23 | **Heatmap-Zählwürfel (D26):** In M3 nur ein Mess-Spike der Stufe C, parallel zum Rest; gebaut wird später, nach eigener Entscheidung | fest | adr/0004; plans/m2-format-und-viewer.md D26 |
| 2026-09-23 | **Health-Status je Quelle kommt mit M5**, nicht mit M3; `projektplan.md` M3 wird angepasst | fest | projektplan M3, §5; D29 |
| 2026-09-23 | **Interface-Reflexion** als ADR der Stufe C nach dem Merge der dritten Quelle, mit Opus (Effort hoch); Fable nur, wenn Otto die Session selbst interaktiv führt | fest | projektplan M3, 3.2, 8 |
| 2026-09-23 | **Kleinaufgaben in M3:** Fix der Zoomstufen in `/tilejson.json` als Stufe A; Wechsel auf Python 3.12 als frühe eigene Aufgabe der Stufe B, bevor neue Abhängigkeiten landen | fest | access/tiles.py; adr/0007 F5; .github/workflows/ci.yml |
| 2026-09-23 | **M3-00 Doku (Stufe A):** `projektplan.md` nachziehen (M2-Status, §10, M3-Zeile), die Widersprüche aus der M3-Vorbereitung bereinigen, die Log-Zeilen zu Docker-Allowlist, `Settings.host_allowed` und zum Nachziehen des M2-Plans schließen, eine Zeile "M2 abgenommen" ergänzen | fest | projektplan; ENTSCHEIDUNGSLOG; adr/0006 |
| 2026-09-23 | **Architektur-Konformitätsbericht:** einmal zu Beginn von M3 als Aufgabe der Stufe C, vorerst keine Routine | fest | projektplan 6 |
| 2026-09-23 | **M3-Schnitt, Fassung 1:** `docs/plans/m3-dritte-quelle-und-interface.md` mit M3-00 bis M3-15 und den Vorentscheidungen P1–P18. Feste ADR-Nummern: `adr/0009` dritte Quelle (M3-01), `adr/0010` Zählwürfel (M3-05), `adr/0011` Adapter-Interface (M3-14). Die Viewer-Pakete V1/V2 des Projektplans heißen künftig VP1/VP2; „V-n“ bleiben einzelne Aufgaben des Viewer-Strangs. M3-11 wird nach Annahme von `adr/0009` in Fassung 2 geschnitten | fest | plans/m3-dritte-quelle-und-interface.md; projektplan M3, 4 (Viewer-Strang) |
| 2026-09-23 | **M2 abgenommen** (PR #70, `git log`: Merge pull request #70 from oscipal/claude/funny-carson-acj3l2). Beleg je Kriterium in `docs/plans/m2-12-abnahme.md` | fest | plans/m2-12-abnahme.md; plans/m2-format-und-viewer.md §5 |
| 2026-09-23 | **Keine AOI im Log (M3-02, K-01/K-02):** neue Aufgabe **M3-16** (Stufe A, Sonnet hoch), läuft sofort: Access-Log jedes HTTP-Prozesses ohne Query-String, `configure_logging` und `RequestIdMiddleware` in jedem Prozess, `summarize_geometry` wo Geometrien geloggt werden. M3-08 wird erst nach M3-16 gemergt | fest | plans/m3-02-konformitaetsbericht.md §8; plans/m3-dritte-quelle-und-interface.md M3-08, M3-16; backend/earthx/logging.py; docker-compose.yml |
| 2026-09-23 | **Lizenzstufe an der Kachelroute (M3-02, K-03):** Abnahmekriterium von M3-11. Kacheln verlangen mindestens B11-Stufe „Anzeige“, der Download weiterhin „Processing“ | fest | plans/m3-02-konformitaetsbericht.md §8; plans/m3-dritte-quelle-und-interface.md M3-11; KLAERUNGEN B11; backend/earthx/api/tiler.py |
| 2026-09-23 | **Zielort der Zugriffsauflösung (M3-02, K-04):** `adr/0011` legt ihn als eigene Option fest (heute `api/tiler.py`); der Umbau ist der erste Schritt von M4, bevor `processing` die Auflösung braucht | fest | plans/m3-02-konformitaetsbericht.md §8; plans/m3-dritte-quelle-und-interface.md M3-14; architekturplan 6.1; adr/0011 (entsteht) |
| 2026-09-23 | **Datums-Fallback bleibt im Frontend (M3-02, K-22):** bestätigt die Entscheidung aus M2-07a vom 20.09.2026; Neubewertung mit der Verfügbarkeits-Zeitleiste in M5. `architekturplan.md` 5.2 passt M3-15 an | fest | plans/m3-02-konformitaetsbericht.md §8; plans/m3-dritte-quelle-und-interface.md M3-15; architekturplan 5.2; frontend/src/dateFallback.ts |
| 2026-09-23 | **M3-01: dritte Quelle entschieden, `adr/0009` angenommen** (Otto, Antworten auf §9). Quelle: Copernicus DEM GLO-30 direkt aus dem Bucket (F1), zugleich dritter Datensatz und erste Nicht-STAC-Quelle (F2). Materialisierung: Items in pgstac, erzeugt von einem Einmal-Befehl in `discovery` (F3). Coverage: Vereinigung der Kachel-Umrisse, als Fläche statt Dichte-Heatmap, `completeness = complete` — Lesart von ENTSCHEIDUNGEN §2: „Ausdehnung“ eines Einmal-Produkts ist die Vereinigung der Kachel-Umrisse (F5). Zenodo für M5 vorgemerkt, nur als Discovery- bzw. Metadatenquelle, nicht für Kacheln (Ratengrenze 133 Anfragen/min je IP) (F6). Lizenz des DEM unverändert wie `adr/0003` §11.1 (F4 entfällt). Offen für M3-11: Zeitangabe der Items, Asset-Host, Umgang mit den fehlenden Kacheln (`adr/0009` §10) | fest (Otto am 2026-09-23) | adr/0009; plans/m3-dritte-quelle-und-interface.md M3-01, M3-11; ENTSCHEIDUNGEN §2 |
| 2026-09-23 | Download folgt der Ansicht: heruntergeladen wird, was die Karte zeigt | fest | plans/m3-dritte-quelle-und-interface.md M3-17 |
| 2026-09-23 | Zuschnitt sichtbar: je Gruppe (Überflug, Gruppierungsschlüssel der Trefferliste, ab M3-12 aus der Registry) eine gemergte Datei; verschiedene Gruppen als getrennte Dateien im selben ZIP. Deckel 4096 px und 200 MB bleiben | Deckel ersetzt am 2026-09-24 (native Auflösung, 500 MB, P20) | plans/m3-dritte-quelle-und-interface.md M3-17 |
| 2026-09-23 | Ganze Szene sichtbar, eine Szene gewählt (COG): Download der Originaldatei direkt von der Quelle durch den Browser, ohne Umweg über die Plattform | fest | plans/m3-dritte-quelle-und-interface.md M3-17 |
| 2026-09-23 | Zarr (EOPF): Download immer als Zuschnitt, nie der ganze Speicher. Ohne AOI ist der Download-Knopf deaktiviert, Hinweis „Draw an AOI to download“ | fest | plans/m3-dritte-quelle-und-interface.md M3-17 |
| 2026-09-23 | Mehrere ganze Szenen gewählt: in M3 die Originale einzeln; ein gemergtes Mosaik ganzer Szenen je Überflug ist ein Processing-Job und für M4 vorgemerkt | Vorschlag | plans/m3-dritte-quelle-und-interface.md M3-17, §2 |
| 2026-09-23 | **M3-03: Python 3.12 für Backend, CI, Image und Cloud-Sitzung (F1 (1), F2 (1)).** CI (`backend`, `import-boundaries`) und Live-Smoke auf 3.12, Image auf `python:3.12-slim`, ruff `py312`. In der Sitzung aus dem Ubuntu-Paket des Images (`/usr/bin/python3.12`, 3.12.3), nicht per `uv` oder Umstellen von `python3`. 3.11 läuft in der CI nicht mit; die Job-Namen der Pflicht-Checks bleiben gleich. `zarr` bleibt auf 3.1.x, die Anhebung ist eine eigene Entscheidung. Ein Test hält die fünf Stellen der Version gleich. Schließt die offene Zeile „Wechsel von Python 3.11 auf 3.12 als eigene Aufgabe“ | fest | plans/m3-03-python-312.md; .github/workflows/ci.yml, live-smoke.yml; backend/Dockerfile; pyproject.toml; backend/requirements.txt; scripts/setup-cloud-session.sh; cloud-umgebung.md §2, §7 |
| 2026-09-23 | **M3-03 F3 (1): Die offenen Bereiche gehen mit 3.12 mit.** Unter 3.12 lösen `rasterio` 1.5.1 (GDAL 3.12.4 statt 3.10.3), `numpy` 2.5.3, `rioxarray` 0.23.0, `numcodecs` 0.17.0 und `pyproj` 3.8.0 auf; keine neuen Deckel. Offline-Suite grün; den Lesepfad gegen die echten Quellen prüft Otto lokal je Datensatz mit einer Kachel und einem Zuschnitt-Download | fest | plans/m3-03-python-312.md §2.3; backend/requirements.txt |
| 2026-09-23 | **M3-03 F4 (1): Der SessionStart-Hook setzt `.venv/bin` über `CLAUDE_ENV_FILE` in den `PATH` der Sitzung**, damit ein nacktes `pytest` das des venv ist und nicht das Werkzeug des Images ohne Backend-Pakete. Wirkung in einer neu gestarteten Sitzung noch zu belegen (PR) | fest, in frischer Session belegt am 2026-09-23 | scripts/setup-cloud-session.sh; cloud-umgebung.md §7 |
| 2026-09-23 | **M3-05: Zählwürfel als ADR-Entwurf `docs/adr/0010-coverage-zaehlwuerfel.md`.** Gemessen: Earth Search kappt schon einen einzelnen Monat ab z8 bei 10 000 Zellen (Ursache im `stac-server`-Quelltext: `geotile_grid` ohne Paging); vollständig wird ein Monat nur über eine Zerlegung in Kacheln (z9: 46 Anfragen, 17 s; z8: 13–17 Anfragen, 6 s), geprüft je Monat mit Regel V. D26-Würfel (z9 × Monat × 5 Wolkenklassen) für Sentinel-2: ≈ 25 800 Anfragen Erstbefüllung, ≈ 11,9 Mio. Zeilen, 1,4 GB mit AOI-Index, Latenz synthetisch 3–903 ms; geänderte Monate erkennt eine Anfrage auf `updated`, die Quelle pflegt auch Monate von 2024 nach. Empfohlen: schlanker Würfel z8 × Monat ohne Wolkenklasse, nur für den Weltüberblick ohne AOI, Anstoß im `harvester`, Live-Weg bleibt für alles andere; EOPF ohne Würfel. **Antworten zu §9 (Otto, 23.09.): alle wie empfohlen** — F1 (a) z8 × Monat ohne Wolkenklasse, nur ohne AOI; F2 (a) Zeiträume ohne ganze Monate live; F3 (a) Weltüberblick aus dem Würfel höchstens z7; F4 (a) Schleife im `harvester` mit Advisory Lock; F5 (a) EOPF ohne Würfel, `sample` bleibt; F6 (a) Weltüberblick ohne AOI immer z6 vorgezogen als M3-19. Offenes Risiko zu Regel V: das in der Zerlegung verlorene Item vom Juni 2020 (§10) | fest (Otto am 2026-09-23) | adr/0010; plans/m3-dritte-quelle-und-interface.md M3-19; adr/0004 §5; plans/m2-05-coverage.md F3; plans/m2-format-und-viewer.md D26 |
| 2026-09-23 | Bau des Würfels nicht in M3 (P13); vorgemerkt für M5 zusammen mit der Arbeit am `harvester` | Vorschlag | adr/0010 §8; plans/m3-dritte-quelle-und-interface.md P13 |
| 2026-09-23 | Download-Deckel: 200 MB gelten für die Ausgabe (Summe der Dateien im ZIP, je Gruppe gemerged), nicht für die Summe der Eingabe-Items (ändert M2-06). Der Arbeitsspeicher des `tiler` wird über eine eigene, gemessene Grenze geschützt | Wert ersetzt am 2026-09-24 (500 MB, F10a) | plans/m3-dritte-quelle-und-interface.md M3-18 |
| 2026-09-23 | Zuschnitt auf die AOI-Geometrie: Beim Polygon-AOI sind alle Pixel außerhalb des Polygons nodata; die Datei hat die Ausdehnung der Bounding Box. Bisher wurde auf die Bounding Box zugeschnitten | ersetzt am 2026-09-23 | plans/m3-dritte-quelle-und-interface.md M3-18 |
| 2026-09-24 | **M3-16 umgesetzt.** `configure_logging`/`RequestIdMiddleware` laufen jetzt in allen vier Prozessen (`api`, `tiler`, `worker`, `harvester`); `RequestIdMiddleware` schreibt genau eine Access-Log-Zeile pro Anfrage (Methode, Pfad, Status, Dauer, Request-ID), nie den Query-String — `docker-compose.yml` startet jeden Prozess mit `--no-access-log`, damit uvicorns eigenes Access-Log (das den vollen Query-String schreibt) entfällt. Ersetzt die engere Zwischenlösung aus PR #76 F7a (Schwärzungsfilter für `bbox`/`intersects` im Access-Log von `api`, unmerged): Der jetzige Weg lässt gar keinen Query-String mehr durch, nicht nur die beiden benannten Schlüssel; F7a wird beim Rebase von M3-08 auf diesen Stand hinfällig. **Nebenfund:** `httpx`/`httpx2` loggen selbst bei INFO die volle angefragte URL samt Query-String (`gateway`-Aufrufe mit `intersects`/`bbox`) — vom root-Logger vererbt, sobald der auf INFO steht; `configure_logging` hebt beide auf WARNING an, damit dieser Weg keine AOI ausgibt | fest | plans/m3-02-konformitaetsbericht.md K-01, K-02; plans/m3-dritte-quelle-und-interface.md M3-16; backend/earthx/logging.py; backend/earthx/api/main.py, api/tiler.py, jobs/main.py, discovery/main.py; docker-compose.yml; PR #76 |
| 2026-09-24 | **M3-16, Review-Nachträge zu PR #85 (Otto).** (1) `configure_logging` schaltet `uvicorn.access` jetzt selbst ab (dieselben zwei Zeilen wie uvicorns `--no-access-log`), nicht nur der Startbefehl in `docker-compose.yml` — ein lokaler Start ohne das Flag (z. B. `uvicorn earthx.<modul>.main:app` nach README) war sonst genauso ungeschützt; Test startet einen echten `uvicorn.Server` mit `access_log=True`. (2) `gateway.client._query_digest` war ein unsalzener `sha256` des Query-Strings — bei `bbox` mit seiner begrenzten Präzision und Spanne per Durchprobieren zurückrechenbar; jetzt ein HMAC mit einem bei Prozessstart zufälligen Schlüssel (`secrets.token_bytes(32)`), der nie geloggt oder gespeichert wird | fest (Otto, Review) | PR oscipal/earthX#85; backend/earthx/logging.py; backend/earthx/gateway/client.py |
| 2026-09-23 | **M3-08: `intersects` und `ids` durchgereicht.** Plan-Schritt gemessen gegen Earth Search und die EOPF STAC API (beide können beides, AND-verknüpft mit anderen Filtern, ungeprüfte Koordinaten und Selbstschnitte gehen ohne Wort durch); Otto nahm alle sieben Empfehlungen an: Stützpunkt-Deckel 1000 mit Rückfall auf die bbox samt Hinweis, `ids`-Deckel 100, Adapter erklären ihre Fähigkeiten (`UnsupportedFilter`, mit Test, dass jeder bekannte Adapter beides kann — K8), Punkt-AOI sucht mit dem Punkt statt dem Quadrat, alle GeoJSON-Geometrietypen mit eigener Gültigkeitsprüfung (shapely), Frontend sucht nur noch per `POST`. Umgesetzt in derselben Sitzung, Draft-PR steht. Nebenfund: `SearchParams`-Konstruktion lag außerhalb des Fehlerfangs in `_federated_page` — eine ungültige `bbox` hätte über die echte API `500` statt `400` ergeben; behoben. **Merge-Sperre:** Der parallele M3-02-Bericht (K-01/K-02) verlangt ein Access-Log ganz ohne Query-String in jedem Prozess als eigene Aufgabe M3-16; bis dahin bleibt dieser PR offen (Otto, M3-02 §8 Frage 1) | fest (Otto: alle Empfehlungen) | plans/m3-08-intersects-ids.md; plans/m3-dritte-quelle-und-interface.md M3-08, M3-16; plans/m3-02-konformitaetsbericht.md K-01, K-02; adr/0005 |
| 2026-09-24 | **M3-08: Nachweis auf Ottos Nachfrage, keine Koordinate in `gateway`/Adapter-Logs oder im Such-Cache.** `gateway.Gateway._log` schreibt je Anfrage nur Host, Pfad, einen Hash des Query-Strings, Status und Dauer, nie den Rumpf; für `POST /search` gibt es ohnehin keinen Query-String. Der 500-Zeichen-Auszug eines `UpstreamError` (Antwort der Quelle) erreicht kein Log, auch nicht bei Wiederholung. `search_fingerprint` hängt `intersects`/`ids` nur zum Hashen an; die Cache-Zeile trägt nur die zurückgegebenen Items, die Seitenmarke nur `{v, d, h, m}` — nie die Geometrie. Belegt mit drei Tests gegen `gateway`/Adapter-Logs (Erfolg, Wiederholung, eine Ablehnung der Quelle, die die Koordinate selbst zitiert — Empfindlichkeit von Hand geprüft) und zwei gegen die echte Cache-Tabelle und den `next`-Link. Der Zugriffslog-Filter aus M3-08 (`AccessLogRedactionFilter` in `api`) bleibt eine Zwischenlösung; M3-16 ersetzt oder übernimmt ihn | fest (Nachweis erbracht) | plans/m3-08-intersects-ids.md §8.1; backend/tests/integration/test_api_federating.py (TestNoCoordinatesReachAnyLog, TestSearchCacheAndPageTokenCarryNoGeometry); adr/0005 Regel III |
| 2026-09-24 | **M3-08: `main` (mit M3-16) geholt, die Zwischenlösung aus F7a entfernt.** `AccessLogRedactionFilter`, `redact_query_string` und `redact_access_log_coordinates` (`earthx/logging.py`) samt Verdrahtung in `api/main.py` und ihren Tests gelöscht — M3-16s eigener Weg (uvicorns Access-Log ganz abgeschaltet, `RequestIdMiddleware` in jedem Prozess) übernimmt die Aufgabe vollständig, wie in M3-16s eigener Log-Zeile vom selben Tag angekündigt | fest | plans/m3-08-intersects-ids.md; backend/earthx/logging.py, api/main.py, tests/test_logging.py; plans/m3-dritte-quelle-und-interface.md M3-16 |
| 2026-09-23 | Weltüberblick ohne AOI: Anfrage auf den sichtbaren Ausschnitt, Zellstufe folgt dem Kartenzoom; ersetzt adr/0010 Antwort 6a | fest | adr/0010 §9; plans/m3-19-weltueberblick-ausschnitt.md; plans/m3-dritte-quelle-und-interface.md M3-19 |
| 2026-09-24 | **M3-19, Antworten zu §7 (Otto), alle wie empfohlen.** F1 (a) Zielzahl 2 000 Zellen je Bildschirm (1920×1080: Zellstufe ≈ Kartenzoom + 4); F2 (a) Footprints auch ohne AOI zeigen, sobald die Quelle im Ausschnitt unter 500 Treffer meldet; F3 (a) Cache-Frist der Route unverändert (5 min bei offenem Rand, kein neuer Parameter); F4 (a) Farbskala bleibt aus dem Maximum der jeweiligen Antwort abgeleitet. Umgesetzt in derselben Session | fest (Otto am 2026-09-24) | plans/m3-19-weltueberblick-ausschnitt.md §7; frontend/src/coverage.ts, store.ts, components/MapView.tsx |
| 2026-09-24 | **M3-09: Vollauflösung zeigt nur den Zuschnitt (F1 (1), F2 eigener Wortlaut, F3).** Jede Kachel wird im Browser per `maplibregl.addProtocol` auf die AOI maskiert (`frontend/src/aoiClip.ts`); die AOI verlässt dafür nie den Browser, nichts wird zwischengespeichert. Aus einem Knopf wurden zwei — „Crop to AOI“ und „View full selection“ unter der gemeinsamen Beschriftung „Full resolution“ — beide führen in dieselbe Vollauflösung. Die Karte stapelte überlappende Szenen bisher umgekehrt zum Download (letzte Auswahl oben statt erster gültiger Pixel); behoben. F3 erweitert zu „Download folgt der Ansicht“: eine als ganze Szene ohne Zuschnitt angeheftete Ansicht lädt beim Download auch keinen AOI-Zuschnitt, sondern die Szene selbst; mehrere ganze Szenen gleichzeitig bleiben M3-17 vorbehalten (Otto nannte ausdrücklich die Einzahl) | F3 zurückgenommen am 2026-09-24 (Review-Nachträge zu #84) | plans/m3-09-zuschnitt-ansicht.md; frontend/src/aoiClip.ts, mapLayers.ts, store.ts, layers.ts, download.ts, components/ViewBar.tsx, components/ViewerControls.tsx |
| 2026-09-24 | **M3-09, Review-Nachtrag zu PR #84 (Otto).** F3 aus der vorigen Zeile zurückgenommen: der Download eines angehefteten Layers folgt nicht der Ansicht, sondern verlangt weiter immer eine AOI, wie vor M3-09 — ein Zuschnitt über den bestehenden Endpunkt (Szenen-bbox als „AOI“) deckelt auf `MAX_OUTPUT_SIDE_PX` und liefert damit kein Original, wie P19 es für eine ganze COG-Szene verlangt. Die Prüfung dazu steht jetzt als Fundstelle bei M3-17 | fest (Otto, Review) | PR oscipal/earthX#84; plans/m3-09-zuschnitt-ansicht.md; plans/m3-dritte-quelle-und-interface.md M3-17; frontend/src/download.ts |
| 2026-09-24 | **M3-09, weiterer Nachtrag zu PR #84 (Otto).** Beschriftung „Crop to AOI“ → „Crop & merge to AOI“: der Download dieser Ansicht führt schon seit vor dieser Aufgabe je Gruppe (Überflug) zusammen, verschiedene Gruppen bleiben getrennt (P19) — der Name beschreibt nur die bestehende Regel, kein neues Verhalten | fest (Otto) | PR oscipal/earthX#84; plans/m3-09-zuschnitt-ansicht.md §6; frontend/src/components/ViewBar.tsx |
| 2026-09-24 | **M3-09, neuer Plan-Schritt zu PR #84 (Otto, §10 in `plans/m3-09-zuschnitt-ansicht.md`), Session hält an.** Im Zuschnitt-Modus soll die Auswahl-Umrandung je Gruppe genau einen Ring zeigen (AOI ∩ Vereinigung der Szenen-Umrisse der Gruppe) statt je Szene, und der Layer-Manager (Ottos Empfehlung, geteilt) einen Eintrag je Gruppe mit eigenem Download zeigen. Vorgeschlagen für Vereinigung/Schnitt im Browser: `polygon-clipping` (gemessen 9,8 kB gzip für beide Operationen, nimmt/liefert direkt `Polygon`/`MultiPolygon.coordinates`), gegen `polybooljs` (kleiner, fremdes Format), `martinez-polygon-clipping` (von turf wegen Robustheitsfehlern verlassen), `polyclip-ts`/`@turf` (robuster, aber schwerer) und `clipper-lib` (am schwersten) abgewogen. Antimeridian bleibt eine bekannte Lücke: Rückfall auf die alte Pro-Szene-Umrandung für eine betroffene Gruppe statt falscher Geometrie | Vorschlag, wartet auf Otto (F4/F5) | PR oscipal/earthX#84; plans/m3-09-zuschnitt-ansicht.md §10 |
| 2026-09-24 | **M3-09, F3 endgültig bestätigt (Otto).** Der Download eines angehefteten Layers verhält sich wieder wie vor PR #84 — er verlangt immer eine AOI, unabhängig vom Zuschnitt-Modus der Ansicht; kein Download ganzer Szenen über den Zuschnitt-Endpunkt. Bekannte Abweichung bis M3-17 im Plan-Text festgehalten: „View full selection“ mit gezeichneter AOI zeigt in der Karte die ganze Szene, der Download liefert aber weiter den AOI-Zuschnitt. In M3-17 unter „Im Plan-Schritt vorschlagen“ vermerkt: der Zuschnitt-Endpunkt taugt nicht für Originale (Deckel `MAX_OUTPUT_SIDE_PX`), das Original muss direkt von der Quelle kommen; für Zarr ohne AOI fehlt noch der deaktivierte Download-Knopf mit Hinweis „Draw an AOI to download“ | fest (Otto) | PR oscipal/earthX#84; plans/m3-09-zuschnitt-ansicht.md §7; plans/m3-dritte-quelle-und-interface.md M3-17 |
| 2026-09-24 | **M3-09 §10: F4 (a) `polyclip-ts`, F5 (ja) — umgesetzt (Otto).** F4: `polyclip-ts` direkt eingebunden statt `polygon-clipping` oder `@turf/union`/`@turf/intersect` — robuster gegen Selbstschnitte und Grenzfälle, die rund 5 kB gzip Unterschied sind Otto das wert. Jede Vereinigung/jeder Schnitt läuft in `try`/`catch`; schlägt sie fehl oder übersteigt die kombinierte Lon-Spanne einer Gruppe (Footprints + AOI) 180°, fällt nur diese eine Gruppe auf ihre Pro-Szene-Umrandung zurück (`frontend/src/groupOutline.ts`, Test für beide Rückfälle). F5: im Zuschnitt-Modus legt `addCurrentToLayers` einen `MapLayer` je Gruppe an statt einem für die ganze Auswahl, jeder mit eigenem Download — ohne Änderung an `LayerManager.tsx`, `LayerRestore` oder dem Download-Endpunkt | fest (Otto am 2026-09-24) | plans/m3-09-zuschnitt-ansicht.md §10; frontend/src/groupOutline.ts, mapLayers.ts, store.ts, geoUtils.ts; package.json (`polyclip-ts`) |
| 2026-09-24 | **M3-09: Plan-Fassung „2“ auf „Fassung 1.3“ umbenannt (Otto).** „Fassung 2“ ist für den Aufgabenschnitt von M3-11 reserviert und wird in `plans/m3-09-zuschnitt-ansicht.md` nicht mehr verwendet | fest (Otto) | plans/m3-09-zuschnitt-ansicht.md |
| 2026-09-24 | **M3-18 freigegeben mit F1 (1), F2 (1), F3 (2), F4 (1), F5 (2), F6 (2), F7 (1), F8 (1).** Ausgabegröße vor dem Lesen aus AOI und Item-Metadaten (`gsd`, `raster:bands`/`bands`), ersetzt die alte Items×Assets-Rechnung, die ab drei Items jeden Zuschnitt abwies. `threads=1` hält den Speicher unabhängig von der Item-Zahl (gemessen: bis zu 1,6 GB am 200-MB-Deckel statt bisher bis 4,5 GB); 25 Items je Anfrage. Keine Grenze für gleichzeitige Zuschnitte im Prozess (F5) und kein AOI-Stützpunkt-Deckel im Download (F6) in M3. Download-Route nutzt jetzt die GDAL-Umgebung von `gateway` (F7, Nebenbefund aus M2-06) | F5 (keine Gleichzeitigkeitsgrenze) und 200-MB-Deckel ersetzt am 2026-09-24 (F10a) | plans/m3-18-download-deckel-maske.md |
| 2026-09-24 | **M3-18, Abweichung von F3 (2):** Der Zuschnitt maskiert die AOI über eine GDAL-interne Maskenband (kein Alpha-Band, gleiche Bandzahl bei 1 und vielen Items), aber **ohne** den in F3 (2) zusätzlich vorgesehenen `nodata`-Tag — Grund in der nächsten Zeile | ersetzt am 2026-09-24 (Bug B: nodata-Tag der Quelle, Maske als eigene Datei) | plans/m3-18-download-deckel-maske.md §4.3, §9 |
| 2026-09-24 | **M3-18, Nebenfund, noch nicht durch Otto bestätigt:** Ein maskierter Zuschnitt wird als ZSTD statt DEFLATE komprimiert. Gemessen (mehrere hundert Läufe je Variante, außerhalb von `pytest` reproduziert): `cog_translate` mit DEFLATE und einer GDAL-internen Maske erzeugte in dieser GDAL-Version reproduzierbar gelegentlich eine unlesbare COG (`ZIPDecode: incorrect data check`); ein nachträglich gepatchter `nodata`-Tag war eine eigene, eigenständige Fehlerquelle (GDAL schreibt dabei den IFD um). ZSTD zeigte den Fehler in keinem von über 550 Läufen. Folge: ein sehr altes, nicht gegen aktuelles libtiff gebautes Programm könnte die heruntergeladene Datei nicht lesen | ersetzt am 2026-09-26 (M3-22: Use-after-free im Test-Helfer, nie eine defekte Datei ausgeliefert) | plans/m3-18-download-deckel-maske.md §9 |
| 2026-09-24 | **M3-18 §9, Aktualisierung/Verschärfung (Otto-Review von PR #86):** Die oben gemessene Korruption tritt reproduzierbar **auch bei ZSTD** auf (nicht nur DEFLATE) — bestätigt auf dem PR-Stand vor dieser Sitzung (Commit 2b2e5e1), nicht neu eingeführt. Vermutlich die eigentliche Ursache von Befund A („could not be read from the source“ bei bestimmten Items): ein COG-Schreibfehler in unserem eigenen Code, vorher durch einen zu breiten `except (RasterioError, …)` fälschlich als Quellen-Lesefehler gemeldet (jetzt korrigiert, siehe nächste Zeile). Reproduzierbar nur beim Ausführen der echten `pytest`-Datei, nicht in isolierten Stress-Skripten außerhalb von `pytest` (0/220) — deutet auf eine Interaktion mit `pytest` selbst hin, nicht auf einen reinen GDAL-Datenrace. Die Korruption selbst bleibt offen; Otto entscheidet über das weitere Vorgehen (GDAL/libtiff-Version, `NUM_THREADS`, Schreibstrategie) | ersetzt am 2026-09-26 (M3-22: Use-after-free im Test-Helfer, nie eine defekte Datei ausgeliefert) | plans/m3-18-download-deckel-maske.md §12.1 |
| 2026-09-24 | **Bug A und Bug B aus Ottos lokaler Prüfung von PR #86 behoben.** Bug A: die Download-Route meldete jeden `RasterioError` (nicht nur echte Lesefehler) als „could not be read from the source“ — jetzt nur noch `RasterioIOError`/`GatewayError`, alles andere erreicht `build_app`s bestehende Handler und kommt als 500 mit echtem Traceback und Request-ID im Log an; zusätzlich schließt `_image_to_asset_crop_bytes` eine Lücke, in der eine AOI, die die echten (oft gedrehten) Daten eines Mosaiks verfehlt, als leerer „Erfolg“ statt als `AoiOutsideItems` durchging. Bug B: die weißen Pixel im Zuschnitt von S2B_T32TNT_20260922T102300_L2A kamen von einer über alle Bänder kombinierten Gültigkeitsmaske (`.any(axis=0)`), die ein Pixel in jedem Band ausblendete, sobald irgendein Band an seinem eigenen nodata-Wert lag (gemessen: 8600/686/2437 nodata-Pixel je Band in einem 1024×1024-Fenster, nur 23 in allen drei gleichzeitig — der Rest war echte, sehr dunkle Bilddaten). Der Zuschnitt trägt jetzt den Quellen-`nodata`-Wert als reinen Tag statt eines kombinierten internen Maskenbands; die Pixelwerte selbst waren nie betroffen | fest | plans/m3-18-download-deckel-maske.md §12; backend/earthx/access/download.py, backend/earthx/api/tiler.py; backend/tests/earthx/access/test_download_edge_cases.py |
| 2026-09-23 | Download immer in nativer Auflösung; gröber nur auf ausdrückliche Wahl des Nutzers; keine automatische Verkleinerung (ersetzt `max_size` 4096 aus M2-06) | fest | plans/m3-18-download-deckel-maske.md §10 |
| 2026-09-23 | Exporte über der synchronen Grenze in voller Auflösung werden in M4 als Job umgesetzt | Vorschlag | plans/m3-18-download-deckel-maske.md §10.4 |
| 2026-09-24 | **M3-18 §10.8, F10a–c beantwortet (Otto).** F10a: 500 MB roh als Ausgabe-Deckel (`MAX_TOTAL_OUTPUT_BYTES`), zusätzlich höchstens ein Download ab 100 MB roh gleichzeitig je tiler-Prozess (`LARGE_DOWNLOAD_THRESHOLD_BYTES`) — ein weiterer bekommt 503 mit `Retry-After` und der Meldung „another large download is running, try again shortly“; `docker-compose.yml` setzt dem tiler-Dienst kein Speicherlimit (geprüft, kein `mem_limit`/`deploy.resources`/`memory:`). F10b: fensterweises Lesen umgesetzt (`_write_native_windowed_cog`, nur für einen einzelnen COG-Item-Zuschnitt in nativer Auflösung; Mosaik, Zarr und ausdrücklich gröbere Auflösung bleiben beim bisherigen Ganzarray-Pfad). F10c: generischer Auflösungsfaktor (Native, 2×, 4×, 10×, `RESOLUTION_FACTORS`), keine neuen Registry-Felder; Dialog zeigt die entstehende Auflösung in Metern je Asset, wo aus `gsd`/`proj:transform` ablesbar; die Abweisungsmeldung über dem Deckel nennt den kleinsten passenden Faktor als Vorschlag, nie automatisch gewählt | fest (Otto am 2026-09-24) | plans/m3-18-download-deckel-maske.md §10.8; backend/earthx/access/download.py, backend/earthx/api/tiler.py, frontend/src/{api,download,store}.ts, frontend/src/components/DownloadDialog.tsx |
| 2026-09-24 | **Maske statt nodata (Otto), ersetzt die Zeile vom 2026-09-23 über nodata außerhalb des Polygons.** Die Datendatei im Download-ZIP ist immer ein reiner Bounding-Box-Zuschnitt: jedes Pixel behält Wert und Gültigkeit der Quelle, unabhängig davon, ob es innerhalb des AOI-Polygons liegt — nie wird ein Pixel wegen der Polygonform auf nodata gesetzt oder maskiert. Was das Polygon tatsächlich abdeckt, steht stattdessen in einer eigenen Datei je Asset: ein einfaches uint8-GeoTIFF im selben Raster wie die Daten, 1 = innerhalb, 0 = außerhalb (keine COG — eine Binärmaske braucht keine Kachelpyramide). Die AOI-Geometrie selbst liegt zusätzlich einmal je ZIP als `aoi.geojson` bei. Ein Rechteck-AOI bekommt ebenfalls eine Maskendatei, bewusst der Einheitlichkeit halber: ein Rechteck in WGS84-Lon/Lat ist nicht zwangsläufig am (womöglich gedrehten) Pixelraster der Quelle ausgerichtet, sodass zuverlässiges Erkennen des Sonderfalls fast dieselbe Rasterisierung bräuchte, die diese Datei ohnehin immer erzeugt | präzisiert am 2026-09-24 | plans/m3-18-download-deckel-maske.md §11; backend/earthx/access/download.py, backend/earthx/readers/zarr_reader.py |
| 2026-09-23 | **Präzisierung zur Maske (Otto), M3-18 §13.** ZIP-Inhalt je Gruppe unverändert: Datendatei und Maske je Asset, dazu einmal je ZIP die AOI als GeoJSON — immer die *Original-AOI*, wie gezeichnet oder hochgeladen, nie beschnitten. Neu ist die Ausdehnung von Datendatei und Maske: nicht mehr die Bounding Box der ganzen AOI, sondern die Bounding Box von (AOI ∩ Vereinigung der Footprints der Szenen dieser Gruppe) — dieselbe Geometrie, die die gelbe Umrandung aus PR #84 (`groupOutline.ts`) schon vor dem Download zeigt. Deckt eine Szene die AOI nur teilweise ab, enden Datei und Maske an der Szene; deckt sie die AOI ganz ab, bleibt es wie bisher die Bounding Box der AOI. Die Maske selbst ist unverändert 1 innerhalb des AOI-Polygons, 0 außerhalb, auf demselben Raster wie die Datendatei — sie markiert weiterhin nur die AOI, nie den (womöglich kleineren) Footprint; das Nodata der Szene bleibt Sache der Datendatei | fest | plans/m3-18-download-deckel-maske.md §13; backend/earthx/access/download.py, backend/earthx/api/tiler.py; backend/tests/earthx/access/test_download_edge_cases.py |
| 2026-09-26 | MinIO-Images seit 24.09.2026 nicht mehr öffentlich; Übergang bitnamilegacy/minio per Digest, nur CI/lokal | fest | docker-compose.yml, README.md |
| 2026-09-26 | Neubewertung MinIO wird vorgezogen: Spike zu einem Ersatz (Garage, SeaweedFS, RustFS u. a.) vor M4 | fest | docker-compose.yml |
| 2026-09-26 | **CI-Rot auf PR #86 behoben: `TestWindowedReadMatchesTheWholeArrayRead` war ein Test-Helfer-Bug, kein Fehler im Produktivcode.** `test_download_mask.py::_open_zip_member` gab `memfile.open()` zurück, ohne das `MemoryFile`-Objekt selbst am Leben zu halten — es wurde beim Rücksprung aus der Funktion für den Garbage Collector frei, dessen `__del__` den `/vsimem/`-Puffer auflöste, während das zurückgegebene, noch offene Dataset ihn weiter brauchte (nicht deterministisch, daher die intermittierenden „TIFF directory is missing required ImageLength field“/„Computed scanline size is zero“-Fehler). Bestätigt als Ursache: mit einer Referenz, die das `MemoryFile` am Leben hielt, liefen 60/60 Wiederholungen fehlerfrei, ohne sie 29/60 fehl; `_write_native_windowed_cog` und die anderen Produktionsfunktionen in `download.py` halten ihr jeweiliges `MemoryFile` immer bis zum `.read()` der fertigen Bytes am Leben und sind von diesem Bug nicht betroffen — erklärt **nicht** Befund A (§12.1), der unabhängig davon in der Fehlerabbildung lag und bereits behoben ist. Fix: `_open_zip_member` als Context-Manager, der `MemoryFile` und Dataset verschachtelt offenhält (Muster aus `test_download.py` übernommen); neuer Helfer `_open_bytes` für dieselbe Lücke am zweiten, baugleichen Aufruf. Beiläufig: die `> 0.99`-Toleranz im Rechteck-Test war selbst ein Symptom dieses Bugs, jetzt exakter Vergleich; die verbleibende Toleranz im Schräg-AOI-Test ist unabhängig davon echt (zwei unabhängig berechnete Ausgaberaster, Nearest-Neighbor-Rundung am Rand). Volle Suite 4× hintereinander grün (1246/1246), 50 erzeugte COGs einzeln mit `rio cogeo validate` geprüft | fest | plans/m3-18-download-deckel-maske.md §12.1; backend/tests/earthx/access/test_download_mask.py |
| 2026-09-23 | **M3-17 hängt nicht mehr von M3-12 ab (Otto).** Die Gruppierung je Überflug hat PR #84 (M3-09) gebaut (`store.groups`, `groupIndexOfItem`, ein Layer je Gruppe); M3-17 verwendet sie wieder und baut keine neue datensatzspezifische Stelle. Den festen Eigenschaftsnamen der Gruppierung in die Registry zu räumen bleibt bei M3-12 | fest | plans/m3-dritte-quelle-und-interface.md §3; plans/m3-17-download-folgt-ansicht.md |
| 2026-09-26 | **M3-Schnitt, Fassung 2:** M3-11 in M3-11a (materialisierte Quellen in Registry, Dispatch und Item-Abruf, K-03, K-05), M3-11b (DEM-Adapter, Einmal-Befehl in `discovery`, Registry-Eintrag) und M3-11c (Coverage über eigene Items, K-06) geschnitten; neuer Spike M3-20 (Ersatz für MinIO → `adr/0012`); P20–P24 halten die Entscheidungen vom 23./24.09.2026 fest; M3-17 hängt nicht mehr an M3-12; Statusspalte in §3 | fest | plans/m3-dritte-quelle-und-interface.md |
| 2026-09-26 | **Zählwürfel: kein Bau auf Vorrat.** In M5 je Datensatz entscheiden; nur bauen, wo die Weltansicht gemessen zu langsam oder zu grob ist. Vorher prüfen, ob die Zählung über eigene Items im Harvester den Würfel überflüssig macht. Ersetzt den Vorschlag „Bau des Würfels vorgemerkt für M5“ | fest | adr/0010; plans/m3-dritte-quelle-und-interface.md |
| 2026-09-26 | **Bestätigt (Otto):** Mehrere ganze Szenen werden in M3 als Originale einzeln geladen; ein gemergtes Mosaik ganzer Szenen je Überflug wird in M4 als Processing-Job umgesetzt. Setzt die Zeile vom 2026-09-23 („Mehrere ganze Szenen gewählt …“, Vorschlag) auf fest | fest | plans/m3-dritte-quelle-und-interface.md P19 |
| 2026-09-26 | **Bestätigt (Otto):** Exporte über dem synchronen Deckel in voller Auflösung werden in M4 als Job umgesetzt; kein Teil-Download in M3. Setzt die Zeile vom 2026-09-23 („Exporte über der synchronen Grenze …“, Vorschlag) auf fest | fest | plans/m3-dritte-quelle-und-interface.md P20 |
   | 2026-09-26 | **Durchsicht auf Widersprüche:** neue Aufgaben M3-21 (Doku-Abgleich, Stufe A) und M3-22 (gelegentlich unlesbare Download-Dateien, Stufe B, vor M3-11a); P12 um die zwei Knöpfe aus M3-09 ergänzt | fest | plans/m3-dritte-quelle-und-interface.md |
| 2026-09-26 | **M3-06a: `POST /aoi/upload` für GeoJSON, KML und Shapefile-ZIP, umgesetzt (Otto: F1 (1), F2 (1), F3 (1), F4 (1), F5 (2), F6 (1)).** Reine Python-Lösung (`pyshp`, `defusedxml`, `pyproj`), Prüfung in `access/aoi_upload.py`, Route in `api/aoi_upload_route.py` im Prozess `api`. F5 (2) heißt: mehrere polygonale Features werden zu einer `MultiPolygon` vereinigt statt abgewiesen; eine Mischung mit einem nicht-polygonalen Typ bleibt ein `400`. **Nebenfund beim Umsetzen von F3:** Starlettes `max_part_size` deckelt nur normale Formularfelder, keine Datei-Teile eines `multipart/form-data`-Uploads — ein Datei-Teil über 1 MiB hätte trotz gesetztem `max_part_size` in eine echte `SpooledTemporaryFile`-Platte-Datei gespult (mit einer echten `TestClient`-Anfrage nachgemessen: 1 048 576 Byte kein `rollover()`, 1 048 577 Byte einer). Die Route liest deshalb den rohen Anfragekörper selbst in Häppchen (`filename` als Query-Parameter statt `FormData`-Feld) statt über `UploadFile`/`multipart/form-data` — derselbe Deckel (1 MiB), dasselbe Ziel („nie auf Platte“), nur der Übertragungsweg ist ein anderer als in F3 ursprünglich vorgeschlagen | fest | plans/m3-06a-aoi-upload-backend.md; backend/earthx/access/aoi_upload.py, backend/earthx/api/aoi_upload_route.py, backend/earthx/api/main.py; backend/requirements.txt |
| 2026-09-26 | **M3-21 umgesetzt:** Status-Spalte in `ENTSCHEIDUNGSLOG.md` an neun Stellen auf ersetzte/zurückgenommene Werte nachgezogen (Text unverändert), zwei COG-Korruptionsbefunde mit „→ M3-22“ markiert; `projektplan.md` auf Version 1.3 (M3-Tabelle P1–P24, M3-00 bis M3-22), Nicht-STAC-Quelle als entschieden markiert, MinIO-Ersatz als offene Entscheidung ergänzt; `adr/0004` und `architekturplan.md` je um einen Nachtrag zu P22 bzw. P23/M3-20 ergänzt, Originaltext stehen gelassen | fest | plans/m3-dritte-quelle-und-interface.md M3-21; ENTSCHEIDUNGSLOG.md, projektplan.md, adr/0004-coverage-map.md, architekturplan.md |
| 2026-09-26 | **M3-17 freigegeben, alle Fragen (F1–F6) nach Empfehlung (Option 1).** Original-Download nur die Assets der sichtbaren Darstellung; Liste von Links im Dialog statt eines automatischen Sammel-Downloads; Gruppen im Request als `groups: list[list[str]]`; COG/Zarr-Unterscheidung über neues `earthx:format` (Abbildung des vorhandenen `DatasetConfig.format`, kein neues Registry-Feld); Download aus der Trefferliste folgt der AOI wie bisher; ein PR trotz Überschreiten des 400-Zeilen-Richtwerts, weil die Teile nur zusammen prüfbar sind | fest | plans/m3-17-download-folgt-ansicht.md §9, §10 |
| 2026-09-26 | **Review von PR #88 (M3-17) behoben:** eine übersprungene Gruppe (verfehlt die AOI) meldet die Antwort jetzt über `X-Total-Groups`/`X-Skipped-Groups`, der Dialog zeigt einen englischen Hinweis statt zu schließen; `earthx:format` in `architekturplan.md` 5.1 um Bedeutung, erlaubte Werte und Herkunft ergänzt; B11 für den Original-Download belegt (Stufe „Katalogeintrag“ erlaubt schon einen Link zur Quelle, keine Lizenzsperre nötig), Attribution/Terms im Dialog jetzt mit Komponententest | fest | plans/m3-17-download-folgt-ansicht.md §12 |
| 2026-09-26 | **M3-22 freigegeben und umgesetzt (Otto: F1 (1), F2 (1), F3 (1), F4 (1)).** Ursache der „unlesbaren Download-Dateien“: ein Use-after-free im Leseweg der Tests, nie eine defekte Datei. rasterio `MemoryFile(bytes)` legt den `/vsimem/`-Puffer ohne Kopie und ohne Besitz auf das `bytes`-Objekt (`VSIFileFromMemBuffer(…, 0)`, rasterio 1.5.1); wird das `MemoryFile` eingesammelt, während ein daraus geöffnetes Dataset noch liest, liest es freigegebenen Speicher. Berichtigt den Mechanismus-Satz der Zeile „CI-Rot auf PR #86 behoben“ (rasterio hat kein `MemoryFile.__del__`). Belege: Produktivpfad 0 Fehler in 2 800 vollständig zurückgelesenen Dateien (DEFLATE/ZSTD, seriell und 8 Threads), alter Test-Helfer 22/30 bzw. 18/30 Läufe rot mit genau den bekannten Meldungen. F1: jede Daten- und Maskendatei wird vor der Auslieferung vollständig zurückgelesen (jeder Block, jede Overview, gleiches Raster), einmal neu erzeugt, sonst `500` mit Request-ID; ZIP-CRC geprüft; 5–9 % Mehrzeit am 500-MB-Deckel. F2: DEFLATE statt ZSTD für Datendatei und Maske. F3: statischer Test gegen `MemoryFile(<bytes>)` außerhalb von `with` in Produktiv- und Testcode. F4: kein Issue bei rasterio. Stresstest (50 Runden) läuft in jeder CI | fest | plans/m3-22-unlesbare-downloads.md; plans/m3-18-download-deckel-maske.md §9; backend/earthx/access/download.py, backend/earthx/api/tiler.py; backend/tests/test_memoryfile_lifetime.py, backend/tests/earthx/access/test_download_verification.py, test_download_stress.py |
| 2026-09-26 | **M3-06b freigegeben und umgesetzt (Otto: F1 (1), F2 (1), F3 (1), plus zwei Ergänzungen).** Frontend ruft ab jetzt `POST /aoi/upload` (M3-06a) statt im Browser zu parsen; `aoiFile.ts` (`readAoiFile`) bildet die Route auf Store-Geometrie oder eine englische Fehlermeldung ab, `AoiExtras` sperrt den Knopf während der Anfrage, Dev-Proxy um `/aoi` ergänzt. Verhaltensänderungen für Nutzer: `LineString`-Dateien werden abgewiesen, mehrere Polygone in einer Datei werden vereinigt statt „erste gewinnt“, eine KML-Datei muss auf `.kml` enden, Dateien über 1 MB werden abgewiesen. Ergänzung 1 (F2): die 1-MB-Konstante im Frontend (`AOI_UPLOAD_MAX_BYTES`) trägt einen Kommentar auf `MAX_UPLOAD_BYTES` in `backend/earthx/access/aoi_upload.py`, damit beide zusammen geändert werden. Ergänzung 2 (Befund aus M3-06b, in diesem PR statt im Frontend-Umfang behoben): Eine Datei mit nur Linien (KML `LineString`, Shapefile-Linien) oder nur `MultiPoint` ergab bisher die nicht hilfreiche „no usable geometry found in the file“ — `_combine_geometries` nennt jetzt den gefundenen Typ ausdrücklich („the file contains only lines/points; an AOI needs a polygon or a point“), mit Backend-Tests für KML und Shapefile | fest | plans/m3-06b-aoi-upload-frontend.md; frontend/src/aoiFile.ts, frontend/src/api.ts, frontend/src/components/ControlPanel.tsx, frontend/vite.config.ts; backend/earthx/access/aoi_upload.py; prototyp-inventar.md F3 |
| 2026-09-26 | **M3-11a freigegeben und umgesetzt (Otto: F1–F8 je Option 1).** Neues Pflichtfeld `SourceInfo.item_holding` (`federated`/`materialized`, ohne Vorgabewert) trennt „welcher Adapter“ von „föderiert oder eigene Items in pgstac“ (K-05) — vorher entschied allein, ob `earthx:source.adapter` ein bekannter `AdapterKind` war. `FederatingCoreCrudClient._holding_of` liest das Feld aus dem Collection-Dokument; fehlt es oder ist es unbekannt, antwortet die Route `500` statt eines stillen Rückfalls (F5, adr/0005 Regel I) — jede Collection in pgstac stammt aus `catalog.load`, also ist das nur bei einer nicht neu geladenen Datenbank oder einem Fehler der Fall. `api/tiler.py::build_item_source` bekommt einen zweiten Weg für Items aus pgstac (`catalog.pgstac.fetch_item`, über `pgstac.get_item`); `_lifespan` baut die Item-Quelle jetzt aus der Registry, die `build_app` tatsächlich bekommen hat, nicht mehr aus dem modulweiten `REGISTRY`. K-03 (Lizenzstufe an der Kachelroute, am 23.09.2026 schon entschieden): `_check_display_allowed` weist Kachel, `/statistics`, `/info`, `/point` und `/tilejson.json` unterhalb der Stufe „Anzeige“ mit `403` ab, vor jedem Item-Abruf; die Registry verlangt zusätzlich, dass ein Eintrag der Stufe `catalog` kein `viewer`-Feld trägt. F4: alle vier neuen Regeln (Item-Haltung ↔ Coverage-Anbieter, föderiert ⇒ kein Harvest-Lauf, `catalog`-Stufe ⇒ kein Viewer) laufen zusätzlich zur Konstruktionsprüfung als benannte Tests über jeden Registry-Eintrag (`TestEveryEntry` in `test_registry.py`), damit ein künftiger fehlerhafter Eintrag in der CI auffällt statt erst zur Laufzeit mit `500`. `catalog.pgstac` bekommt `upsert_items` (blockweise, `pgstac.upsert_items`, weist eine föderierte Quelle und ein Item mit fremder `collection` ab) für den Einmal-Befehl aus M3-11b. Bestehende Datensätze unverändert (`item_holding=federated`); eine bestehende Datenbank braucht einmal `catalog-load` neu (README) | fest | plans/m3-11a-materialisierte-quellen.md; plans/m3-dritte-quelle-und-interface.md M3-11a; architekturplan.md 5.1; README.md; backend/earthx/catalog/registry.py, collection.py, datasets.py, pgstac.py; backend/earthx/adapters/__init__.py; backend/earthx/api/federating_client.py, tiler.py |
| 2026-09-26 | **M3-20: ADR-Entwurf `adr/0012-objektspeicher.md` (Ersatz für MinIO).** MinIO Community Edition ist ungepflegt (README seit 12.02.2026 „no longer maintained“), frei nur noch als Bau aus dem Quelltext; `bitnamilegacy/minio` liegt vor einem Sicherheitsfix vom 15.10.2025 und bekommt keine Updates. Garage 2.4.1, SeaweedFS 4.47, RustFS 1.0.0 und MinIO aus dem Quelltext in der Cloud-Sitzung selbst gebaut und mit denselben S3- und GDAL-Prüfungen gemessen (ohne Docker). Empfehlung: Garage ersetzt MinIO in compose und CI, SeaweedFS als Rückfallweg, Umstellung als eigene Stufe-B-Aufgabe noch in M3; neutrale `.env`-Namen. Offen für M4: Weg vom eigenen Code zum eigenen Speicher, da `gateway` nur `https` und öffentliche Adressen zulässt. Sechs Fragen an Otto (F1–F6), alle nach Empfehlung (Option 1) beantwortet; Umstellung als M3-23 | fest (Otto am 2026-09-26) | adr/0012; plans/m3-dritte-quelle-und-interface.md (M3-20, M3-23); docker-compose.yml; .github/workflows/ci.yml; architekturplan 12.1 |
| 2026-09-26 | **M3-11c freigegeben und umgesetzt (Otto: F1 (1), F2 (1), F3 (1), F4 (1), F5 (2), F6 (1)), mit zwei Anpassungen aus der `time_range`-Entscheidung von M3-11b (PR #99).** Die Coverage-Route wählt ihren Weg jetzt ausschließlich aus dem Registry-Eintrag (K-06): eine neue Funktion `adapters.coverage` löst `(AdapterKind, CoverageProvider)` auf eine der beiden föderierten Wege auf (Earth Search, EOPF), statt dass die Route selbst zwischen `aggregate_coverage` und `sample_coverage` wählt. Neu ist der dritte, lokale Weg `catalog.local_coverage.area_coverage`: für `local-sql` mit `single_coverage_product` die Vereinigung der Item-Footprints aus `pgstac.items` als GeoJSON-`MultiPolygon` (Feld `area` der Antwort), zugeschnitten auf `bbox`/`intersects` (eine `bbox` über die Datumsgrenze als zwei Rechtecke) und den Zeitfilter, ohne Toleranz-Vereinfachung, `completeness=complete`; ein Selbstschnitt in `intersects` wird vor jeder SQL-Anfrage mit `400` abgewiesen (keine Koordinate erreicht Antwort oder Log). Cache-Frist fest 5 Minuten, Schlüssel ohne `level`/`max_cloud_cover`. **Zwei Anpassungen (fest, Otto 26.09.2026):** (1) Für jeden Datensatz mit `capabilities.time_range=False` verwirft die Route den Zeitfilter vor jedem Weg, nicht nur dem lokalen — die Fläche bzw. Dichte ist dieselbe für jeden Zeitraum; (2) `max_cloud_cover` wird auf dem Flächenweg ignoriert statt mit `400` abgewiesen (ersetzt F5-Empfehlung „400“ aus dem Plan-Schritt). Beide Fälle stehen im neuen Antwortfeld `ignored_filters` (z. B. `["datetime", "max_cloud_cover"]`), damit die Oberfläche sie anzeigen kann, statt dass die Antwort stillschweigend eine engere Frage beantwortet. `adr/0004` §5 trägt einen Nachtrag mit der Messung an der echten DEM-Kachelliste (Weltfläche 35–89 kB, rund 1 s; Ausschnitt einstellig bis niedrig zweistellig ms) | fest (Otto am 2026-09-26) | plans/m3-11c-coverage-eigene-items.md §8; adr/0004 §5; plans/m3-dritte-quelle-und-interface.md M3-11c; backend/earthx/catalog/coverage.py, local_coverage.py; backend/earthx/adapters/__init__.py; backend/earthx/api/coverage_route.py, dependencies.py; frontend/src/api.ts |
