# Pip alternative to environment.yml (for users who prefer a plain venv).
# NOTE: rasterio/rio-tiler pip wheels bundle their own GDAL. If you already
# have a system GDAL you may prefer the conda route (see environment.yml).
fastapi>=0.110
uvicorn[standard]>=0.29
pystac-client>=0.8
rasterio>=1.3
rio-tiler>=6.4
rio-cogeo>=5.2
shapely>=2.0
httpx>=0.27
requests>=2.31
python-dotenv>=1.0
pydantic-settings>=2.2
# pgstac schema migration for the compose `pgstac-migrate` service
# (docs/plans/m1-08-compose-topologie.md); pinned to the version measured
# working in docs/cloud-umgebung.md §5.
pypgstac[psycopg]>=0.9,<0.10
